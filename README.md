# 🔥 Java Algorithm Study

매일 꾸준히 알고리즘 문제를 풀고 기록합니다.

## 📚 학습 목표
- **언어:** Java 11+
- **목표:** 하루 1문제 이상 풀기 & 효율적인 자료구조 고민하기
- **기간:** 2025.12.08 ~ (진행중)

---

### Day 106 (2026.07.03) - Hash Combination 공식 및 Queue 인덱스 보존 제어

알고리즘 연속 풀이 106회차. 해시맵을 활용한 다항식 조합 연산 최적화와, 큐(Queue) 내부 데이터의 가변 순회 과정에서 고유 인덱스를 보존하는 다차원 배열 적재 테크닉을 완벽히 마스터함.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.2 | **[의상](https://school.programmers.co.kr/learn/courses/30/lessons/42578)** | `Clothes.java` | **HashMap 기반 빈도수 카운팅 및 조합 경우의 수 도출**. <br> 1. 의상 종류(`strings[1]`)를 Key로 설정하여 `getOrDefault`를 통해 종류별 수량을 정확히 누적함. <br> 2. 모든 조합을 직접 탐색하는 대신, '해당 의상을 입지 않는 경우'를 포함한 `(수량 + 1)`의 총승을 구한 뒤, 모두 입지 않는 공집합 상태(`- 1`)를 제외하는 수학적 최적화를 구현함. |
| Lv.2 | **[프로세스](https://school.programmers.co.kr/learn/courses/30/lessons/42587)** | `Process.java` | **다차원 배열 Queue 적재를 통한 원본 인덱스 보존 및 전수조사**. <br> 1. 대기 큐 내부에서 데이터 순서가 유동적으로 변할 때 원래 위치를 잃지 않도록 `Queue<int[]>` 구조에 `[원본 인덱스, 우선순위]`를 세트로 묶어 상태를 보존함. <br> 2. `for-each` 루프로 대기 큐 전체를 전수조사하여 현재 우선순위보다 높은 프로세스가 있는지 검증하고, 탈출 시점의 카운트를 누적하여 `location` 일치 여부를 판별함. |

#### 💡 Today I Learned (TIL)
1. **자료구조 내 객체 지향적 데이터 보존:**
   - 단순 1차원 데이터 큐잉 시 순서 뒤틀림으로 발생하는 상태 상실 문제를 해결하기 위해, 배열(`int[]`) 인터페이스를 큐의 제네릭 요소로 활용하여 원본 컨텍스트(인덱스)를 끝까지 트래킹하는 설계 감각을 익힘.
2. **수학적 접근을 통한 시간 복잡도 최소화:**
   - 완전 탐색이나 백트래킹으로 조합의 가짓수를 직접 구하려 하지 않고, 공집합 개념을 대입한 대수적 연산으로 변환하여 $O(N)$의 단순 루프만으로 해시 맵 결과 값을 처리하는 최적화 능력을 배양함.

### Day 105 (2026.07.01) - Stack/Queue 연산 제어 및 DFS 백트래킹 탐색

알고리즘 연속 풀이 105회차. 조건부 반복 메커니즘을 활용한 가변 큐(Queue) 데이터 일괄 소모 패턴과, 명시적 반환문 없이 제어 흐름 필터링으로 종단하는 DFS 백트래킹 기법을 완벽히 마스터함.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.2 | **[기능개발](https://school.programmers.co.kr/learn/courses/30/lessons/42586)** | `Development.java` | **Queue를 활용한 연속 데이터 일괄 배포(Batch Processing) 구현**. <br> 1. 작업 완료일 계산 시 `Math.ceil` 내부 정수 나눗셈 함정을 `double` 캐스팅으로 방어함. <br> 2. 단일 `if`문 대신 내부 `while`문을 결합하여, 기준일보다 일찍 끝나는 연속된 대기 작업들을 한방에 큐에서 털어내며 묶음 배포 크기를 정확히 산출함. |
| Lv.2 | **[피로도](https://school.programmers.co.kr/learn/courses/30/lessons/87946)** | `Fatigue.java` | **조건부 재귀 호출을 통한 암묵적 종료 방식의 DFS 백트래킹**. <br> 1. 던전 최대 개수가 8개인 점에 착안, $8!$의 모든 경우의 수를 완전탐색하는 DFS 구조를 설계함. <br> 2. 함수 상단에 명시적인 `return` 분기점을 두는 대신, `for`문 내부에서 방문 여부와 피로도 진입 조건을 사전 필터링(Guard Clause)하여 유효한 경로만 재귀 호출하고 `}`를 통해 자연스럽게 자원을 회수하는 백트래킹을 구현함. |

#### 💡 Today I Learned (TIL)
1. **제어 흐름을 통한 재귀 종료의 이해:**
   - 재귀 함수는 반드시 상단에 `if (base_case) return;`을 적어야만 멈추는 것이 아니라, 호출부의 조건문 필터링을 통해 진입 자체를 차단함으로써 `void` 함수의 말단(`}`)에서 자연스럽게 묵시적 리턴을 유도할 수 있음을 체화함.
2. **반복 제어문 선택의 본질 (`if` vs `while`):**
   - 선행 작업에 종속된 후행 데이터의 누적 개수가 가변적일 때, 단순 1회성 검사(`if`)가 아닌 조건 충족 시까지 지속해서 큐를 소모하는 내부 `while`문의 필요성을 논리적으로 검증하고 구현에 적용함.

### Day 104 (2026.06.30) - Map & Set 자료구조 연계 및 가변 컬렉션 활용 구현

알고리즘 연속 풀이 104회차. 복잡한 문자열 기반의 실시간 상태 관리와 다중집합 연산을 처리하기 위해, `Map`과 `Set`을 결합한 다차원 자료구조 핸들링 및 조건부 파싱 최적화를 마스터함.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.2 | **[오픈채팅방](https://school.programmers.co.kr/learn/courses/30/lessons/42888)** | `OpenChat.java` | **HashMap 유저 상태 스냅샷 관리와 가변 배열(List) 최적화**. <br> 1. 유저 ID의 가변적인 닉네임을 실시간 덮어쓰기(`put`) 형식으로 동기화하여 최종 상태를 $O(1)$로 유지함. <br> 2. `Leave` 로그의 인덱스 예외 처리를 분기문 내부로 격리하고, 동적 크기의 정답 메시지 적재를 위해 `ArrayList` 사용 후 `toArray()`로 안전하게 변환함. |
| Lv.2 | **[뉴스 클러스터링](https://school.programmers.co.kr/learn/courses/30/lessons/17677)** | `NewsClustering.java` | **다중집합의 빈도수(Frequency) Map 매핑 및 Set을 활용한 키 병합**. <br> 1. 문자열 2글자 슬라이싱 시 `substring(i, i+2)`를 활용해 문자 연산 오차를 방지하고 `Character.isLetter()`로 예외 필터링 함수를 모듈화함. <br> 2. 두 맵의 키를 `HashSet`으로 병합하여 중복 없는 전체 다중집합의 스코프를 확보한 뒤, `Math.min`/`Math.max`와 `getOrDefault`의 조합으로 교집합/합집합을 일괄 연산함. `0 / 0 (NaN)` 분모 예외 제어 포함. |

#### 💡 Today I Learned (TIL)
1. **자료구조의 결합을 통한 복잡도 감소:**
   - 다중집합의 `min`, `max` 연산 규칙을 배열로 직접 비교하려 하지 않고, 두 `Map`의 키셋을 `HashSet`으로 통합하여 순회하는 구조를 설계함. 이로 인해 분기 조건이 극적으로 단순해져 가독성과 안전성이 동시에 향상됨을 경험함.
2. **사고의 점진적 확장:**
   - 복잡한 구현 문제를 만났을 때 믹서기(`makeMap`) 같은 하위 헬퍼 메서드를 독립적으로 분리하여 메인 로직의 추상화를 높이는 훈련을 완수함. 코드의 모듈화가 실전 디버깅 속도를 얼마나 단축시키는지 체화함.

### Day 103 (2026.06.29) - 자료구조(Map) 활용 시뮬레이션 및 수학적 오버플로우 예외 제어

알고리즘 연속 풀이 103회차. 대규모 문자열 파싱과 정렬 조건을 만족하기 위한 자료구조 설계, 그리고 진법 변환 후 발생하는 대형 수의 소수 판별 시 자료형 오버플로우 예외를 제어하는 실전 최적화 기법을 마스터함.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.2 | **[주차 요금 계산](https://school.programmers.co.kr/learn/courses/30/lessons/92341)** | `ParkingFee.java` | **TreeMap을 활용한 자동 키 정렬과 정수 나눗셈 올림 오차 제어**. <br> 1. 입차/출차가 반복되는 상태를 `HashMap`으로 관리하고, 차량 번호 순 출력을 위해 최종 누적 시간은 `TreeMap`에 적재하여 별도 정렬 없이 $O(N \log N)$으로 해결함. <br> 2. `Math.ceil()` 사용 시 정수 나눗셈(`int / int`)에 의한 소수점 절삭 버그를 방지하기 위해 피연산자를 `double`로 명시적 캐스팅 처리함. |
| Lv.2 | **[k진수에서 소수 개수 구하기](https://school.programmers.co.kr/learn/courses/30/lessons/92305)** | `PrimeInBaseK.java` | **split("0")을 이용한 조건부 파싱 및 제곱근 루프 변수의 자료형 최적화**. <br> 1. 복잡한 4가지 인접 0 조건을 `split("0")` 문자열 파싱 한 줄로 단순화하고 빈 문자열(`""`) 및 `"1"` 찌꺼기를 예외 처리함. <br> 2. 변환된 대형 수가 `int` 범위를 초과하므로 소수 판별 루프 변수 `i`를 `long`으로 선언하여, `i * i` 연산 시 발생하는 `int` 오버플로우와 그로 인한 무한 루프(시간 초과)를 원천 차단함. |

#### 💡 Today I Learned (TIL)
1. **자료형 바운더리에 대한 고해상도 시야:**
   - 알고리즘 입력 파라미터가 소형(`n <= 1,000,000`)이더라도 진법 변환이나 문자열 결합을 거치며 숫자가 거대화될 수 있음을 인지함. 파싱 타겟뿐만 아니라 탐색 루프 제어 변수(`i * i`)까지 `long` 타입의 사정권 안에 두어야 안전함을 깊이 체화함.
2. **시뮬레이션 정렬 조건의 최적화:**
   - 정렬 조건이 까다로울 때는 데이터를 다 뽑고 정렬하기보다, 삽입과 동시에 정렬을 유지하는 `TreeMap` 같은 이진 검색 트리 기반 자료구조를 선택하는 것이 코드 라인을 줄이고 실수를 방지하는 무기임을 재확인함.

### Day 102 (2026.06.23) - 격자 제어 축 고정과 주기 연산을 활용한 2차원 시뮬레이션 마스터

알고리즘 연속 풀이 102회차. 2차원 좌표계에서 데이터 유실 없이 경계면만 정밀 타격하는 인덱스 고정 기법과, 복잡한 상태 변수 없이 수학적 주기를 활용해 격자 내 방향 전환을 자동화하는 고도화된 배열 제어 피지컬을 확립함.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.2 | **[행렬 테두리 회전하기](https://school.programmers.co.kr/learn/courses/30/lessons/77485)** | `MatrixBorderRotation.java` | **빈 방 생성 후 역방향 당기기(Shifting) 메커니즘을 통한 데이터 유실 차단**. <br> 1. 시계 방향 회전 시 값을 정방향 대입하면 데이터 도미노 현상으로 기존 값이 오버라이트되어 파괴됨을 인지함. <br> 2. 왼쪽 위 모서리(`matrix[x1][y1]`)를 `temp` 변수에 백업하여 합법적 '빈 방'을 확보한 후, 시계 반대 방향으로 인덱스를 역추적하며 데이터를 당겨오는 사중 단독 루프 파이프라인 구축. <br> 3. 이중 루프 대신 단차원 축 고정 방식(`y1`, `x2`, `y2`, `x1`)을 사용하여 내부 데이터를 제외한 순수 테두리 경계면만 저격 성공. |
| Lv.2 | **[삼각 달팽이](https://school.programmers.co.kr/learn/courses/30/lessons/68645)** | `TriangleSnail.java` | **나머지 연산(`% 3`)을 통한 방향 전환 자동화와 인덱스 오프셋 보정**. <br> 1. [아래 -> 오른쪽 -> 대각선 위]의 3가지 회전 주기를 상태 변수 없이 외곽 루프 인덱스 `i % 3` 메커니즘 하나로 캡슐화함. <br> 2. 내부 루프 진입 시 무조건 선제 실행되는 `x++` 연산 타이밍을 고려하여 시작 좌표를 `-1` 오프셋으로 역산 배치하는 빌드업 전개. <br> 3. 평탄화(Flatten) 작업 시 직각삼각형 채우기 영역의 경계 조건(`j <= i`)을 정밀 타격하여 인덱스 참조 예외(`ArrayIndexOutOfBounds`)를 원천 봉쇄함. |

#### 💡 Today I Learned (TIL)
1. **오버라이트(Overwrite)를 활용한 공백 취급 가설의 증명:**
   - 자바 배열에서 물리적 `null`을 만들지 않더라도, 데이터를 안전 가옥(`temp`)에 백업했다면 해당 공간은 기획적으로 '빈 방' 취급하여 마음껏 덮어씌워도 안전하다는 인과관계의 순서를 코드 흐름으로 제어함.
2. **다차원 배열 탐색의 기하학적 제어:**
   - 2차원 배열을 무조건 이중 루프로 면 분할 탐색하는 관성에서 벗어나, 특정 인덱스를 벽으로 고정하고 단독 루프를 선형으로 긋는 '선 탐색 피지컬'과 직각삼각형 구조에 맞춘 유효 영역 경계 필터링(`j <= i`)의 중요성을 완벽히 체화함.

### Day 101 (2026.06.19) - 수학적 규칙 증명을 통한 워프와 롤백(Rollback) 기반 시뮬레이션 고도화

알고리즘 연속 풀이 101회차. 대규모 데이터셋에서 탐색을 배제하고 수학적 규칙으로 정답을 유도하는 메커니즘과, 복잡한 예외 조건을 '선 조치 후 전수조사' 형태로 단순화하는 고도화된 시뮬레이션 설계 기법을 마스터함.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.2 | **[줄 서는 방법](https://school.programmers.co.kr/learn/courses/30/lessons/12940)** | `LineUp.java` | **팩토리얼 가계부와 몫/나머지 연산을 이용한 $O(N^2)$ 탐색 워프**. <br> 1. $N=20$ 환경에서 전체 순열($20! \approx 240$경)을 구하는 DFS 방식은 시간 초과가 발생함을 제약사항을 통해 사전 증명함. <br> 2. 자릿수별 남은 칸수의 팩토리얼 크기(`blockSize`)를 이용해, 몫(`/`)으로는 건너뛸 덩어리(카드 인덱스)를 찾고 나머지(`%`)로는 다음 타겟 인덱스를 축소 리셋하는 수학적 파이프라인을 구축함. |
| Lv.3 | **[기둥과 보 설치](https://school.programmers.co.kr/learn/courses/30/lessons/60061)** | `PillarAndBeam.java` | **3차원 공간 배열 설계와 선 구조물 변형 후 전수조사(isValid) 검사**. <br> 1. 한 좌표에 기둥과 보가 동시 적재되는 특성을 반영해 `boolean[n+3][n+3][2]` 구조를 설계하고 인덱스 패딩으로 에러를 원천 차단함. <br> 2. 구조물 변경 시 주변의 수많은 예외 분기를 일일이 잡는 대신, 일단 설치/삭제를 반영한 후 전체 지도의 합법성을 검사하고 위반 시 원상복구(Rollback)하는 패러다임으로 복잡도를 혁신적으로 낮춤. <br> 3. 별도의 정렬 함수 없이 `x -> y -> type` 순서의 삼중 루프 순회만으로 출력 조건을 자동 만족시킴. |

#### 💡 Today I Learned (TIL)
1. **확신을 만드는 수학적 역추적 기법:**
   - 구현 전 제약사항을 쪼개어 "왜 이 알고리즘만 가능한가"를 증명하는 근육을 키움. `k % blockSize`가 단순 연산이 아니라 '이미 지나쳐온 카드 덩어리들을 통째로 빼주는 수학적 필터'임을 본질적으로 체화함.
2. **시뮬레이션의 단순화와 안정성 패딩:**
   - 경계면 체크로 코드가 기괴해질 때는 배열 크기에 여유 공간(`+3`)을 주어 안전장치를 만들고, 복잡한 조건문은 `isValid()` 독립 메서드로 분리하여 캡슐화하는 것이 코드의 가독성과 확신을 모두 챙기는 실전 무기임을 확립함.

### Day 100 (2026.06.17) - 문자열 예외 처리의 최적화와 역발상 다대일(Many-to-One) BFS의 정립

알고리즘 연속 풀이 100회차 달성. 내장 메서드의 특성을 활용한 예외 처리 분기 최적화와, 대규모 쿼리 환경에서 탐색의 주체를 뒤집어 시간 복잡도를 혁신적으로 줄이는 역발상 그래프 최단거리 매커니즘을 마스터함.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.2 | **[JadenCase 문자열 만들기](https://school.programmers.co.kr/learn/courses/30/lessons/12951)** | `JadenCase.java` | **연속 공백 예외 방어와 상태 플래그 활용**. <br> 1. 연속된 공백 무더기로 인해 데이터가 유실되는 `split(" ")` 대신, `char` 단위 순회 파이프라인을 구축하여 공백을 완벽히 보존함. <br> 2. `Character.toUpperCase()`가 숫자를 만났을 때 원본을 그대로 유지하는 성질을 활용해 불필요한 조건 분기를 제거하고 우아하게 최적화함. |
| Lv.3 | **[부대복귀](https://school.programmers.co.kr/learn/courses/30/lessons/132540)** | `TroopReturn.java` | **대용량 그래프 환경에서의 인접 리스트 배열 선택과 역방향 BFS 트릭**. <br> 1. 정점 개수($N \le 100,000$) 환경에서 2차원 배열 대신 `ArrayList<Integer>[]` 인접 리스트 배열 서랍장을 설계하여 메모리 초과(OOM)를 원천 차단함. <br> 2. 수많은 출발지에서 각자 BFS를 돌리는 대신, 목적지(destination)를 유일한 시작점으로 삼는 역발상 탐색을 통해 단 1회의 BFS로 마스터 가계부를 완성하는 시간 최적화를 달성함. |

#### 💡 Today I Learned (TIL)
1. **인접 리스트 배열(`ArrayList<Integer>[]`)의 본질 체화:**
   - 서랍장의 방 번호(인덱스)는 출발지 라벨을 의미하고, 그 서랍 속 주머니(List) 내부에는 순수하게 목적지만 담긴다는 메모리 적재 구조를 명확히 이해함. 정점의 수가 대규모일 때 무조건 선택해야 하는 실전 무기임을 확립함.
2. **역발상(Reverse BFS) 탐색을 대절하는 3대 시그널 수립:**
   - 그래프가 '양방향(왕복 가능)'이고, 시작점의 개수가 '다수'인데 목적지가 '단 하나'인 다대일(Many-to-One) 구조이면서, 쿼리 크기가 터무니없이 클 때는 반드시 목적지를 큐에 넣고 출발해야 효율성을 통과할 수 있음을 본질적으로 이해함.

### Day 99 (2026.06.16) - 문자열 조작 감각 유지와 복합 백트래킹(DFS) 제어의 정석

알고리즘의 피지컬을 유지하기 위한 가벼운 문자열 유틸리티 조작과, '정렬+해시맵+DFS+백트래킹'이 복합적으로 결합된 고난도 카카오 기출 레벨 3 문제를 해결함. 데이터 구조의 유연한 활용과 깊이 있는 상태 복구 매커니즘을 완벽히 체화한 턴.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.2 | **[이진 변환 반복하기](https://school.programmers.co.kr/learn/courses/30/lessons/70129)** | `BinaryTransform.java` | **자바 기본 문자열 메서드 및 진법 변환 감각 유지**. <br> 1. 자바 `String`의 불변성(Immutable) 특성을 기억하고 `s = s.replace("0", "")` 형태로 장부를 갱신해야 함을 재확인함. <br> 2. `Integer.parseInt()`의 오용으로 인한 오버플로우 함정을 방어하고, 0이 제거된 문자열의 '길이'를 `Integer.toBinaryString()`에 주입하는 올바른 매커니즘을 정립함. |
| Lv.3 | **[여행경로](https://school.programmers.co.kr/learn/courses/30/lessons/43165)** | `TravelPath.java` | **사전순 정렬과 해시 맵을 결합한 백트래킹(DFS)의 정석**. <br> 1. 알파벳 순서가 앞서는 경로를 우선 탐색하기 위해, DFS 시작 전 2차원 배열 자체를 람다식(`Comparator`)으로 사전순 정렬해 두는 치트키를 활용함. <br> 2. 공항 이름이 문자열이기에 `Map<String, List<Ticket>>` 구조로 인접 리스트 장부를 구축하여 탐색 효율성을 극대화함. <br> 3. 길이 끊겼을 때 사용한 티켓 마킹을 해제(`isUsed = false`)하고 장바구니에서 잘못된 목적지를 빼내는(`list.remove`) 백트래킹 스코프 제어를 완벽히 마스터함. |

#### 💡 Today I Learned (TIL)
1. **자료구조 선택의 유연성 (String Index 매핑):**
   - 인덱스가 숫자가 아닌 문자열(`"ICN"`, `"JFK"`)로 주어질 때 무작정 2중 `ArrayList`를 고집하기보다, `Map`의 Key-Value 구조를 융합하여 인접 리스트를 구현하는 유연성을 확보함.
2. **백트래킹(소모와 복구)의 엄격한 스코프 관리:**
   - 상태를 복구하는 코드는 아무 데나 두면 안 되고, 반드시 내가 해당 자원을 '선택해서 소모했던' `if` 조건문 블록 내부(`dfs` 호출 바로 아래)에 가두어야 엄한 데이터가 오염되지 않는다는 흐름 제어의 본질을 완벽히 이해함.
3. **지속 가능한 알고리즘 루틴의 정립:**
   - 무겁고 매운 알고리즘만 풀다 보면 놓치기 쉬운 기본 문자열 조작 메서드들을 손풀기용으로 하나씩 배치함으로써, 실전 코테에서 발생할 수 있는 잔실수를 예방하고 뇌를 깨우는 영리한 스트레칭 전략을 구축함.

### Day 98 (2026.06.15) - 그래프 최단 경로 알고리즘의 확장과 다차원 다익스트라의 정립

단일 출발점 탐색의 한계를 넘어 모든 정점 간의 최단거리를 전수조사하는 플로이드-워셜 알고리즘과, 격자판 탐색에 '방향성'이라는 과거 상태를 누적하는 3차원 다익스트라 알고리즘을 완벽히 체화함. 조건과 제약사항에 따라 최적의 공간/시간 복잡도를 설계하는 실전 카카오 기출 변별력 패러다임을 마스터함.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.3 | **[합승 택시 요금](https://school.programmers.co.kr/learn/courses/30/lessons/72413)** | `TaxiFare.java` | **플로이드-워셜 기반의 다중 최단 경로 비용 최적화**. <br> 1. 정점 개수($N \le 200$)가 작고 경유지에 따른 전수조사가 필요하다는 점에서 3중 `for`문 플로이드-워셜을 도출함. <br> 2. `INF` 값을 3,000만으로 안전하게 설정하여 삼단논법 최솟값 갱신 시 발생하는 자바 정수 오버플로우 대참사를 완벽히 방어함. <br> 3. `(S ➔ K) + (K ➔ A) + (K ➔ B)` 수식을 전 정점에 대입하여 최솟값을 추출함으로서, 각자 갈 때(`K = S`)의 예외 케이스까지 하나의 루프로 완벽히 제어함. |
| Lv.3 | **[경주로 건설](https://school.programmers.co.kr/learn/courses/30/lessons/67259)** | `BuildTrack.java` | **3차원 배열을 활용한 격자판 기반 방향별 다익스트라 최적화**. <br> 1. 다음 행동(직진/코너)의 비용을 결정하기 위해 과거 진입 방향 상태를 기록해야 하므로 `dist[x][y][dir]` 3차원 장부를 설계함. <br> 2. 큐에서 객체를 단 한 번만 꺼내어 변수 오염을 막고, 이전 방향과 현재 방향을 비교해 직진(100원) 및 코너(600원) 요금을 유연하게 누적함. <br> 3. 탐색 완료 후 목적지 `(N-1, N-1)` 칸에 누적된 4가지 진입 방향의 영수증 중 `Math.min`으로 최종 최솟값을 낚아채어 정답을 도출함. |

#### 💡 Today I Learned (TIL)
1. **수학적 모델링을 통한 예외 처리의 최소화:**
   - '합승을 하는 경우'와 '각자 가는 경우'를 조건문 분기로 나누지 않고, 경유지 $K$가 출발지 $S$와 일치할 때 자동으로 `dist[s][s] = 0`이 되어 각자 요금이 산출되는 수학적 성질을 이용해 로직의 유연성을 극대화함.
2. **상태 관리를 위한 다차원 장부(버스 요금제 표)의 본질 체화:**
   - 단순 2차원 위치 장부로는 "어디서 어떤 버스를 타고 환승해서 왔는지" 알 수 없기에, '방향'이라는 차원을 한 층 더 파서 과거 기록을 보존해야만 정확한 할증 요금(코너 비용)을 계산할 수 있다는 공간 설계의 본질을 완벽히 이해함.
3. **독립적 고민과 삽질의 가치 재발견:**
   - 완성된 뼈대나 힌트를 먼저 보는 것보다, 큐에서 값을 꺼내는 타이밍이나 변수 누적 과정에서 발생하는 실수를 직접 마주하고 디버깅하는 과정이 알고리즘을 온전히 내 무기로 만드는 가장 빠른 지름길임을 깊이 체화함.

### Day 97 (2026.06.12) - 그래프 전수조사와 연결 요소(Connected Component) 분석

특정 출발점이 없는 대규모 관계망에서 플로이드-워셜(Floyd-Warshall) 알고리즘을 적용하는 실전 시그널을 확립함. 또한, 이미 완성된 인접 행렬 지도를 활용하여 독립된 정점 무리(연결 요소)의 개수를 정석 BFS로 최적화하여 찾아내는 피지컬을 다짐.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.3 | **[순위](https://school.programmers.co.kr/learn/courses/30/lessons/49191)** | `Rank.java` | **모든 정점 간의 관계 전수조사와 삼단논법 추론 ($O(N^3)$)**. <br> 1. 선수 수($N \le 100$)가 극도로 작다는 점과 모든 선수 간의 상대적 우위를 비교해야 한다는 점에서 플로이드-워셜을 도출함. <br> 2. `K` 선수를 중간 징검다리 삼아 `I가 K를 이기고, K가 J를 이기면 ➔ I는 J를 이긴다`는 삼단논법 장부 갱신 로직을 구현함. <br> 3. 최종 장부에서 특정 선수의 `승리 횟수 + 패배 횟수 == N - 1` 관계를 만족하는 정점만 골라내어 정확한 순위 확정 가능 인원을 색출함. |
| Lv.3 | **[네트워크](https://school.programmers.co.kr/learn/courses/30/lessons/43162)** | `Network.java` | **인접 행렬 기반의 독립 네트워크 무리(연결 요소) 개수 산출**. <br> 1. 이미 완벽히 주어진 `int[][] computers` 지도를 별도의 전처리 없이 직접 참조하여 메모리와 탐색 속도를 최적화함. <br> 2. 방문 장부(`visited[]`)를 전역적으로 유지하며, 아직 방문하지 않은 정점을 만날 때마다 네트워크 카운트를 올리고 BFS 엔진을 가동함. <br> 3. BFS 내부에서 `queue.offer(i)`와 같이 새로 개척한 이웃 인덱스를 정확히 큐에 탑승시켜 무한 루프와 배달 사고를 방지함. |

#### 💡 Today I Learned (TIL)
1. **플로이드-워셜을 대절하는 3대 타이밍 확립:**
   - 정점의 개수 $N$이 100 안팎으로 터무니없이 작고, 특정 시작점 없이 모든 정점 대 모든 정점의 전수조사가 필요하며, 'A➔B➔C' 형태의 삼단논법 징검다리 추론이 결합될 때는 묻지도 따지지도 말고 3중 `for`문 버스를 대절해야 함을 마스터함.
2. **장부의 역할 분담과 전처리 유의점:**
   - 입력 지도가 이미 주어졌을 때는 무분별한 전처리로 `visited` 장부를 오염시키면 안 되며, 오직 실제 탐색이 일어나는 선착순 방문 마킹용으로만 장부를 순수하게 관리해야 정확한 무리 분할이 가능하다는 점을 체화함.

### Day 96 (2026.06.10) - 최단 경로 탐색(Shortest Path)과 인접 리스트 선택 조건 확립

그래프의 간선 가중치 유무에 따라 최적의 최단 경로 알고리즘(다익스트라 vs BFS)을 선택하는 선구안을 장착함. 또한, 실전 코딩 테스트에서 중첩 리스트(`ArrayList<ArrayList<T>>`) 자료구조를 도입해야 하는 명확한 시그널과 격자판(미로) 문제와의 차이점을 완벽하게 정립함.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.2 | **[배달](https://school.programmers.co.kr/learn/courses/30/lessons/12978)** | `Delivery.java` | **가중치(시간)가 다른 그래프에서의 다익스트라(Dijkstra) 알고리즘 최적화**.<br>1. 도로마다 걸리는 시간이 다르므로 단순 BFS가 아닌 **우선순위 큐(`PriorityQueue`) + 그리디** 속성을 활용함.<br>2. `cost > dist[dest]` 방어막을 통해 중복 처리된 비효율적 경로를 `continue`로 즉시 솎아내어 연산 효율을 극대화함.<br>3. `newCost < dist[neighbor.to]` 최솟값 비교를 통해 `dist[]` 최단거리 장부를 실시간 갱신(`Relaxation`)함. |
| Lv.3 | **[가장 먼 노드](https://school.programmers.co.kr/learn/courses/30/lessons/49189)** | `FarthestNode.java` | **가중치가 없는(모든 거리가 1인) 그래프에서의 정석 BFS 선착순 탐색**.<br>1. 모든 간선의 길이가 동일할 때는 **무조건 먼저 도착하는 경로가 최단거리를 보장하는 '선착순 게임'**임을 이해하고 큐 기반의 BFS를 적용함.<br>2. 굳이 최솟값 비교 연산이 필요 없으므로 무한대 대신 **`-1`로 초기화하여 방문 체크와 거리 누적 장부 트릭**을 동시에 해결함 (`dist[neighbor] = dist[cur] + 1`).<br>3. 연산 종료 후 배열의 최댓값(`maxVal`)을 도출하여 1번 노드로부터 가장 먼 정점의 개수를 정확히 카운팅함. |

#### 💡 Today I Learned (TIL)
1. **중첩 리스트(`ArrayList<ArrayList<Integer>>`) 도입의 3대 실전 시그널 확립:** 입력 데이터가 간선 쌍(`int[][] edge`) 리스트로 주어질 때, 정점 개수 N이 대규모(1,000개 이상)일 때, 지문이 네트워크/인프라/인간관계를 다룰 때 중첩 리스트 서랍장을 설계해야 함을 인지함. 또한 상하좌우(dx, dy)로 움직이는 바둑판 격자형(미로 찾기) 구조에서는 큰 상자 없이 일반 2차원 배열(`int[][]`)로 즉시 탐색해야 함을 명확히 구분함.
2. **장부 초기화 트릭의 본질적 이유 분석:** 다익스트라는 끊임없이 더 작은 샛길 비용과 비교 연산을 수행해야 하므로 첫 방문 시 무조건 새 값으로 갱신되도록 장부를 무한대(`Integer.MAX_VALUE`)로 초기화하고, BFS는 선착순으로 먼저 밟는 경로가 곧 최단 거리이므로 단순 방문 체크를 위해 담백하게 `-1`로 초기화하여 방문 체크를 겸함.

### Day 95 (2026.06.09) - 그래프 구조 분석과 최소 신장 트리(MST)의 핵심 패러다임

무지성 그래프 탐색(BFS/DFS)의 한계를 인식하고, 정점의 진입/진출 차수(Degree)만을 활용한 최적화 기법을 체화함. 또한, 생애 처음으로 최소 신장 트리(MST)의 정석인 크루스칼(Kruskal) 알고리즘과 사이클 방어막인 유니온-파인드(Union-Find) 자료구조의 내부 동작 메커니즘을 완벽히 분석하고 장착함.

| 난이도 | 문제 이름 | 파일 이름 | 핵심 정리 |
|:---:|:---|:---|:---|
| Lv.2 | **[도넛과 막대 그래프](https://school.programmers.co.kr/learn/courses/30/lessons/258711)** | `GraphStructure.java` | **대용량 데이터(100만 개) 환경에서의 O(N) 차수(Degree) 제어**. <br> 1. 정적 배열 `in[]`, `out[]` 장부를 활용해 해시맵의 오버헤드(박싱/언박싱)를 방지하고 속도를 최적화함. <br> 2. 생성된 정점(`out >= 2, in == 0`), 막대 끝점 (`out == 0`), 8자 중심점(`out >= 2, in >= 2`)의 구조적 특징을 탐색 없이 단 한 번의 스캔으로 색출함. <br> 3. 도넛 그래프의 개수를 독립적으로 탐색하지 않고, `전체 그래프 수(생성 정점의 out) - 막대 - 8자` 수식으로 도출하는 수학적 인사이트를 적용함. |
| Lv.3 | **[섬 연결하기](https://school.programmers.co.kr/learn/courses/30/lessons/42861)** | `IslandConnect.java` | **크루스칼(Kruskal) 및 유니온-파인드(Union-Find) 기반의 최소 신장 트리(MST) 구축**. <br> 1. **탐욕법(Greedy):** 모든 노드를 연결하는 최저 비용을 구하기 위해 간선 배열을 비용(`cost`) 기준 오름차순으로 엄격히 정렬하고 시작함. <br> 2. **기차놀이 조장 장부:** `parent[]` 배열을 통해 각 섬의 소속 팀을 관리하고, `find()` 함수로 최종 대빵 조장을 비교하여 이미 연결된 정점 간의 중복 다리 건설(사이클)을 완벽 차단함. <br> 3. **경로 압축(Path Compression):** 재귀 하산 과정에서 `parent[x] = find(...)` 대입을 통해 중간 상사를 건너뛰고 총대장 번호를 장부에 다이렉트로 갱신하여 탐색 연산 속도를 $O(1)$에 가깝게 혁신함. |

#### 💡 Today I Learned (TIL)
1. **탐색을 이기는 장부 정리 (차수의 미학):** 100만 개라는 압도적인 데이터 크기 앞에서 무작정 큐를 파고 BFS를 돌리는 것이 아니라, 화살표의 시작과 끝점이라는 데이터의 본질적 규칙성을 찾아내어 복잡도를 $O(N)$으로 묶어버리는 역발상의 중요성을 깨달음.
2. **낯선 무기, 유니온-파인드(Union-Find)와의 첫 만남:** 처음 접했을 때는 재귀와 배열이 꼬여있어 생소했으나, '팀 합치기(Union)'가 장부를 기차놀이 형태로 빌드업해두면 '대빵 찾기(Find)'가 그 장부를 기반으로 상사를 타고 올라간다는 전체적인 인과관계를 완벽히 이해함.
3. **경로 압축의 실무적 가치:** 알고리즘의 최적화란 결국 고생스러운 연산 과정을 한 번 겪은 뒤, 다음 번 연산을 위해 메모리(장부)의 상태를 다이렉트로 펼쳐버리는(Flatten) 작업임을 코드를 통해 실시간으로 체화함. 앞으로 "최소 비용으로 모든 정점 연결" 구조가 나오면 기계적으로 꺼내 들 명품 템플릿을 장착함.

### Day 94 (2026.06.08) - 탐색의 시야 확장: 백트래킹 순열과 2배 차원 확대 치트키

거대하고 복잡한 최종 형태를 한 번에 구하려는 고정관념에서 벗어나, 데이터를 한 조각씩 장부에 누적하고 제어하는 '증분 전처리' 시야를 확보함. 또한 격자판 배열에서 컴퓨터의 무단횡단 버그를 막기 위한 좌표 2배 확대 트릭과 정석 BFS/DFS 결합 모델을 완벽히 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[단체사진 찍기 (1835)](https://school.programmers.co.kr/learn/courses/30/lessons/18352)** | **DFS 순열(Permutation) 생성과 조기 탈출(Early Exit) 조건 제어**. <br> 1. `visited=true ➔ dfs() ➔ visited=false` 백트래킹 3박자를 활용하여 8명의 친구를 일렬로 세우는 40,320가지의 모든 우주를 빈틈없이 탐색함. <br> 2. 문자열 인덱스 조회를 통해 "두 사람 사이에 낀 사람 수"를 `Math.abs(idx1 - idx2) - 1` 수식으로 정확히 도출하는 수학적 디테일을 챙김. <br> 3. 모든 조건을 AND로 검사할 때, 만족하는 것을 찾기보다 위반 사례를 찾는 즉시 `return false`로 솎아내는 범죄자 솎아내기 분기를 통해 연산 속도를 최적화함. |
| Lv.3 | **[아이템 줍기 (84021)]** | **좌표 2배 뻥튀기 트릭과 3상태 지도를 활용한 외곽 테두리 분리**. <br> 1. **무단횡단 버그 방지:** 격자판에서 선과 선 사이 간격이 1칸일 때 컴퓨터가 옆 트랙으로 순간이동하는 버그를 인지하고, 지도를 가로세로 2배로 늘려 선 사이에 갈 수 없는 빈 공간(`0`)을 강제로 창조하는 치트키를 적용함. <br> 2. **3상태 장부 마킹:** 사각형의 순수 안쪽 알맹이는 무조건 금지 구역 `2`로 밀어버리고, 테두리 선은 이미 `2`로 덮인 곳이 아닐 때만 `1`로 칠하는 상호작용 로직을 구현하여 거대한 다각형의 '순수 겉 껍데기 외곽선'만 `1`로 완벽히 발라냄. <br> 3. **정석 BFS 거리 추적:** 갈림길에서 걸음 수가 뻥튀기되는 것을 막기 위해 큐에 `{X, Y, distance}` 상태를 묶어 다니며 탐색하고, 방문 도장(`visited=true`) 누락 방지를 통해 무한 루프를 완벽 차단함. 최종 정답은 `/ 2`로 스케일 복구함. |

#### 💡 Today I Learned (TIL)
1. **거대하게 보지 말고 조각으로 누적하라:** 복잡한 다각형 테두리를 한 번에 구하려다 뇌에 락(Lock)이 걸렸으나, 도화지를 깔아두고 사각형 하나씩 순차적으로 누적해서 장부에 덮어쓰고 잠그는 시야의 전환을 통해 난제를 아주 우아하게 해결함. 뇌지컬이 한 단계 완전히 업그레이드됨을 체감함.
2. **1칸짜리 틈새의 차원 확장 법칙:** 2차원 배열 최단 거리 문제 중 "테두리/둘레 선만 따라가라"는 제약이 있고 간격이 1칸인 구역이 보이면, 고민하지 말고 무조건 **좌표 2배 확대 트릭**을 꺼내 드는 실전 가이드라인을 수립함.
3. **기본기의 엄격함:** 백트래킹 탐험 후 리셋 버튼(`false`)을 누르는 타이밍과 BFS 탐색 시 큐에 넣음과 동시에 방문 처리(`visited=true`)를 해주는 사소한 디테일 한 줄이 레벨 3 고난도 문제의 런타임 성공 여부를 결정짓는 핵심 열쇠임을 다시금 각인함.

### Day 93 (2026.06.05) - 동적계획법(DP) 코어 타격: 장부 기록을 통한 차원 최적화

수많은 갈래길을 전수조사하는 탐색 대신, 한 번 계산한 결과를 메모장(배열)에 기록하고 재활용하는 '동적계획법(Dynamic Programming)'의 핵심 메커니즘을 학습함. 1차원 선형 점화식부터 2차원 격자판 누적 제어까지 단계별로 확장하며 복잡한 문제를 완벽하게 최적화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[2 x n 타일링 (12900)](https://school.programmers.co.kr/learn/courses/30/lessons/12900)** | **'마지막 타일의 딜레마'를 통한 피보나치 점화식 유도**. <br> 1. 맨 오른쪽 끝 칸을 채우는 방법이 '세로 타일 1개(남은 공간 n-1)' 또는 '가로 타일 2개(남은 공간 n-2)'라는 두 갈래 평행우주뿐임을 논리적으로 증명함. <br> 2. 이를 통해 단순히 규칙을 외우는 것이 아닌, $\text{dp}[n] = \text{dp}[n-1] + \text{dp}[n-2]$ 라는 피보나치 점화식의 본질을 도출하고 자료형 오버플로우를 막기 위한 나머지 연산(`% 1,000,000,007`)을 매 연산마다 안전하게 적용함. |
| Lv.3 | **[등굣길 (42898)]** | **2차원 공간 패딩과 장애물 우회 누적 알고리즘 설계**. <br> 1. **공간 합산 원리 체화:** "왔다갔다" 탐색하는 대신, 각 교차로마다 `위쪽 칸에서 온 경로의 수`와 `왼쪽 칸에서 온 경로의 수`가 강물처럼 합쳐지며 최종 목적지까지 누적되는 DP의 본질을 완벽히 이해함. <br> 2. **실무형 방어 코드 및 함정 극복:** 웅덩이가 없는 날을 대비한 빈 배열 방어 조건(`puddle.length > 0`)을 구축하고, 문제의 고질적인 함정인 `[가로(m), 세로(n)]` 좌표 반전 매핑을 `dp[puddle[1]][puddle[0]]`으로 정밀 제어함. <br> 3. **인덱스 패딩 트릭:** `[n+1][m+1]` 크기의 완충 지대를 설계하여 벽을 넘어가는 인덱스 에러를 방지하고, 시작점 `dp[1][1] = 1`이 루프 내에서 초기화되지 않도록 예외 처리함. |

#### 💡 Today I Learned (TIL)
1. **동적계획법(DP)의 진짜 의미:** 과거의 탐색 결과를 바닥(배열)에 숫자로 적어두고, 다음 칸은 그 기록을 **'믿고 더하기'**만 하면서 전진하는 강력한 최적화 테크닉이다. 큰 문제를 작은 문제(바로 위 칸, 바로 왼쪽 칸)들의 합으로 쪼개어 해결함으로써 복잡도를 획기적으로 줄일 수 있다.
2. **좌표축의 엄격한 제어:** 2차원 배열을 다룰 때 지문에서 제시하는 가로/세로(`m`, `n`) 기준과 코드상의 행/열(`i`, `j`) 인덱스가 뒤집히는 함정을 항상 경계해야 한다. 한 끗 차이의 디테일과 예외 방어 코드가 Lv.3 고난도 문제의 정답률을 결정짓는 핵심 열쇠임을 다시금 깨달았다.

### Day 92 (2026.06.04) - 정렬의 마법과 차원 축소: 다중 조건 정렬 및 해시 활용

자료구조의 기본인 해시(Hash)의 성능적 이점과 더불어, 두 가지 조건이 얽혀있는 복잡한 다중 조건 시뮬레이션을 '정렬 뒤틀기 트릭'을 통해 단 한 번의 순회($O(N)$)로 압축하는 고난도 아키텍처 테크닉을 완벽하게 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[전화번호 목록 (42577)](https://school.programmers.co.kr/learn/courses/30/lessons/42577)** | **문자열 정렬(`Arrays.sort`) 및 해시셋(`HashSet`)의 투-트랙 풀이 체득**. <br> 1. 문자열을 정렬하면 접두어 관계인 단어들이 사전순으로 이웃하게 배치된다는 점을 활용, `startsWith()` 메서드로 효율성 테스트를 통과함. <br> 2. **[해시셋 확장 학습]** 모든 번호를 `HashSet` 주머니에 담아두고, 각 문자열을 1글자부터 슬라이싱(`substring`)하며 $O(1)$의 속도로 주머니에 존재하는지 조회하는 대용량 데이터 처리용 해시 최적화 원리($O(N)$)까지 완벽하게 격파함. |
| Lv.3 | **[인사고과 (152995)]** | **다중 조건 커스텀 정렬을 활용한 공간 통제와 조건 필터링**. <br> 1. **평행우주 정렬 트릭:** `근무 점수 내림차순(▼), 동료 점수 오름차순(▲)`의 다중 조건 람다식을 설계하여, "내 앞에 지나간 사원들은 무조건 나보다 근무 점수가 높거나 같다"는 일방통행 환경을 강제로 구축함. <br> 2. **차원 축소 ($O(N^2) \rightarrow O(N)$):** 한쪽 점수를 정렬로 통제했기에, 과거 생존자들이 남긴 동료 점수 최고점인 `maxPeer` 하나만 들고 순회하며 2중 for문 없이 단 1번의 순회로 탈락자를 전수 솎아내는 치트키 로직을 구현함. <br> 3. **우아한 석차 산출:** 생존자 중 완호의 총점보다 높은 에이스를 만날 때마다 완호의 등수를 뒤로 밀어내는(`rank++`) 방식을 도입하여, 별도의 재정렬이나 동석차 예외 처리 없이 깔끔하게 등수를 산출함. |

#### 💡 Today I Learned (TIL)
1. **자바 Comparator 람다식의 본질:** 자바의 정렬 엔진은 `(a, b) -> { ... }` 람다식이 리턴하는 값의 **'부호'**만 본다. 양수(+)가 반환되면 앞뒤 원소의 위치를 무조건 바꾸기 때문에, 내림차순을 원하면 뒤에서 앞을 뺀 `b - a`를, 오름차순을 원하면 앞에서 뒤를 뺀 `a - b`를 활용하여 정렬 축을 마음대로 통제할 수 있다.
2. **다중 조건의 꼬임 풀기:** 두 개의 조건이 모두 나보다 큰 녀석을 찾는 복잡한 다중 조건 필터링은, 하나를 내림차순, 하나를 오름차순으로 어긋나게 정렬하면 기준이 1차원으로 선형화되면서 단 하나의 `max` 변수만으로 완벽한 통제가 가능해진다. 혼자였다면 발견하기 힘들었을 명품 정렬 패턴을 완벽히 소화함.

### Day 91 (2026.06.02) - 대기열의 미학: 스택/큐(Queue)와 힙(Priority Queue)

코딩 테스트의 단골 자료구조인 큐(Queue)와 우선순위 큐(Heap)를 집중 공략함. 큐의 선입선출(FIFO) 특성을 활용한 시뮬레이션부터, 두 개의 우선순위 큐를 동기화하여 이중 우선순위 큐를 구현하는 Lv.3 아이디어까지 완벽하게 소화하며 자료구조 활용 능력을 한 단계 끌어올림.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[기능개발 (42586)](https://school.programmers.co.kr/learn/courses/30/lessons/42586)** | **큐(Queue)를 활용한 진도 시뮬레이션**. <br> 1. `Math.ceil((100.0 - progress) / speed)`를 이용해 남은 작업 일수를 한 번에 계산하는 수학적 접근 적용. <br> 2. 스택(Stack)을 따로 두지 않고 큐 하나와 `maxDays`(기준일) 변수만으로 병목 현상(앞 기능이 끝나야 뒤 기능이 배포됨)을 우아하게 해결함. <br> 3. 같은 날 끝나는 기능들도 함께 배포되어야 하므로 부등호(`<=`) 처리가 매우 중요함을 깨달음. |
| Lv.3 | **[이중우선순위큐 (42628)](https://school.programmers.co.kr/learn/courses/30/lessons/42628)** | **최소 힙과 최대 힙의 동기화 (Two Priority Queues)**. <br> 1. 오름차순(Min-Heap) 큐와 내림차순(Max-Heap, `Collections.reverseOrder()`) 큐 2개를 생성하여 양극단 값을 추출할 수 있는 환경을 구축함. <br> 2. 한쪽 큐에서 최댓값/최솟값을 제거(`poll()`)할 때, 반대쪽 큐에서도 해당 값을 지워(`remove()`) 두 큐의 상태를 동일하게 유지하는 동기화 로직을 구현함. <br> 3. **[Troubleshooting]** 조건문 안에서 `poll()`을 두 번 호출하면 값이 2번 빠져나가는 사이드 이펙트가 발생하므로, 변수에 할당하여 안전하게 지워야 함을 체득함. |

#### 💡 Today I Learned (TIL)
1. **Queue의 동반자, 기준 변수:** 큐에서 뭉텅이로 데이터를 빼내야 할 때는 `peek()`로 다음 값을 엿보면서, '현재 묶음의 기준값(예: maxDays)'과 비교하는 while문 패턴이 가장 깔끔하다.
2. **PriorityQueue의 변신:** 자바의 우선순위 큐는 기본적으로 최소 힙(Min-Heap)이다. 최대 힙(Max-Heap)이 필요하다면 `new PriorityQueue<>(Collections.reverseOrder())`를 잊지 말자.
3. **메서드 호출의 사이드 이펙트 주의:** `remove(pq.poll())`처럼 메서드 안에 메서드를 넣을 때는, 안쪽에 있는 `poll()`이 실제로 데이터를 빼내는(상태를 변화시키는) 동작임을 항상 명심해야 버그를 막을 수 있다.

### Day 90 (2026.05.29) - 완전 탐색의 꽃: DFS 재귀와 BFS 연결 요소 찾기

코딩 테스트의 영원한 단골손님인 탐색(DFS/BFS) 테마를 집중 타격함. 재귀함수를 이용해 모든 경우의 수를 쪼개어 탐색하는 DFS의 기초를 다지고, 큐(Queue)를 활용해 2차원 배열 상에서 독립된 덩어리(네트워크)의 개수를 세는 BFS 실전 응용력까지 완벽하게 소화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[타겟 넘버 (43165)](https://school.programmers.co.kr/learn/courses/30/lessons/43165)** | **재귀함수를 이용한 DFS(깊이 우선 탐색) 기초**. <br> 1. 현재 깊이(`depth`)의 숫자를 '더하는 평행우주'와 '빼는 평행우주' 두 갈래로 쪼개어 탐색하는 상태 공간 트리(State Space Tree) 템플릿을 체화함. <br> 2. 탐색 중간에 멈추지 않고, 모든 숫자를 다 사용했을 때(`depth == numbers.length`) 종료 조건을 발동시키는 DFS의 정석을 이해함. <br> 3. 전역 변수 `answer`를 활용하여 종료 시점에 `sum == target` 조건을 만족할 때만 정답을 카운트하는 로직 구현. |
| Lv.3 | **[네트워크 (43162)](https://school.programmers.co.kr/learn/courses/30/lessons/43162)** | **방문 처리(`isVisited`)와 BFS를 활용한 연결 요소(Connected Components) 찾기**. <br> 1. 외부 `for`문과 내부 `BFS` 함수의 완벽한 역할 분담: 외부 `for`문은 방문하지 않은 새로운 덩어리의 시작점을 찾고(`answer++`), `BFS`는 시작점부터 연결된 모든 노드를 찾아 페인트통을 붓듯 방문 처리(`isVisited[end] = true`)함. <br> 2. `Queue`를 생성하여 현재 노드와 인접한 노드(`computers[cur][end] == 1`)를 뻗어나가는 너비 우선 탐색(BFS) 로직을 깔끔하게 구현함. <br> 3. 그래프 탐색 시 무한 루프를 방지하기 위한 핵심 장치인 방문 기록장(`boolean[] isVisited`)의 중요성을 재확인함. |

#### 💡 Today I Learned (TIL)
1. **DFS의 본질 (평행 우주 쪼개기):** 한 지점에서 여러 가지 선택지(예: +, -)가 있을 때, 재귀함수를 그 선택지의 개수만큼 호출하면 컴퓨터는 마치 평행 우주를 탐험하듯 모든 경우의 수를 끝까지 파고들어 간다.
2. **덩어리(영역) 개수 세기의 마스터키:** 지도 상에서 '몇 개의 구역으로 나뉘어 있는가?'를 묻는 문제는 무조건 이 패턴이다. `[1] 전체를 순회하며 아직 방문 안 한 노드를 찾는다 -> [2] 정답을 +1 한다 -> [3] 그 노드와 연결된 모든 노드를 BFS/DFS로 싹 다 방문 처리해 버린다.` 이 3박자만 기억하면 어떠한 변형 문제도 뚫어낼 수 있다.

### Day 89 (2026.05.28) - 굳은 뇌 깨우기: 2차원 배열 빡구현과 그리디(투 포인터)

일주일의 공백기 이후 복귀. 굳어있는 타이핑 근육과 인덱스 감각을 깨우기 위한 2차원 배열 시뮬레이션 문제와, '이기적인' 최적의 수를 찾는 레벨 3 그리디 문제를 통해 코어 논리력을 완벽하게 예열함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[삼각 달팽이 (68645)](https://school.programmers.co.kr/learn/courses/30/lessons/68645)** | **2차원 배열 인덱스 제어와 방향 시뮬레이션**. <br> 1. 삼각형을 `n x n` 정사각형 배열의 절반으로 치환하여 생각하는 공간적 발상의 전환을 적용함. <br> 2. `아래 -> 오른쪽 -> 대각선` 3가지 방향에 따른 `(x, y)` 증감 규칙을 `i % 3`으로 우아하게 제어함. <br> 3. 방향이 바뀔 때마다 이동 횟수가 1씩 줄어드는 규칙을 안쪽 루프의 시작점(`j = i`)으로 통제하는 이중 for문 테크닉을 체화함. |
| Lv.3 | **[숫자 게임 (12987)](https://school.programmers.co.kr/learn/courses/30/lessons/12987)** | **그리디(Greedy) 최적화와 투 포인터(Two Pointers)**. <br> 1. $O(N^2)$의 이중 루프를 피하기 위해, A팀과 B팀 배열을 모두 오름차순 정렬(`Arrays.sort`)하여 $O(N)$으로 해결하는 투 포인터 전략을 수립함. <br> 2. "질 판은 가장 약한 카드를 제물로 바치고, 이길 수 있는 상대의 약한 카드는 절대 놓치지 않는다"는 완벽한 그리디 논리를 단 하나의 `if(B[bIndex] > A[aIndex])`로 구현해 냄. <br> 3. 패배 시 A팀의 포인터(`aIndex`)를 증가시키지 않고 대기시키는 이유(쉬운 상대를 버리지 않기 위함)를 논리적으로 완벽히 이해함. |

#### 💡 Today I Learned (TIL)
1. **1차원 배열로의 평탄화:** 2차원 배열에 담긴 데이터를 1차원 배열로 옮겨 담을 때는, 2차원 인덱스(`i`, `j`)에 의존하지 않고 별도의 1차원 전용 포인터(`index++`)를 사용해야 순서가 꼬이지 않는다.
2. **그리디의 본질:** "어떻게 해야 가장 얄밉게 점수를 챙길까?" 페널티가 없는 상황에서는 버릴 건 과감히 버리고(가장 약한 카드를 희생양으로), 취할 수 있는 이득만 쏙쏙 빼먹는 것이 최고의 알고리즘이다. 투 포인터는 이런 이기적인 로직을 구현하는 데 최고의 무기다.

### Day 88 (2026.05.21) - Phase 5 공채 특화 모드: 해시(Hash) 끝판왕과 슬라이딩 윈도우 (새로운 루틴 도입)

오늘부터 알고리즘 사고력의 한계를 뚫기 위해 **'매일 Lv.3 1문제 + Lv.2 1문제'**로 훈련 루틴을 전면 업그레이드함. 그 첫날로, 카카오 기출의 단골손님인 '이중 해시맵 + 객체 정렬' 패턴과 '슬라이딩 윈도우' 최적화 기법을 완벽하게 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.3 | **[베스트앨범 (42579)](https://school.programmers.co.kr/learn/courses/30/lessons/42579)** | **객체 지향 정렬(`Comparable`)과 이중 해시맵(HashMap) 설계**. <br> 1. 장르별 총 재생 횟수(`Map<String, Integer>`)와 장르별 노래 목록(`Map<String, List<Song>>`)을 분리하여 관리하는 관심사 분리(Separation of Concerns) 아키텍처를 스스로 설계함. <br> 2. `Song` 클래스를 만들어 고유 번호(`id`)와 재생 횟수(`play`)를 묶고, `compareTo` 메서드 내부에 조건(재생수 내림차순, 고유번호 오름차순)을 한 번에 제어하는 객체 지향 빡구현을 완벽히 소화함. <br> 3. `HashMap`은 자체 정렬이 불가능하므로, `KeySet`을 `ArrayList`로 추출한 뒤 람다식을 이용해 Value 기준으로 정렬(`sort`)하는 실무형 치트키를 체득함. |
| Lv.2 | **[할인 행사 (131127)](https://school.programmers.co.kr/learn/courses/30/lessons/131127)** | **슬라이딩 윈도우(Sliding Window)와 투 맵(Two-Map) 전략**. <br> 1. 원본 장부(타겟 맵)의 훼손을 막기 위해, 매 10일 치 창문을 검사할 때마다 새로운 서브 장부(`new HashMap<>()`)를 생성하는 데이터 오염 방지 로직을 구현함. <br> 2. 자바의 `HashMap.equals()` 메서드를 활용하여, 두 맵의 Key 종류와 Value 수량이 완벽히 일치하는지 단 한 줄로 검증하는 맵 비교의 정석을 활용함. <br> 3. 이중 루프 내부에서 증감식 오타(`i++` vs `j++`)가 불러오는 치명적인 런타임/시간 초과 에러를 직접 디버깅하며 꼼꼼한 인덱스 제어의 중요성을 재확인함. |

#### 💡 Today I Learned (TIL)
1. **루틴의 진화:** 구현력과 사고력의 밸런스를 동시에 잡기 위해 '매일 Lv.3 한 문제 + Lv.2 한 문제'라는 새로운 훈련 루틴을 스스로 도입했다. 뇌의 과부하를 즐기며 한 단계 더 성장할 준비를 마쳤다.
2. **Map과 List의 역할 분담:** `Map`은 데이터를 키워드(Key)로 묶어 던져두는 **'분류 창고'**이고, `List`는 그 창고의 물건들을 순서대로 진열하는 **'매대'**다. 정렬(줄 세우기)이 필요할 때는 망설임 없이 Map에서 List로 데이터를 빼내어 조립해야 한다는 거대한 흐름을 이해했다.
3. **객체 지향적 데이터 묶기:** 인덱스(고유 번호)와 값(재생 횟수)이 별개의 배열에 쪼개져 있으면 정렬할 때 데이터가 미아 상태가 된다. 이럴 땐 두 데이터를 하나의 클래스(`Class`)로 묶어 `Comparable`을 달아주는 것이 논리적 꼬임을 막는 가장 강력한 방어책이다.

### Day 87 (2026.05.20) - Phase 5 자립심 훈련: 시간 복잡도의 한계 돌파 (효율성 최적화)

입력값이 최대 100만 개에 달하는 효율성 테스트에서, 이중 루프($O(N^2)$)의 유혹을 뿌리치고 단 한 번의 순회($O(N)$)로 답을 도출해 내는 최적화 훈련을 진행함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[롤케이크 자르기 (132265)](https://school.programmers.co.kr/learn/courses/30/lessons/132265)** | **Two-Pass 전략과 장부의 분리 ($O(N)$)** <br> 1. 매번 자를 때마다 배열을 새로 순회하면 $O(N^2)$로 무조건 시간 초과가 발생함을 인지함. <br> 2. 형(`HashSet`)과 동생(`HashMap`)의 역할을 완벽히 분리함. <br> 3. 동생이 모든 토핑을 다 가졌다고 세팅한 뒤, 형이 하나씩 뺏어올 때마다 동생 장부의 Value를 1씩 차감함. Value가 0이 되는 순간 종류를 삭제(`remove`)하여 O(1)의 속도로 크기를 비교하는 최적화를 달성함. |
| Lv.2 | **[점 찍기 (140107)](https://school.programmers.co.kr/learn/courses/30/lessons/140107)** | **원의 방정식과 자료형의 한계 방어 ($O(N)$)** <br> 1. 원의 방정식 $x^2 + y^2 \le d^2$을 활용하여, 특정 $x$ 좌표에서의 최대 높이 $y$를 $y = \sqrt{d^2 - x^2}$ 수식으로 도출함. <br> 2. $y$축을 반복문으로 돌지 않고, `(maxY / k) + 1` 사칙연산 한 번으로 점의 개수를 구하는 수학적 최적화를 적용함. <br> 3. $x$가 커질 때 발생하는 $x \times x$의 `int` 오버플로우 함정을 파악하고, 반복문의 $x$ 자체를 `long` 타입으로 선언하여 런타임 에러를 완벽하게 방어함. |

#### 💡 Today I Learned (TIL)
1. **$O(N^2)$ vs $O(N)$의 본질 차이:** 100만 개의 데이터를 매번 처음부터 다시 세면 1조 번의 연산이 터지지만, 상태(State)의 증감만 업데이트하면 단 200만 번의 연산으로 끝낼 수 있다.
2. **자료형 우선순위의 함정:** 자바에서 `long limit = d * d`나 `limit - (x * x)`를 계산할 때, 괄호 안의 변수가 `int`라면 `int` 범위 안에서 이미 쓰레기 값으로 오버플로우가 발생한 뒤에 `long`으로 넘겨진다. 큰 수의 연산이 예상된다면 아예 처음부터 `long` 타입 수레에 담고 계산을 시작해야 안전하다.

### Day 86 (2026.05.13) - Phase 5 자립심 훈련: 상태 공간 탐색과 분할 정복

두 달 만에 다시 마주한 BFS 상태 공간 탐색의 뼈대를 백지에서 재조립하고, 대기업 코딩 테스트의 단골손님인 '분할 정복(Divide & Conquer)' 알고리즘을 새로운 무기로 장착한 7일 차.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[숫자 변환하기 (154538)](https://school.programmers.co.kr/learn/courses/30/lessons/154538)** | **BFS 최단 거리 탐색과 1차원 방명록(`visited`)**. <br> 1. `+n`, `*2`, `*3` 세 갈래로 뻗어나가는 상태 트리(State Space Tree)에서 BFS를 무기로 선택함. <br> 2. **메모리 폭발 방어:** 큐에 진입하기 전 `visited` 배열을 확인하여, 다른 경로로 이미 도달했던 중복 숫자의 파동 연쇄를 완벽히 차단함. <br> 3. 큐에 숫자를 넣을 때 외부 변수(`count`)를 쓰지 않고, `new int[]{숫자, 누적 연산 횟수}` 형태로 거리를 함께 묶어 던지는 BFS 거리 측정의 정석을 체화함. |
| Lv.2 | **[쿼드압축 후 개수 세기 (68936)](https://school.programmers.co.kr/learn/courses/30/lessons/68936)** | **분할 정복(Divide & Conquer)과 재귀(Recursion) 설계**. <br> 1. **관심사 분리 (Top-Down):** 영역을 4개로 쪼개며 지시를 내리는 매니저(`quad`)와, 주어진 영역의 압축 가능 여부만 깡통처럼 검사하는 실무자(`check`)로 메서드를 완벽히 분리함. <br> 2. **4등분 좌표 수식:** 한 변의 길이가 `size`일 때, 절반인 `newSize = size / 2`를 구하고 이를 `x`, `y` 좌표에 각각 더하여 1, 2, 3, 4사분면의 새로운 시작 좌표를 도출해 내는 수학적 규칙을 직관적으로 이해함. <br> 3. 파라미터로 넘어가는 `size`가 갱신됨에 따라 검사해야 할 영역이 자연스럽게 좁아지는 재귀의 본질을 타이핑을 통해 몸으로 익힘. |

#### 💡 Today I Learned (TIL)
1. **변화하는 상태값의 기준:** 숫자 변환하기 문제에서, `moves` 배열을 처음 한 번만 고정해 두면 안 된다. 큐에서 새롭게 꺼낸 '현재 숫자(`curNum`)'를 기준으로 `+n, *2, *3`을 매번 새롭게 계산해야 다음 단계로 전진할 수 있다.
2. **재귀를 믿어라 (Leap of Faith):** 재귀 함수를 짤 때는 끝까지 파고들어 시뮬레이션하려는 뇌의 본능을 멈춰야 한다. "탈출 조건(전부 같은 숫자인가?)"과 "1단계 행동(4등분 해서 던진다)"만 명확히 짜둔 뒤, 나머지는 내 분신(재귀 함수)이 알아서 해결해 줄 것이라 믿는 뻔뻔함이 필요하다.

### Day 85 (2026.05.12) - Phase 5 자립심 훈련: 데이터 흐름 제어와 다중 목표 탐색

자료구조(Stack)를 활용한 최적화된 그리디 탐색과, 상태 초기화를 동반한 다중 경유지 BFS(Breadth-First Search)를 훈련한 6일 차.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[큰 수 만들기 (42883)](https://school.programmers.co.kr/learn/courses/30/lessons/42883)** | **Stack(LIFO)을 활용한 그리디(Greedy) 탐색**. <br> 1. 순서를 유지하면서 가장 큰 수를 만들어야 하므로 전체 조합을 구하는 것은 불가능. <br> 2. 숫자를 순회하며 **"내 앞에 있는 숫자가 나보다 작으면 쫓아낸다(pop)"**는 로직을 스택의 `while`문으로 구현. <br> 3. 내림차순(예: "54321")으로 정렬되어 있어 `k`가 남는 엣지 케이스는 스택의 맨 뒷부분을 잘라내는(`size() - k`) 방식으로 완벽 방어. |
| Lv.2 | **[미로 탈출 (159993)](https://school.programmers.co.kr/learn/courses/30/lessons/159993)** | **다중 목표 BFS 설계와 상태 공간 관리**. <br> 1. 시작점(S) $\rightarrow$ 레버(L), 레버(L) $\rightarrow$ 출구(E) 두 개의 독립된 미션으로 분리하여 탐색. <br> 2. 첫 번째 미션 완료 후 이전에 지나온 길을 다시 밟을 수 있어야 하므로 **`visited` 배열 초기화** 필수. <br> 3. 단순 이동이 아닌 '거리'를 구해야 하므로, 큐(Queue)에 `new int[]{x, y, dist}` 형태로 **거리 상태를 함께 보관하며 최단 거리를 누적**. <br> 4. 어느 한 구간이라도 도달 불가(-1) 시 즉시 -1을 반환하는 예외 처리의 중요성 확인. |

#### 💡 Today I Learned (TIL)
1. **스택의 위력:** "순서를 지키며 조건에 맞는 것을 골라라"라는 문장을 보면 반사적으로 스택을 꺼내자. 스택은 '과거의 선택을 가장 빠르게 되돌리고 수정할 수 있는' 최고의 무기다.
2. **Queue.poll() 주의보:** 큐에서 데이터를 꺼낼 때는 절대 여러 번 `poll()`을 연달아 쓰지 말자. 반드시 한 번만 꺼내서 배열이나 객체에 담아둔 뒤에 그 안의 속성값을 꺼내 써야 런타임 에러(NPE)나 데이터 증발을 막을 수 있다.
3. **BFS 거리 추적의 정석:** 이동할 때마다 외부 변수(`count`)를 1씩 올리면 사방 탐색 시 4배로 카운트가 오르는 대참사가 일어난다. 반드시 **'큐에 들어가는 좌표 자체가 자신의 이동 거리를 기억하게'** 만들어야 정확한 최단 거리가 측정된다.

### Day 84 (2026.05.09) - Phase 5 자립심 훈련: 시간 복잡도 최적화와 원형 데이터

이중 for문의 한계를 돌파하기 위한 자료구조(HashMap)의 올바른 활용법과, 원형(Circular) 수열을 선형 배열처럼 다루는 발상의 전환을 훈련한 5일 차.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[시소 짝꿍 (152996)](https://school.programmers.co.kr/learn/courses/30/lessons/152996)** | **해시맵(HashMap)을 활용한 $O(1)$ 탐색과 조회/갱신의 분리**. <br> 1. 배열을 오름차순 정렬하여 '나보다 가벼운 짝꿍'만 탐색하도록 방향을 단방향으로 고정. <br> 2. `1:1`, `2:3`, `1:2`, `3:4` 비율을 실수형(`Double`)으로 계산. <br> 3. **가장 중요한 핵심:** 해시맵을 다룰 때 짝꿍이 있는지 **조회(`get`)**하는 작업과 내 몸무게를 **등록(`put`)**하는 작업을 완벽히 분리하여, 중복된 몸무게가 연속으로 등장할 때 발생하는 경우의 수(조합) 오류를 원천 차단. |
| Lv.2 | **[연속 부분 수열 합의 개수 (131701)](https://school.programmers.co.kr/learn/courses/30/lessons/131701)** | **원형 배열 트릭과 누적 합(Sweeping) 최적화**. <br> 1. 원형 수열의 `%` 연산 인덱싱을 피하기 위해 **배열의 길이를 2배로 늘려 똑같이 이어 붙이는 트릭** 사용. <br> 2. 길이를 기준으로 탐색하면 3중 for문이 나오지만, **'시작점'을 기준으로 고정해 두고 한 칸씩 전진하며 기존 합에 누적(Sweeping)**하는 방식으로 2중 for문으로 시간 복잡도 대폭 감소. <br> 3. 도출된 모든 합은 `HashSet`에 넣어 중복을 깔끔하게 제거. |

#### 💡 Today I Learned (TIL)
1. **Map의 Get과 Put은 분리하라:** 누적 카운트 문제에서 조건문 안에 `put`을 섞어 쓰면 이전 값이 덮어씌워지는 대참사가 발생한다. 항상 과거의 데이터를 먼저 '조회'해서 정답에 반영한 뒤, 마지막에 현재 상태를 '등록'하는 2-Step을 유지하자.
2. **원형 수열의 마법:** 끝과 처음이 이어진 데이터는 골치 아프게 인덱스 계산을 할 필요 없이, 처음부터 데이터를 두 배로 복제해 일자형으로 펴버리면 로직이 압도적으로 단순해진다.
3. **빗자루질(Sweeping) 누적 합:** 매번 구간의 합을 0부터 다시 구하는 것은 하수다. 시작점을 박아두고 한 칸씩 밀고 나가면서 "방금 전까지 구한 뭉치 + 새로운 숫자 1개" 형태로 누적하면 연산량을 극단적으로 줄일 수 있다.

### Day 83 (2026.05.08) - Phase 5 자립심 훈련: 공간 탐색과 최적의 선택 (Greedy)

2차원 격자에서의 완전 탐색(BFS)과 수의 자릿수를 쪼개어 판단하는 탐욕법(Greedy)을 훈련한 4일 차. 알고리즘의 뼈대를 넘어, '데이터 전처리'와 '연산자 우선순위' 등 실무적인 시야까지 한 단계 끌어올림.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[무인도 여행 (154540)](https://school.programmers.co.kr/learn/courses/30/lessons/154540)** | **데이터 전처리와 2차원 BFS 탐색**. <br> 1. "상하좌우 연결" 키워드에서 본능적으로 BFS를 도출. <br> 2. `String` 배열을 매번 `charAt()`으로 검사하는 대신, 사전에 `int[][]`로 변환(X는 0, 숫자는 정수형)하는 **데이터 전처리(Preprocessing)**를 통해 탐색 로직의 가독성과 효율성을 극대화. |
| Lv.2 | **[마법의 엘리베이터 (148653)](https://school.programmers.co.kr/learn/courses/30/lessons/148653)** | **그리디(Greedy) 탐색과 치명적 경계값 처리**. <br> 1. 자릿수를 10으로 나눈 나머지(`%`)와 몫(`/`)을 활용해 1의 자리부터 최적의 선택을 누적. <br> 2. **숫자 '5'의 딜레마:** 현재 자릿수가 딱 5일 때는, **'다음 자릿수'**가 5 이상인지 4 이하인지를 미리 내다보고 올림/버림을 결정해야 최소 이동 횟수를 보장할 수 있다는 핵심 인사이트 획득. |

#### 💡 Today I Learned (TIL)
1. **데이터 전처리의 힘:** 주어진 데이터를 그대로 쓰기보다, 내가 다루기 편한 형태(예: `String` -> `int`)로 한 번 정제해 두면 이후의 메인 로직(BFS 등)이 압도적으로 깔끔해지고 실수가 줄어든다.
2. **그리디의 숨겨진 조건:** 단순하게 '현재 상황'만 보고 판단하면 함정에 빠진다. 선택지가 동일한 비용을 가질 때(예: 올림 5번 vs 내림 5번), **"이 선택이 '다음 스텝'에 어떤 영향을 미치는가?"**를 기준으로 삼아야 진짜 최적해를 찾을 수 있다.
3. **🚨 잊지 말자, 연산자 우선순위:** `storey /= 10 + 1;` 은 10을 나누고 1을 더하는 것이 아니라, `10+1` 즉 `11`을 나누어버리는 대참사를 부른다. 명시적으로 `storey = (storey / 10) + 1;` 처럼 괄호를 묶거나 풀어써서 의도를 정확히 해야 한다.

### Day 82 (2026.05.07) - Phase 5 자립심 훈련: 흐름 제어와 자료구조

과거에 풀었던 문제들을 백지상태에서 다시 마주하며, 지문 속 제약 조건을 바탕으로 가장 적합한 자료구조(Stack, Priority Queue)를 스스로 선택하고 논리를 전개하는 자립 훈련 3일 차.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[택배상자 (131704)](https://school.programmers.co.kr/learn/courses/30/lessons/131704)** | **스택(Stack)을 활용한 데이터 흐름 제어**. <br> 1. "입구 외에 막혀있다"는 제약에서 LIFO(Stack) 자료구조를 본능적으로 도출. <br> 2. 조건문을 복잡하게 쪼개는 대신, **'메인 벨트의 상자를 일단 스택에 넣고(`push`), 스택 맨 위가 타겟과 일치하는 동안 연속해서 뺀다(`while + pop`)'**는 우아한 흐름 제어 패턴 체화. |
| Lv.2 | **[호텔 대실 (155651)](https://school.programmers.co.kr/learn/courses/30/lessons/155651)** | **시간 데이터 파싱과 우선순위 큐(PQ), 그리고 객체 지향 정렬**. <br> 1. "HH:MM" 문자열을 `(HH * 60) + MM` 형태의 단일 정수(분)로 변환하여 시간 계산과 대소 비교를 압도적으로 단순화. <br> 2. `int[][]`와 람다식에 의존하던 기존 방식을 넘어, `Reserve` 클래스와 `Comparable` 인터페이스를 구현하여 가독성 높은 객체 지향적 대기열 정렬 로직 설계. <br> 3. 대기열은 '시작 시간(배열)', 방 관리는 '종료 시간(PQ)'으로 기준을 완벽히 분리하여 런타임 함정 회피. |

#### 💡 Today I Learned (TIL)
1. **무지성 Push 패턴:** 스택이나 큐를 쓸 때는 입고 조건과 출고 조건을 복잡하게 섞지 말고, 일단 담은 뒤에(`push`) 조건이 맞을 때 연속으로 털어내는(`while`) 구조가 가장 버그가 적다.
2. **객체 지향적 데이터 관리:** 2차원 배열의 인덱스(`arr[0]`, `arr[1]`)에 의존하면 머릿속에서 의미가 꼬이기 쉽다. 데이터를 하나의 객체(Class)로 묶어 `startTime`, `endTime`으로 부르면 코드가 곧 명세서가 된다.
3. **PQ의 정확한 역할 분담:** 우선순위 큐에 객체를 통째로 넣을 때는 정렬 기준이 무엇인지(`compareTo`) 반드시 확인해야 하며, 큐의 목적(예: 종료 시간이 가장 빠른 방 찾기)에 맞춰 필요한 숫자만 깔끔하게 넣는 것이 안전하다.

### Day 81 (2026.05.05) - Phase 5 자립심 훈련: 수학적 직관과 최적화

힌트 없이 지문 속에서 수학적 규칙(최대공약수, 원의 방정식)을 찾아내고, 시간 초과가 뻔한 $O(N^2)$ 탐색을 $O(N)$으로 최적화하는 완전한 자립 훈련 2일 차. 오버플로우 방지와 경계값 처리(Edge Case)를 완벽히 통제함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[숫자 카드 나누기 (135807)](https://school.programmers.co.kr/learn/courses/30/lessons/135807)** | **유클리드 호제법(GCD)을 활용한 누적 공약수 도출 및 교차 검증**. <br> 1. 배열 전체를 관통하는 최대공약수를 눈덩이처럼 누적(`gcdA = gcd(gcdA, arrayA[i])`)하여 구하는 패턴 체화. <br> 2. 상태 검증을 위한 플래그(`boolean`) 변수를 활용하여 `for`문 진행 중의 '섣부른 정답 확정'을 방지하고, 루프 종료 후 정답을 안전하게 등록하는 로직 구축. |
| Lv.2 | **[두 원 사이의 정수 쌍 (181187)](https://school.programmers.co.kr/learn/courses/30/lessons/181187)** | **원의 방정식($x^2 + y^2 = r^2$)과 1사분면 $O(N)$ 최적화**. <br> 1. 2차원 공간 전체를 탐색하는 대신 x축을 기준으로 최대/최소 높이($y$)를 구하는 $O(N)$ 1차원 탐색으로 변환. <br> 2. **올림/내림 경계 처리:** 천장을 뚫지 않기 위한 큰 원의 내림(`Math.floor`), 금지 구역을 침범하지 않기 위한 작은 원의 올림(`Math.ceil`) 원리를 완벽히 이해. <br> 3. `(long)` 캐스팅을 통한 100만 $x, r$ 값의 오버플로우 사전 방어 및 `NaN` 예외 처리(`x < r1`). |

#### 💡 Today I Learned (TIL)
1. **누적(Snowball) 로직의 힘:** 인자가 2개뿐인 함수(GCD 등)로 배열 전체의 공통 속성을 뽑아낼 때는 변수 하나를 쥐고 덮어씌우며 누적하는 방식이 가장 우아하다.
2. **플래그(Flag) 변수의 타이밍:** 반복문 안에서는 '탈락(false)' 여부만 체크하고 `break`로 도망친 뒤, 최종 통과 여부는 무조건 `for`문이 끝난 밖에서 판별해야 한다.
3. **경계값과 형변환:** 제곱 연산 시 자료형의 한계(`int` 오버플로우)를 항상 의심하고 `long`으로 안전벨트를 채워야 하며, 올림(`ceil`)과 내림(`floor`)은 내가 허용하는 '합법 구역'이 어디인지를 기준으로 선택해야 한다.

### Day 80 (2026.05.04) - Phase 5 자립심 훈련: 사이클 탐색과 One-Pass 해시맵

힌트 없이 지문만 보고 '사이클(그룹)'을 찾아내고, 이중 루프의 시간 초과를 막기 위해 '정렬+Map'을 결합하는 완전한 자립 훈련 1일 차. 수학적 비율과 인덱스의 어긋남(Off-by-one)을 완벽히 통제함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[혼자 놀기의 달인 (131130)](https://school.programmers.co.kr/learn/courses/30/lessons/131130)** | **방문 배열(`visited`)을 활용한 사이클 탐색**. <br> 1. `for`문으로 전체를 훑고, 방문하지 않은 상자(`!isOpen[i]`) 발견 시 `while`문으로 꼬리를 물며 사이클을 추적함. <br> 2. **Off-by-one 제어:** 사람의 번호(1부터)와 배열 인덱스(0부터)의 차이를 `current - 1`로 정확히 매핑하여 런타임 에러 방어. <br> 3. 그룹이 딱 1개 나왔을 때 조기 종료(`return 0`)하는 예외 처리 위치를 `for`문 바깥으로 빼내어 논리적 꼬임을 해결함. |
| Lv.2 | **[시소 짝꿍 (152996)](https://school.programmers.co.kr/learn/courses/30/lessons/152996)** | **정렬과 `HashMap`을 이용한 One-Pass $O(N)$ 최적화**. <br> 1. 최대 10만 명의 데이터를 이중 루프로 돌리면 터진다는 직관 도출 후 정렬 적용. <br> 2. **조회 선행, 등록 후행:** 나 자신과 짝꿍을 맺거나 중복 카운팅되는 것을 막기 위해, Map에서 과거의 짝꿍 비율(1, 2/3, 1/2, 3/4)을 먼저 찾고(`containsKey`) 합산한 뒤, 자신을 맵에 등록(`put`)하는 우아한 패턴 체화. <br> 3. `get(내 몸무게)`가 아니라 `get(내가 찾는 짝꿍 몸무게)`를 꺼내야 한다는 Map 조회의 본질을 완벽히 이해함. |

#### 💡 Today I Learned (TIL)
1. **조회와 등록의 순서:** Map에 데이터를 무작정 욱여넣고 시작하는 것이 능사가 아니다. "조회를 먼저, 등록을 나중에" 하는 One-Pass 패턴을 쓰면 중복 카운트 방지와 본인 제외 로직이 한 번에 해결된다.
2. **인덱스 함정 탈출:** 상자 번호와 배열 인덱스가 다를 때 헷갈려하지 않고, `current - 1`로 확실하게 핀포인트를 짚어내는 감각이 생겼다.
3. **15분 샌드박스의 위력:** 코드를 바로 치지 않고, 15분 동안 데이터의 흐름과 사용할 무기(Map, while문)의 뼈대를 미리 세우고 들어가니 복잡한 로직도 단숨에 뚫어낼 수 있었다.

### Day 79 (2026.04.28) - Phase 5: 시간 시뮬레이션 & 커스텀 정렬 (빡구현)

힌트 없이 지문만 보고 올바른 자료구조(무기)를 선택하고, 머릿속의 논리를 코드로 번역하는 '야생 훈련' 1일 차.
큰 그림(스택, 정렬)을 잡는 시야는 확실히 좋아졌음을 느꼈으나, 인덱스 계산이나 조건문 디테일에서 잦은 에러가 발생함. 디테일을 잡는 구현력 훈련의 필요성을 체감함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[과제 진행하기 (176962)](https://school.programmers.co.kr/learn/courses/30/lessons/176962)** | **객체지향 설계(시간 파싱) + Stack 시뮬레이션**. <br> 1. **시간 정수화:** `"11:40"` 같은 문자열을 `split`하여 `(시 * 60) + 분`의 정수형태로 클래스 생성자에서 미리 가공함. <br> 2. **시간 간격(Gap) 제어:** `다음 과제 시작 시간 - 현재 과제 시작 시간`으로 나에게 주어진 잉여 시간을 먼저 구하는 것이 핵심. <br> 3. **Stack 털기:** 잉여 시간이 있고 스택에 멈춘 과제가 있다면 `while(!stack.isEmpty() && leftTime > 0)` 루프를 돌며 남은 시간을 깎거나 과제를 완료 처리함. <br> 4. **마지막 과제 처리:** `for`문이 끝난 뒤, 인덱스 초과(`length`)에 주의하며 `tasks[length - 1]` 마지막 과제를 처리하고 스택의 잔반을 털어주는 필수 후처리 과정 체득. |
| Lv.2 | **[테이블 해시 함수 (147354)](https://school.programmers.co.kr/learn/courses/30/lessons/147354)** | **다중 조건 람다식 정렬 + 오프-바이-원(Off-by-one) 방어**. <br> 1. **람다 정렬(`a-b`):** `Arrays.sort(data, (a, b) -> {})` 내부에서 조건 분기 시, 오름차순은 `a-b`, 내림차순은 `b-a`를 리턴하는 절대 공식 체화. <br> 2. **사람 숫자 vs 컴퓨터 숫자:** 지문의 `col`, `row`는 1부터 시작하므로, 배열 접근 시에만 `-1`을 해주고(`data[i-1]`), `for`문이나 나누기 연산(`% i`)은 사람 기준 번호를 그대로 써서 로직의 꼬임을 막음. <br> 3. **XOR 누적 연산:** 비트 연산자 캐럿(`^`)을 활용해 `answer ^= s_i` 형태로 값을 누적하는 방법 학습. (0과 XOR하면 자기 자신이 나오므로 초기화 분기 불필요) |

#### 💡 Today I Learned (TIL)
1. **클래스의 상태 유지:** 매번 배열의 String을 꺼내 파싱하지 말고, 아예 처음 객체를 생성할 때(Constructor) 다루기 쉬운 `int`형으로 세팅해 두면 이후 시뮬레이션 로직이 엄청나게 쾌적해진다.
2. **람다식의 본질:** 자바의 정렬 함수는 던져준 숫자의 '부호'만 본다. 양수(+)가 나오면 두 원소의 자리를 바꾸기 때문에, `a - b`는 오름차순, `b - a`는 내림차순이 된다는 직관을 얻었다.
3. **인덱스 트라우마 극복하기:** "마지막 요소를 처리해야 해!" 하다가 `length`를 넣어서 터지고, "배열이니까 무조건 -1이지!" 하다가 반복문이 덜 돌아서 터졌다. 무작정 -1을 외울 게 아니라, **"내가 지금 다루는 변수가 배열의 방 번호인가, 아니면 사람의 카운팅 숫자인가?"**를 명확히 구분하는 습관을 들여야겠다.

### Day 78 (2026.04.27) - Phase 5 카카오 & 실전 기출: 객체 지향 그리디 & 순수 논리 구현

알고리즘 패턴(BFS/DFS 등)에 끼워 맞추는 고정관념에서 벗어나, 문제의 비즈니스 로직 자체를 분석하고 객체(Object)와 조건문(if)으로 풀어내는 '순수 코딩 피지컬' 집중 훈련을 진행함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[광물 캐기 (172927)](https://school.programmers.co.kr/learn/courses/30/lessons/172927)** | **객체 지향 설계(OOP) + 그리디(Greedy)**. <br> 1. **시야의 전환:** 광물 순서를 못 바꾼다면, 5개씩 잘라서 '덩어리(Chunk)'로 만들면 정렬할 수 있다는 발상의 전환. <br> 2. **클래스 설계:** `MineralChunk` 클래스를 만들어 다이아, 철, 돌 개수와 '돌 곡괭이로 캤을 때의 피로도'를 상태로 저장함. <br> 3. **Comparable 정렬:** 돌 피로도가 높은(가장 캐기 힘든) 덩어리가 맨 앞으로 오도록 내림차순 정렬(`o.stoneFatigue - this.stoneFatigue`)하여 좋은 곡괭이를 먼저 매칭시킴. <br> 4. **점프 루프:** `for(int i = 0; i < minerals.length && i < maxMineral; i+=5)`를 통해 캘 수 있는 한계치까지만 5칸씩 점프하며 덩어리를 생성하는 안전한 파이프라인 구축. |
| Lv.2 | **[혼자서 하는 틱택토 (160585)](https://school.programmers.co.kr/learn/courses/30/lessons/160585)** | **비즈니스 로직(규칙) 검증 + Edge Case 빡구현**. <br> 1. 복잡한 자료구조 없이, 게임의 논리적 모순을 찾아내는 것이 핵심. <br> 2. **개수 필터:** 선공이 'O'이므로 `O == X` 또는 `O == X + 1` 상태만 정상임을 검증. <br> 3. **승패 모순 필터:** 빙고 판독 메서드(`hasWon`)를 분리. 선공 'O'가 이겼는데 개수가 같거나(`O == X`), 후공 'X'가 이겼는데 'O'가 더 많은(`O == X + 1`) 모순 상황(게임 종료 후 돌을 더 둔 반칙)을 잡아내어 차단함. |

#### 💡 Today I Learned (TIL)
1. **하이브리드 알고리즘의 이해:** "이 문제는 무슨 알고리즘이지?" 하고 견적이 안 설 때는, 십중팔구 **"그리디의 탈을 쓴 빡구현"**이다. 배열의 인덱스가 꼬일 것 같으면 주저하지 말고 객체(Class)를 만들어 데이터를 캡슐화하는 것이 실무적이고 안전한 접근이다.
2. **로직의 본질 파악:** 틱택토 문제처럼 '규칙'이 주어지는 문제는 코드를 치기 전에 규칙의 허점(모순점)을 먼저 한글로 적어보는 것이 필수다. 일상적인 논리를 코드로 번역하는 능력이 곧 진짜 개발자의 역량임을 깨달았다.

#### Day 77 (2026.04.24) - Phase 5 카카오 기출: DFS 백트래킹 & 역방향 그리디 최적화

카카오 Level 3와 Level 2 중에서도 가장 악랄한 '경우의 수 탐색'과 '그리디 발상의 전환'을 요구하는 두 문제를 격파함. 특히 정규표현식 치트키와, 마이너스(-) 부호를 여유 공간으로 활용하는 실무형 꼼수를 완벽하게 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.3 | **[불량 사용자 (64064)](https://school.programmers.co.kr/learn/courses/30/lessons/64064)** | **정규식 매칭(.matches) + DFS 백트래킹 + HashSet**. <br> 1. **정규식 치트키:** 카카오의 `*`는 정규식에서 \"0개 이상 반복\"을 의미하므로, 이를 \"아무 글자 1개\"를 뜻하는 마침표(`.`)로 `replace` 한 뒤 `.matches()`로 0.001초 만에 검증하는 로직 구현. <br> 2. **DFS 백트래킹:** 한 조건에 여러 명이 매칭될 때, `visited=true` -> `dfs()` -> `visited=false` (원상복구) 3박자를 통해 평행우주(다른 조합)를 탐색하는 구조 완벽 이해. <br> 3. **자동 정렬 중복 제거:** 목표 도달 시, `visited` 배열을 처음부터 훑으면 굳이 `sort`를 쓰지 않아도 자동으로 정렬된 고유 문자열(`A B`)이 튀어나와 `HashSet`으로 깔끔하게 중복이 제거됨. |
| Lv.2 | **[택배 배달과 수거하기 (150369)](https://school.programmers.co.kr/learn/courses/30/lessons/150369)** | **역방향 그리디(Greedy) + 여유 공간(Negative Caching) 시뮬레이션**. <br> 1. **역방향 훑기:** 무조건 제일 먼 집부터 처리하면서 돌아오는 것이 왕복 거리를 최소화한다는 그리디 전략 도출. <br> 2. **마이너스(-)의 마법:** 집마다 짐을 누적 변수(`deliver`, `pickup`)에 더하고, 0보다 클 때만 `while`문을 돌아 거리(`(i+1)*2`)를 추가함. <br> 3. 처리 후 남은 트럭 용량(`cap`)을 빼서 **음수가 된 값을 \"트럭의 빈자리\"로 활용**하여, 다음 루프(앞집)의 짐을 꽁짜로 상쇄시키는 카카오식 $O(N)$ 최적화 꼼수 완벽 체화. |

#### 💡 Today I Learned (TIL)
1. **정규식의 본질과 직역:** 일상생활의 `*`(별표)와 컴퓨터 언어의 `*`는 다르다. 카카오의 의도(\"아무거나 한 글자\")를 컴퓨터가 알아듣는 조커 카드(`.`)로 번역해 주는 것이 파싱 문제의 첫걸음이다.
2. **백트래킹의 이유:** 내가 방금 썼던 방문 도장(`visited=true`)을 지워주지(`false`) 않으면, 다른 경우의 수를 찾으러 갈 때 그 데이터가 영원히 잠겨버린다. (미로에서 뒤돌아 나올 때 빵가루 줍기!)
3. **변수 상태 유지(State)를 통한 최적화:** '택배 배달' 문제에서 매 집마다 새로운 트럭을 계산하지 않고, `deliver` 변수를 `for`문 밖으로 빼서 상태를 계속 누적/이월시키는 방식은, 현실 세계의 '남은 여유 공간'을 컴퓨터의 '음수'로 아주 기가 막히게 추상화한 최고의 아이디어였다. 끝까지 포기하지 않고 이해해 낸 나 자신 칭찬해!

### Day 76 (2026.04.23) - Phase 5 카카오 기출: 투 포인터 최적화 & 2차원 배열 빡구현

어제에 이어 카카오 블라인드/인턴십 기출 Lv.3 2연전. 투 포인터와 해시맵을 결합한 효율성 최적화 로직과, 2차원 배열을 3배로 확장(Padding)하여 회전시키는 실전 구현력을 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.3 | **[보석 쇼핑 (67258)](https://school.programmers.co.kr/learn/courses/30/lessons/67258)** | **Two Pointers & HashMap (슬라이딩 윈도우)**. <br> 1. 전체 보석 종류를 `HashSet`으로 먼저 파악하여 목표치(`targetSize`) 설정. <br> 2. `left`, `right` 포인터를 모두 0에서 시작하는 '빈 수레' 전략. <br> 3. `for`문으로 `right`를 전진하며 장바구니(`Map`)에 담고, 다 모이면(`size == targetSize`) `while`문 안에서 `left`를 당기며 구간을 압축함. <br> 4. 꼬리를 자를 때 개수가 0이 되면 반드시 `map.remove()`를 해야 루프를 탈출할 수 있음을 뼈저리게 깨달음. |
| Lv.3 | **[자물쇠와 열쇠 (60059)](https://school.programmers.co.kr/learn/courses/30/lessons/60059)** | **2차원 배열 회전(Rotation) & 패딩(Padding) 빡구현**. <br> 1. 열쇠가 자물쇠 밖으로 삐져나갈 때 발생하는 `IndexOutOfBounds`를 막기 위해, 자물쇠보다 3배 큰 거대한 도화지(`N*3`)를 만들어 정가운데에 배치함. <br> 2. **회전 공식:** 90도 시계방향 회전 시 `[j][N - 1 - i]` 패턴을 외우고 적용하는 치트키 체화. <br> 3. 4방향 회전 $\to$ 2중 for문으로 이동 $\to$ 열쇠 더하기 $\to$ 검사(`check`) $\to$ 열쇠 빼기(원상복구)의 4단계 시뮬레이션 파이프라인 완벽 구현. |

#### 💡 Today I Learned (TIL)
1. **슬라이딩 윈도우의 본질:** 다 모았다고 끝이 아니라, "조건을 만족한 상태에서 맨 앞의 짐을 버려도 유지가 되는가?"를 테스트하며 최소 구간을 쥐어짜 내는 것이 투 포인터의 진정한 최적화다.
2. **도화지 확장 (Padding) 기법:** 배열이 겹치거나 밖으로 삐져나가는 시뮬레이션에서는 억지로 `if`문으로 경계 처리를 하는 것보다, 메모리를 넉넉히 써서 배열 자체를 3배로 뻥튀기하는 것이 훨씬 안전하고 빠른 실무적 꼼수임을 배웠다.
3. **패턴 암기의 중요성:** 2차원 배열 90도 회전 공식(`[j][N-1-i]`)처럼 실전 코테에 밥 먹듯이 나오는 패턴은, 백지에서 발명하려 하지 말고 원리를 이해한 뒤 템플릿으로 외워버려야 한다. 나는 알고리즘을 베낀 게 아니라 선배들의 최적 무기를 전수받은 것이다!

### Day 75 (2026.04.22) - Phase 5 실전 기업 코테 돌입: 카카오 기출 (다익스트라 & 3차원 BFS)

백준의 '건축가 모드(처음부터 끝까지 창조)'에서 벗어나, 프로그래머스의 '외주 개발자 모드(주어진 부품 활용)'에 적응하는 첫날. 카카오 기출문제를 통해 실생활 언어를 알고리즘으로 번역하고, 기존 다익스트라와 BFS를 3차원 상태 공간으로 확장하는 훈련을 완료함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.3 | **[합승 택시 요금](https://school.programmers.co.kr/learn/courses/30/lessons/72413)** | **다익스트라(Dijkstra) 3회 호출 및 무향 그래프 응용**. <br> 1. 출발지(`S`), 라이언 집(`A`), 어피치 집(`B`) 세 곳에서 각각 다익스트라를 돌려 최단 거리 가계부(`distS`, `distA`, `distB`)를 획득함. <br> 2. 양방향 통행(무향 그래프)의 특성을 활용해, 'K에서 A로 가는 요금'을 'A에서 K로 가는 요금(`distA[k]`)'으로 대체함. <br> 3. 모든 노드를 순회하며 `distS[k] + distA[k] + distB[k]` (S->K 같이 이동 + 각자 집으로 이동)가 최소가 되는 합승 종료 지점을 한 줄의 수식으로 추상화함. |
| Lv.3 | **[경주로 건설](https://school.programmers.co.kr/learn/courses/30/lessons/67259)** | **3차원 상태 공간 BFS + 우선순위 큐(다익스트라)**. <br> 1. `Node` 객체에 좌표(`x, y`)뿐만 아니라, 미래의 비용에 영향을 미치는 **진입 방향(`dir`)**과 **누적 요금(`cost`)**을 함께 저장함. <br> 2. 방향 전환(코너) 시 요금이 비싸지므로, 다른 방향에서 온 싼 경로가 덮어씌워지는 것을 막기 위해 방명록을 **3차원 배열(`dist[방향][x][y]`)**로 분리하여 통제함. <br> 3. `PriorityQueue`를 이용해 가장 싼 경로를 먼저 탐색하고, 목적지에 도달하는 순간 즉시 종료(`return cost`)하여 탐색 효율을 극대화함. |

#### 💡 Today I Learned (TIL)
1. **환경 적응 (백준 vs 프로그래머스):** 처음부터 모든 것을 창조하는 백준과 달리, 실전 코딩 테스트는 주어지는 파라미터(`fares` 등)를 내가 원하는 형태(다익스트라용 `graph`)로 가공하는 '전처리 능력'이 매우 중요함을 깨달았다.
2. **상태(State)를 기억하는 탐색의 중요성:** "내가 지금 어느 방향을 보고 있는가?"처럼 다음 결과에 영향을 미치는 상태 요소가 존재한다면, 그것은 단순 2차원 탐색이 아니라 그 상태까지 포함하는 차원 확장(3차원 배열)이 필수적이다.
3. **실생활 언어의 알고리즘 번역:** 코딩 테스트의 포장지를 벗기는 문제 해석력이 필요하다. "직진과 코너의 비용이 다르다" -> "가중치가 다르므로 단순 방문(`visited`) 처리가 아니라 비용 배열(`dist`) 갱신으로 통제한다"로 번역하는 시야를 길렀다. 앞으로는 코딩 전 무조건 손 코딩(설계)을 먼저 진행할 것이다.

### Day 74 (2026.04.21) - 백준(BOJ) 스터디 20일 차: 플로이드-워셜(Floyd-Warshall) 완전 정복

오늘은 모든 정점에서 모든 정점으로의 최단 거리를 구하는 **플로이드-워셜 알고리즘**의 기본 구현부터, 2차원 장부를 활용한 **경로 역추적(Path Tracking)**까지 완벽하게 마스터함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 골드 4 | **[플로이드 (BOJ 11404)](https://www.acmicpc.net/problem/11404)** | **플로이드-워셜 템플릿 체화**. <br> 1. `k(거쳐가는 노드) - i(출발) - j(도착)` 순서의 3중 for문이 핵심. <br> 2. 입력 시 같은 노선에 여러 요금의 버스가 있을 수 있으므로 `Math.min`으로 가장 싼 값만 갱신. |
| 골드 2 | **[플로이드 2 (BOJ 11780)](https://www.acmicpc.net/problem/11780)** | **플로이드-워셜 경로 추적(Path Reconstruction)**. <br> 1. 2차원 배열(`prevNode`)을 만들어 **"i에서 j로 가기 위해 가장 먼저 가야 할 다음 도시"**를 기록. <br> 2. `if (dist[i][k] + dist[k][j] < dist[i][j])` 조건 성립 시, `prevNode[i][j] = prevNode[i][k]`로 장부를 갱신하는 논리 완벽 구현. <br> 3. `while(curr != 0)` 루프를 통해 다음 도시로 점프하며 경로를 List에 담아 출력. |

#### 💡 Today I Learned (TIL)
* **경유지가 핵심이다:** 다익스트라가 '내 위치' 기준이라면, 플로이드-워셜은 **'어떤 도시(k)를 거쳐갈 때 더 싸지는가?'**를 모든 도시에 대해 묻는 3중 for문 구조다.
* **장부 갱신의 원리:** i에서 j로 가는 최적의 길이 k를 거쳐간다면, **i에서 가장 먼저 발을 내디뎌야 할 다음 도시는 'i에서 k로 갈 때의 다음 도시'와 똑같다.** (`prevNode[i][j] = prevNode[i][k]`)
* **INF 값 주의:** 무한대를 의미하는 `INF`를 설정할 때, `INF + INF` 연산 시 오버플로우가 나지 않도록 `1_000_000_000` (10억) 정도로 설정하는 것이 가장 안전하다.

### Day 73 (2026.04.18) - 백준(BOJ) 스터디 19일 차: 다익스트라(Dijkstra) 완전체 진화

오늘은 다익스트라 알고리즘을 단순한 '도시 간 연결'을 넘어, **'2D 미로 탐색'**과 **'내비게이션 경로 역추적'**까지 확장하며 다익스트라의 모든 변형 기법을 완벽하게 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 골드 4 | **[녹색 옷 입은 애가 젤다지? (BOJ 4485)](https://www.acmicpc.net/problem/4485)** | **2D 배열 다익스트라 구현 (BFS 결합)**. <br> 1. 시작점 `[0][0]`에서도 비용(도둑루피)이 발생함을 주의. <br> 2. 다익스트라에서는 먼저 도착한 경로가 최적이라는 보장이 없으므로, `visited` 배열을 쓰면 안 됨. 오직 `if (next_cost < dist[nx][ny])` (가계부 갱신) 조건만으로 통제해야 함. |
| 골드 3 | **[최소비용 구하기 2 (BOJ 11779)](https://www.acmicpc.net/problem/11779)** | **다익스트라 경로 역추적(Path Tracking)**. <br> 최단 거리를 갱신할 때 `preNode[next.dest] = cur_dest;` 로 "어느 도시를 거쳐서 왔는지" 장부를 기록함. 탐색 종료 후, 목적지부터 `preNode`를 타고 `0`을 만날 때까지 `Stack`에 담은 뒤 `.pop()`으로 꺼내 정방향 경로를 출력함. |

#### 💡 Today I Learned (TIL)
* **다익스트라는 "메모리가 있는 그리디"다:** 매 순간 가장 싼 경로를 선택하되, 지금까지의 누적 비용을 가계부(`dist` 배열)에 기록하고 더 싼 길을 찾으면 언제든 갱신하는 논리다.
* **가계부 갱신의 주어 주의:** 내 위치가 아니라, **다음 방문할 곳(`next.dest`)의 가계부**를 갱신해야 한다.
* **역추적 장부(`preNode`) 세팅:** `INF`로 초기화할 필요 없이 자바 기본값인 `0`을 두면, '방문한 적 없음' 또는 '역추적의 끝점(while문의 브레이크)'으로 유용하게 활용할 수 있다.
* **자바 Stack의 함정:** 자바에서 `Stack`을 역순(LIFO)으로 출력하려면 향상된 for문(`for(int i : stack)`)을 쓰면 안 된다. (바닥부터 훑음) 반드시 `while(!stack.isEmpty())`와 `stack.pop()`을 조합해서 맨 위에서부터 꺼내야 한다.

### Day 72 (2026.04.17) - 백준(BOJ) 스터디 18일 차: 다익스트라(Dijkstra) 응용 마스터

오늘은 다익스트라 알고리즘을 단순 템플릿 암기가 아닌, 실전 상황에 맞게 변형하고 레고 블록처럼 조립하는 고급 기법을 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 골드 3 | **[파티 (BOJ 1238)](https://www.acmicpc.net/problem/1238)** | **역방향 그래프(Reverse Graph) 최적화**. <br> 모든 정점에서 단일 목적지로 가는 최단 거리를 구할 때, 지도의 간선을 거꾸로 뒤집고 목적지에서 다익스트라를 딱 1번만 돌리면 시간 복잡도를 획기적으로 줄일 수 있음을 배움. |
| 골드 4 | **[특정한 최단 경로 (BOJ 1504)](https://www.acmicpc.net/problem/1504)** | **다익스트라 모듈화 및 경로 조립**. <br> 다익스트라를 `int` 반환형 모듈로 개조하여 구간별 최단 거리를 합산함. <br> **🚨 주의점:** 길이 끊겨있을 때 반환된 `INF`를 무턱대고 더하면 오버플로우가 발생하거나 로직이 꼬일 수 있으므로, 최종 값 산출 시 `if (totalDist >= INF) return -1` 예외 처리가 필수적임을 뼈저리게 깨달음. |

#### 💡 Today I Learned (TIL)
* 알고리즘에서 말하는 '정방향/역방향'은 현실 시간의 흐름이 아니라 **'입력된 간선의 방향(화살표)을 그대로 썼느냐, 뒤집었느냐'**의 차이다.
* 특정 구간의 최단 거리만 필요할 때는 전역 배열을 덮어쓰는 것보다, 함수 내에서 `dist` 배열을 그때그때 새로 초기화하여 (`Arrays.fill()`) 사용하는 것이 훨씬 모듈화하기 좋다.
* 최소/최단 값을 찾을 때 무심코 `Math.max`를 쓰지 않도록 주의하고, `new int[]` 생성 시 기본값이 0으로 깔린다는 사실을 항상 명심하자.

### Day 71 (2026.04.16) - 백준(BOJ) 스터디 17일 차: 다익스트라(Dijkstra) 알고리즘 입문

오늘은 '모든 간선의 가중치가 동일한' BFS의 한계를 넘어, **'간선마다 비용(가중치)이 다를 때'** 최단 경로를 구하는 [다익스트라 알고리즘]을 완벽하게 템플릿화하여 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 골드 4 | **[최단경로 (BOJ 1753)](https://www.acmicpc.net/problem/1753)** | **다익스트라(Dijkstra) 표준 템플릿 구현**. <br> 1. **Node 클래스 & 정렬:** 도착지(`dest`)와 비용(`cost`)을 묶은 객체를 만들고, `Comparable`을 구현하여 비용 기준 오름차순으로 자동 정렬되게 세팅함. <br> 2. **인접 리스트 활용:** 메모리 초과를 막기 위해 2차원 배열 대신 `ArrayList<Node>[] graph` 구조를 사용하여 운행 노선표를 구축함. |
| 골드 5 | **[최소비용 구하기 (BOJ 1916)](https://www.acmicpc.net/problem/1916)** | **다익스트라 실전 응용 및 예외 처리**. <br> 1. **INF 오버플로우 방어:** 누적 비용이 `Integer.MAX_VALUE`를 넘지 않도록 `1,000,000,000` 등 적당히 큰 수로 무한대를 설정함. <br> 2. **Early Exit (조기 종료):** 모든 도시가 아닌 단일 목표 정점에 도달했을 때 무의미한 남은 큐 탐색을 방지하기 위해 `if (curNode == end) { print; return; }`으로 시스템을 즉시 종료하는 최적화 스킬 장착. |

#### 💡 Today I Learned (TIL)
1. **버스티켓과 노선표 (다익스트라의 본질):** `Node` 객체는 목적지와 요금이 적힌 '버스티켓'이고, `ArrayList[] graph`는 각 도시의 '운행 노선표'다. 내비게이션(PQ)은 항상 가장 싼 티켓부터 안내하며, 도착지에서 노선표를 보고 더 싼 길이 발견되면 바닥(`dist` 배열)에 기록을 갱신한다.
2. **낡은 티켓 폐기 (`continue` 방어막):** 큐에서 꺼낸 티켓의 비용(`curCost`)이 이미 바닥에 갱신된 최저가(`dist[curNode]`)보다 비싸다면, 이는 과거에 큐에 들어간 '낡은 정보'이므로 즉시 버려야(`continue`) 시간 초과(TLE)를 막을 수 있다.
3. **우선순위 큐(PQ)의 원리:** PQ가 알아서 정렬을 해주는 것은 맞지만, '무엇을 기준으로' 정렬할지는 개발자가 `compareTo` 메서드로 명시해 주어야 한다. 내부적으로 Min-Heap 구조를 사용하여 $O(\log N)$의 압도적인 속도로 최저가를 찾아낸다.

### Day 70 (2026.04.15) - 백준(BOJ) 스터디 16일 차: 시뮬레이션 & 듀얼 BFS (Dual BFS) 마스터

오늘은 단순한 탐색을 넘어, '시간의 흐름에 따른 상태 변화(시뮬레이션)'와 '두 가지 대상이 동시에 움직이는 추격전(듀얼 BFS)'을 코드로 설계하는 아키텍처를 집중 훈련함.

| 난이도  | 문제 이름 | 핵심 정리 |
|:----:|:---|:---|
| 골드 4 | **[빙산 (BOJ 2573)](https://www.acmicpc.net/problem/2573)** | **시뮬레이션 & BFS 덩어리 세기**. <br> 1. `while(true)` 무한 톱니바퀴 안에서 '상태 체크(덩어리 개수) -> 탈출 -> 빙산 녹이기'의 Top-Down 설계를 구현. <br> 2. **연쇄 붕괴 방지:** 방금 녹은 빙산이 바로 옆 빙산을 연달아 녹이지 않도록 임시 배열(살생부)을 만들어 일괄 갱신함. |
| 골드 3 | **[불! (BOJ 4179)](https://www.acmicpc.net/problem/4179)** | **듀얼 BFS (Multi-source BFS)**. <br> 1. 불(Fire) 지렁이를 먼저 풀어 바닥에 도달 시간을 기록(`fire_time`)하고, 지훈이가 그 기록을 곁눈질하며 도망가는 2단계 추격전 설계. <br> 2. **방어막 `-1` 초기화:** 방문 여부와 도달 시간을 한 번에 관리하기 위해 배열을 `-1`로 초기화하여 무한 루프 및 `0`초 시작점 꼬임을 완벽 방어함. <br> 3. 미로의 가장자리(배열 범위 이탈)를 탈출 조건으로 삼아 즉시 `return`하는 최적화 로직 체화. |

#### 💡 Today I Learned (TIL)
1. **로직을 깎아내는 힘 (Top-Down 설계):** 기능을 먼저 `main`이나 `while` 루프 안에 부르고(Manager), 그 안의 세부 구현(Worker)은 나중에 짠다. 한글 주석으로 뼈대를 먼저 잡아야 스파게티 코드를 막을 수 있다.
2. **초기값 `-1`의 위력:** 시간 기록장 배열을 `0`으로 초기화하면, '출발지(0초)'와 '안 간 곳(0초)'이 구분되지 않아 지렁이가 뺑뺑이를 돈다. 태초의 상태를 `-1`로 밀어버리는 것이 가장 안전한 방어막이다.
3. **듀얼 BFS 아키텍처:** 적(불)이 먼저 바닥에 도달 시간을 쫙 적어두고, 플레이어(지훈)는 `불의 시간 > 나의 도착 시간` 이거나 불이 오지 않은 곳(`-1`)으로만 움직이는 '낙서 참조 기법'을 완벽하게 이해함.

### Day 69 (2026.04.14) - 백준(BOJ) 스터디 15일 차: BFS 마스터 클래스 (면적 & 완전 탐색)

오늘은 동일한 BFS 엔진을 목적에 따라 '면적 계산'과 '전수 조사'로 자유자재로 변형하여 사용하는 훈련을 진행함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 1 | **[그림 (BOJ 1926)](https://www.acmicpc.net/problem/1926)** | **순수 BFS (면적 계산)**. <br> 1. `Queue`에서 하나를 꺼낼 때마다 `area++`를 수행하여 덩어리의 크기를 구함. <br> 2. `maxSize = Math.max(maxSize, area)`를 통해 가장 큰 덩어리를 갱신함. |
| 골드 5 | **[보물섬 (BOJ 2589)](https://www.acmicpc.net/problem/2589)** | **브루트 포스 + BFS (전수 조사)**. <br> 1. 보물의 위치를 모르므로 모든 육지(`L`)에서 각각 BFS를 시작하는 '무한 폭격' 설계. <br> 2. **방명록 초기화:** 매 BFS 시작 전 `visited` 배열을 새로 생성하여 이전 탐색의 간섭을 완벽히 차단함. <br> 3. `[x, y, dist]`를 큐에 함께 담아 원본 맵을 훼손하지 않고 거리를 보존함. |

#### 💡 Today I Learned (TIL)
1. **BFS의 3가지 변신:** 1) 덩어리 세기(`count++`), 2) 맵에 낙서하기(`map+1`), 3) 배낭에 담기(`time+1`)의 차이를 완벽히 이해하고 상황에 맞게 골라 쓰는 능력을 장착함.
2. **이동 거리 vs 방문 칸수:** 큐에서 뽑을 때 무지성으로 숫자를 올리면 '면적'이 되고, 이전 칸의 기록에 1을 더해 전달하면 '거리'가 된다는 본질적 차이를 체득함.

### Day 68 (2026.04.13) - 백준(BOJ) 스터디 14일 차: 1차원 그래프 탐색과 2차원 방향 시뮬레이션

하루 2문제 루틴 복귀. 1차원 인맥 지도에서의 BFS 탐색과, 2차원 맵에서 방향 인덱스(`dx, dy`)를 직접 조작하는 로봇 청소기 시뮬레이션을 통해 새로운 탐색 감각을 경험함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 2 | **[촌수계산 (BOJ 2644)](https://www.acmicpc.net/problem/2644)** | **1차원 그래프 BFS**. <br> 1. 2차원 맵(`dx, dy`)의 고정관념에서 벗어나, 연결된 노드만 큐에 담는 1차원 탐색 구조 구현. <br> 2. `queue`에 `[현재 노드, 누적 촌수]`를 담아 타겟에 도달할 때까지 거리를 계산함. |
| 골드 5 | **[로봇 청소기 (BOJ 14503)](https://www.acmicpc.net/problem/14503)** | **2차원 방향 시뮬레이션 (구현)**. <br> 1. `Queue` 없이 `while` 무한 루프와 좌표/방향 변수만으로 로봇을 제어하는 구조 경험. <br> 2. `(dir + 3) % 4`를 이용한 왼쪽 90도 회전 공식과, 현재 방향을 유지한 채 반대로 이동하는 후진 로직의 뼈대를 파악함. |

#### 💡 Today I Learned (TIL)
1. **망치 증후군 극복:** 2차원 BFS에 익숙해진 뇌를 유연하게 전환하여, 1차원 노드 탐색 로직으로 구조를 뜯어고치는 적응력을 발휘함.
2. **시뮬레이션의 본질:** 상태(위치, 방향)를 직접 기록하고 지문(조건)의 순서대로 코드를 분기하는 훈련을 시작함. 완벽히 체화되진 않았으나, 방향 인덱스를 제어하는 핵심 원리를 눈으로 확인하고 제출까지 완료함.

### Day 67 (2026.04.10) - 백준(BOJ) 스터디 13일 차: 조건부 BFS 및 무한 시뮬레이션 제어

단순한 BFS 탐색을 넘어, 특정 조건(인구 차이)을 만족할 때만 이동하는 '조건부 지렁이'와, 변화가 없을 때까지 전체 과정을 반복하는 거대한 '시뮬레이션 루프'를 완벽하게 융합함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 골드 4 | **[인구 이동 (BOJ 16234)](https://www.acmicpc.net/problem/16234)** | **조건부 BFS + List 상태 저장 + while 무한 루프 제어**. <br> 1. **무한 시뮬레이션:** `while(true)` 내부에서 하루 단위의 `visited` 배열 초기화 및 `isChanged` 플래그를 통한 탈출 조건(`break`)을 완벽하게 제어함. <br> 2. **조건부 탐색 & 방어막:** `!visited` 확인 전, 배열 경계 이탈 방지 방어막(`nx >= 0 ...`)을 먼저 세우고 인구 차이(`L <= diff <= R`)를 계산하여 안전하게 `Queue`에 진입시키는 로직을 체화함. <br> 3. **List 활용 (데이터 캐싱):** 지나간 나라의 인구를 단순히 더하는 것에 그치지 않고, 그 좌표들을 `ArrayList`에 담아두었다가 탐색 종료 후 일괄적으로 평균값을 덮어씌우는 실무적 최적화 설계를 구현함. |

#### 💡 Today I Learned (TIL)
1. **경계선 방어막의 우선순위:** `map` 배열의 인덱스를 조회(계산)하기 전에, 반드시 해당 인덱스가 유효한 범위(0~N) 안에 있는지 검사하는 조건문이 '무조건 먼저' 와야 함을 뼈저리게 체감함. (에러 펑 방지)
2. **근육 기억(Muscle Memory)의 위력:** 반복 훈련으로 다져진 BFS 뼈대 코드(Queue, 방문 처리, 4방향 탐색)가 손가락 끝에서 자동으로 타이핑되는 경험을 통해 꾸준함의 힘을 증명함.

### Day 66 (2026.04.09) - 백준(BOJ) 스터디 12일 차: 삼성 SW 역량 기출 2연전 (골드 티어)

DFS를 이용한 조합(Combination) 생성과 BFS/수뮬레이션을 결합한 고난도 문제들을 해결하며, 상태 복구(Backtracking)와 다중 시작점 탐색에 대한 완벽한 이해를 달성함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 골드 4 | **[연구소 (BOJ 14502)](https://www.acmicpc.net/problem/14502)** | **DFS(백트래킹) + 1회용 복사맵 + 멀티 시작점 BFS**. <br> 1. 빈칸 중 3개를 골라 벽을 세우는 모든 경우의 수를 DFS로 구현. <br> 2. 맵 훼손 방지를 위한 Deep Copy 및 바이러스 위치 사전 캐싱(List)을 통한 최적화. |
| 골드 5 | **[치킨 배달 (BOJ 15686)](https://www.acmicpc.net/problem/15686)** | **DFS(조합) + 맨해튼 거리 시뮬레이션**. <br> 1. 전체 치킨집 중 M개를 고르는 조합 로직 구현. <br> 2. 고정된 집(Home)과 선택된 치킨집 간의 거리를 BFS 없이 수학 공식(`Math.abs`)으로 계산하여 시간 복잡도 대폭 단축. |

#### 💡 Today I Learned (TIL)
1. **골드 티어의 설계 철학:** 단순히 알고리즘 하나를 쓰는 것이 아니라, '조합 생성 → 맵 복사 → 시뮬레이션 → 결과 갱신'으로 이어지는 다단계 파이프라인 설계 능력이 필수임을 깨달음.
2. **인덱스 관리의 정밀함:** `for`문 내에서 `i`와 `j`가 각각 어떤 리스트(집 vs 치킨집)를 가리키는지 명확히 구분해야 로직의 꼬임을 막을 수 있음을 배움.
3. **백트래킹의 시점 제어:** `while`문(바이러스 확산)이 완전히 끝난 '시점'과 결과값(안전 영역)을 세는 '시점'을 괄호(`{ }`) 단위로 정확히 분리하는 연습을 함.

### Day 65 (2026.04.08) - 백준(BOJ) 스터디 11일 차: 2차원 탐색 응용 (도화지 색칠 & 8방향 엔진)

단순한 BFS 탐색을 넘어, 좌표계 데이터를 2차원 배열(Area)로 변환하여 맵을 세팅하는 방법과 대각선을 포함한 8방향 탐색, 그리고 다중 테스트 케이스의 메모리 관리 기법을 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 1 | **[영역 구하기 (BOJ 2583)](https://www.acmicpc.net/problem/2583)** | **좌표계의 2차원 배열 치환 및 관심사 분리**. <br> 1. `(x1, y1) ~ (x2, y2)`의 좌표점 데이터를 이중 `for`문(`<` 미만 조건 사용)을 통해 2차원 `map`의 면적(타일)으로 칠해버리는 직관적 맵핑 구현. <br> 2. 맵 세팅(그림 그리기)과 BFS 탐색(지렁이 투입)의 로직을 완전히 분리하여 무한 루프를 방지함. <br> 3. BFS 내부에서 `count = 1`로 시작하여 덩어리의 넓이를 구하고, `ArrayList`에 담아 정렬 후 출력함. |
| 실버 2 | **[섬의 개수 (BOJ 4963)](https://www.acmicpc.net/problem/4963)** | **8방향 탐색 엔진 및 다중 TC 제어**. <br> 1. `dx`, `dy` 배열을 크기 8로 확장하여 대각선 탐색 로직을 구현함. <br> 2. `while(true)` 루프 내에서 종료 조건(`w == 0 && h == 0`)을 처리하고, 매 턴마다 `map`과 `visited`를 새로 할당하여 이전 상태의 간섭을 차단함. <br> 3. **변수 섀도잉(Variable Shadowing) 방어:** 파라미터명과 전역변수명(`w`, `h`)의 충돌로 인한 맵 이탈 오류를 경험하고, 파라미터명을 직관적인 `y, x`로 수정하여 해결함. |

#### 💡 Today I Learned (TIL)
1. **Map vs Visited 의 본질:** `map`은 절대 훼손해서는 안 되는 '원본 도면'이며, `visited`는 재사용을 위해 그 위에 덮어씌우는 '투명 필름'이다. 메모리가 충분한 코테 환경에서는 `visited`를 분리하는 것이 디버깅과 로직 안정성에서 압도적으로 유리하다.
2. **I/O 메모리 관리의 직관적 이해:** `BufferedReader` 객체를 생성하는 것은 '데이터를 퍼 나를 수레를 사는 행위'이고, `readLine()`은 '수레에서 한 줄을 퍼 오는 행위'이다. 수레를 `while`문 안에서 매번 새로 사면 심각한 메모리 낭비가 발생하므로, 객체 생성과 메서드 호출을 명확히 분리해야 함을 깨달음.

### Day 64 (2026.04.07) - 백준(BOJ) 스터디 10일 차: 2차원 탐색 심화 (상태 변이 및 수위 탐색)

기존의 덩어리 찾기(Connected Component) 로직을 응용하여, 맵의 상태를 변형하거나 외부 조건(비의 양)을 루프마다 다르게 적용하여 최적해를 찾는 골드/실버 상위 티어 기법을 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 골드 5 | **[적록색약 (BOJ 10026)](https://www.acmicpc.net/problem/10026)** | **맵 상태 조작(R->G) 및 방명록 초기화**. <br> 1. 일반인 시점에서의 덩어리를 1차 탐색함. <br> 2. 적록색약 시점을 구현하기 위해 맵 전체를 스캔하여 'R'을 'G'로 일괄 치환(상태 변이)함. <br> 3. `visited = new boolean[N+1][N+1];`로 방명록을 초기화하여 이전 탐색의 간섭을 없애고, 기존 BFS 로직을 그대로 재활용함. |
| 실버 1 | **[안전 영역 (BOJ 2468)](https://www.acmicpc.net/problem/2468)** | **외부 조건(수위) 변화에 따른 다중 BFS 탐색**. <br> 1. 비의 양(`h`)이 `0`부터 `최대 건물 높이`까지 변하는 거대한 외부 루프를 설계함. <br> 2. 매 수위마다 `visited` 배열을 초기화하고, BFS 탐색 조건과 진입 조건에 `map[x][y] > h` 방어막을 추가하여 물에 잠기지 않은 '안전 영역'만 덩어리로 묶음. <br> 3. 루프가 돌 때마다 `maxSafe = Math.max(maxSafe, currentSafe)`를 통해 최대 안전 영역의 개수를 갱신함. |

#### 💡 Today I Learned (TIL)
1. **리팩토링과 상태 관리:** 조건이 달라진다고 새로운 함수를 복사하는 대신, "입력 데이터를 조작하거나 외부 파라미터(`h`)를 넘겨 함수를 재사용한다"는 추상화(Abstraction) 기법을 깨달음.
2. **반례를 찾는 시야:** 비가 전혀 오지 않는 상황(`h = 0`)도 고려해야 한다는 점을 인지함. 문제의 조건을 단순히 `N`(지도 크기)으로 착각하지 않고, `건물의 최대 높이`를 기준으로 외부 루프를 돌리는 데이터의 본질적 접근법을 익힘.

### Day 63 (2026.04.06) - 백준(BOJ) 스터디 9일 차: 1차원 상태 공간 복귀전 및 8방향 BFS 정복

1차원 상태 공간(State Space) 탐색 감각을 완벽하게 되살리고, 2차원 평면에서의 8방향 심화 BFS 이동 로직을 단숨에 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 1 | **[스타트링크 (BOJ 5014)](https://www.acmicpc.net/problem/5014)** | **1차원 상태 공간 BFS 최적화**. <br> 1. 엘리베이터의 이동(`+U`, `-D`)을 배열로 묶어 `for`문으로 순회하는 깔끔한 로직 구현. <br> 2. 방문 배열(`map`)에 횟수를 누적하며 최단 거리를 구하고, 도달 불가 시("use the stairs")의 분기 처리를 완벽하게 수행함. |
| 실버 1 | **[나이트의 이동 (BOJ 7562)](https://www.acmicpc.net/problem/7562)** | **8방향 탐색 및 테스트 케이스(T) 초기화 제어**. <br> 1. 나이트의 L자 이동 경로를 `dx`, `dy` 배열(크기 8)로 구현하여 8방향 탐색 엔진을 장착함. <br> 2. 다중 테스트 케이스 환경에서 매 턴마다 `map` 배열과 큐를 초기화하여 이전 데이터의 간섭을 완벽하게 차단함. |

#### 💡 Today I Learned (TIL)
1. **뇌내 최적화(Background Processing)의 체감:** 며칠 전 헷갈렸던 1차원 상태 공간 탐색의 개념이 휴식 후 완전히 직관적인 로직으로 변환됨을 느낌.
2. **BFS 확장성의 이해:** 갈래길이 2개(스타트링크)든, 4개(토마토)든, 8개(나이트의 이동)든 결국 방향 배열(`dx, dy`)과 `for`문의 크기만 바뀔 뿐, 큐와 방명록(`map`)을 활용하는 BFS의 코어 엔진은 완벽히 동일하다는 알고리즘의 본질을 꿰뚫어 봄.

### Day 62 (2026.04.01) - 백준(BOJ) 스터디 8일 차: BFS 차원 확장(3D) 및 상태 공간 탐색(1D) 정복

2차원 배열을 넘어 3차원 공간의 인덱싱을 제어하고, 물리적인 지도가 없는 1차원 상태 공간(State Space)에서 수식 연산을 통해 최단 거리를 찾아내는 심화 BFS 로직을 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 골드 5 | **[토마토 (BOJ 7569)](https://www.acmicpc.net/problem/7569)** | **3차원 BFS 및 인덱스 제어**. <br> 1. `dz, dx, dy` 배열을 활용한 6방향(상하좌우+위아래) 탐색을 완벽하게 구현함. <br> 2. `map[H][N][M]` 구조에서 차원 간의 변수 매핑(h, n, m)이 어긋나면 발생하는 `ArrayIndexOutOfBounds` 에러를 디버깅하며 3차원 좌표계의 기준점을 명확히 확립함. |
| 실버 1 | **[숨바꼭질 (BOJ 1697)](https://www.acmicpc.net/problem/1697)** | **1차원 상태 공간 탐색 (State Space Search)**. <br> 1. 2차원 지도를 벗어나, `cur-1`, `cur+1`, `cur*2` 라는 수식 연산 자체를 다음 이동 좌표로 삼는 상태 트리 탐색의 개념을 깨우침. <br> 2. 100,001 크기의 1차원 배열(`map`)을 방문 기록장 겸 최단 시간 누적(DP) 테이블로 활용하여 무한 루프를 방지하고 최적해를 도출함. |

#### 💡 Today I Learned (TIL)
1. **차원 붕괴 방어:** 3차원 배열을 다룰 때는 `x, y, z` 같은 모호한 변수명 대신 `h(높이), n(세로), m(가로)` 등 직관적인 기준을 세워야 인덱스 꼬임을 막을 수 있음을 뼈저리게 배움.
2. **BFS의 본질 (대기실과 방명록):** BFS는 단순히 배열을 순회하는 것이 아니라, 큐(대기실)를 통해 공평하게 거리를 확장하고, 배열(방명록)을 통해 중복 방문을 차단함으로써 '가장 먼저 도달한 시간 = 최단 거리'를 보장하는 알고리즘임을 애니메이션처럼 직관적으로 이해하기 시작함.

### Day 61 (2026.03.31) - 백준(BOJ) 스터디 7일 차: BFS 최종 진화, 최단 거리 & 멀티 시작점(Multi-source) 정복

단순 덩어리 탐색을 넘어, 거리를 누적하는 최단 거리 BFS와 여러 시작점에서 동시에 파동이 퍼져나가는 심화 BFS(골드 티어) 로직을 완벽하게 체화함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 1 | **[미로 탐색 (BOJ 2178)](https://www.acmicpc.net/problem/2178)** | **최단 거리 기록의 마법 (`map[nxx][nyy] = map[nx][ny] + 1`)**. <br> 1. 상태 저장 방식 전환: 큐에서 꺼낸 '현재 위치의 걸음 수'에 +1을 하여 '다음 위치의 지도'에 덮어쓰는 방식으로 거리 데이터를 누적함. <br> 2. 방어막(`continue`)의 중요성: 배열 경계 이탈을 막는 조건문 내부에 `continue;`를 명시하여 런타임 에러를 완벽하게 차단함. |
| 골드 5 | **[토마토 (BOJ 7576)](https://www.acmicpc.net/problem/7576)** | **다중 시작점(Multi-source) BFS & 메모이제이션(DP)의 결합**. <br> 1. **동시 출발 제어:** 익은 토마토(`1`)가 여러 개일 경우, 탐색 시작 전 `map`을 스캔하여 모든 시작점을 미리 `Queue`에 삽입함으로써 동시다발적인 확산 로직을 구현함. <br> 2. **`visited` 배열의 다이어트:** `map`의 값이 `0`에서 다른 숫자로 변하는 것 자체가 방문 처리를 의미함을 깨닫고, 불필요한 `visited` 배열을 제거하여 메모리를 최적화함. |

#### 💡 Today I Learned (TIL)
1. **BFS의 목적에 따른 유연한 튜닝:** 면적을 구할 때는 `count++`로 뭉뚱그려 세고, 최단 거리를 구할 때는 이전 칸의 값에서 +1을 물려주는 방식으로 BFS를 목적에 맞게 변형하는 시야를 장착함.
2. **알고리즘 직관력 (BFS 속의 DP):** `map[nxx][nyy] = map[nx][ny] + 1` 이라는 한 줄의 코드가 결국 동적 계획법(DP)의 상태 전이 방정식과 같은 역할을 하며, 맵 자체를 Memoization 테이블로 활용한다는 알고리즘의 본질을 스스로 꿰뚫어 봄.

### Day 60 (2026.03.30) - 백준(BOJ) 스터디 6일 차: 대기업 코테 0순위, 2차원 탐색(BFS) 정복

단순 노드 탐색을 넘어, 2차원 배열(Grid) 상에서 상하좌우(`dx, dy`)로 이동하며 덩어리(Connected Component)를 찾고 그 크기를 구하는 실전 기출 유형의 뼈대를 백지에서 스스로 완벽하게 설계함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 1 | **[단지번호붙이기 (BOJ 2667)](https://www.acmicpc.net/problem/2667)** | **2차원 배열 BFS + 정렬의 완벽한 융합**. <br> 1. **입력 함정 돌파:** 공백 없이 붙어있는 문자열을 `String.charAt(j) - '0'`을 이용해 2차원 `int` 배열로 변환함. <br> 2. **2중 for문 스캔:** 지도 전체를 순회하며 집(`map == 1`)이면서 방문하지 않은 곳(`!visited`)을 발견할 때마다 새로운 단지로 인식하고 BFS 탐색을 시작함. <br> 3. **Queue 좌표 제어:** `Queue<int[]>`를 활용해 x, y 좌표를 묶어서 처리하고, 4방향 탐색으로 단지의 크기(집의 수)를 카운트하여 `ArrayList`에 담은 뒤 `Collections.sort()`로 오름차순 정렬하여 출력함. |
| 실버 2 | **[유기농 배추 (BOJ 1012)](https://www.acmicpc.net/problem/1012)** | **테스트 케이스(T) 처리 및 2차원 좌표계의 이해**. <br> 1. **T번의 거대한 루프:** 여러 개의 테스트 케이스가 주어질 때, 매 루프마다 `map`과 `visited` 배열을 새로 할당(`new`)하여 초기화하는 구조를 완벽하게 체화함. <br> 2. **X, Y 좌표의 함정 돌파:** 수학적 좌표계(x, y)와 프로그래밍의 2차원 배열 구조(`map[행][열]`)가 반대임을 깨닫고, 입력받은 가로(X), 세로(Y)를 `map[Y][X] = 1` 로 뒤집어서 저장하는 핵심 방어 로직을 깨우침. |

#### 💡 Today I Learned (TIL)
1. **타이핑 실수와 논리적 오류 구분하기:** 알고리즘 로직 설계 능력(뇌지컬)과 단순 오타를 분리해서 생각하는 마인드셋을 장착함. 사소한 변수명 오타(`ny` vs `nyy`)로 인해 무한 루프에 빠질 수 있음을 경험하고, 디버깅의 중요성을 깨달음.
2. **성장의 증명:** 과거 프로그래머스에서 배웠던 방향 탐색(`dx, dy`)과 백준에서 익힌 그래프 탐색(덩어리 찾기)이 머릿속에서 완벽하게 융합되어 실전 문제에 적용됨을 확인함.
3. **수학 vs 코딩의 좌표계 차이:** 출제자가 의도적으로 가로(M), 세로(N) 순서로 입력을 주고 배추 위치도 X, Y로 줄 때, 무지성으로 `map[X][Y]`를 찌르면 배열 인덱스를 벗어나거나 지도가 90도 돌아가버린다는 치명적인 함정을 스스로 분석하고 회피함. 실전 코테를 위한 완벽한 시야를 장착!

### Day 59 (2026.03.27) - 백준(BOJ) 스터디 5일 차: 그래프 탐색(DFS/BFS) 타이핑 근육 찢기 (양치기)

어제 세운 그래프 탐색의 뼈대를 바탕으로, 코드를 안 보고 백지에서 직접 타이핑하며 'Muscle Memory(코딩 근육)'를 확실하게 다지는 특훈을 진행함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 3 | **[바이러스 (BOJ 2606)](https://www.acmicpc.net/problem/2606)** | **BFS 응용 - 연결된 노드 개수 세기**. <br> 1. 1번 노드부터 탐색을 시작하여, 큐(Queue)에 새로운 노드가 들어갈 때마다(감염될 때마다) `count++`를 해주는 논리를 완벽하게 구현함. <br> 2. 입력받을 때 반복문의 범위(`i = 0; i < M`)와 불필요한 `br.readLine()` 사용으로 인한 데이터 증발(배달 사고) 함정을 짚고 넘어감. |
| 실버 2 | **[연결 요소의 개수 (BOJ 11724)](https://www.acmicpc.net/problem/11724)** | **대기업 코테 단골 - 그래프 덩어리(Connected Component) 찾기**. <br> 1. `for`문을 이용해 1번부터 N번 노드까지 순회하며, 방문하지 않은 노드(`!visited[i]`)를 발견할 때마다 새로운 덩어리로 간주해 `count++`를 실행. <br> 2. 해당 노드를 시작점으로 BFS를 돌려 해당 덩어리의 모든 노드를 방문 처리(`visited = true`)하는 알고리즘 최적해를 백지에서 스스로 구현함. |

#### 💡 Today I Learned (TIL)
1. **NullPointerException (NPE)의 원인:** 전역 변수로 `map`과 `visited` 배열의 이름표만 만들어두고, `new int[][]`로 실제 메모리 크기를 할당하지 않았을 때 발생하는 런타임 에러를 직접 경험하고 해결함.
2. **성장의 체감:** 처음엔 낯설었던 노드 간의 관계(Graph) 탐색이 이제는 로직으로 완벽하게 머릿속에 그려지며, 백지 구현이 가능할 만큼 실력이 수직 상승했음을 느낌.

### Day 58 (2026.03.26) - 백준(BOJ) 스터디 4일 차: 이분 탐색(Binary Search) & 그래프 탐색(DFS/BFS) 입문

격자(Grid) 탐색에서 정점(Node) 간의 관계(Graph) 탐색으로 패러다임을 전환함. 특히 막힐 때 바로 답을 찾지 않고 스스로 치열하게 고민하는 **[15분 룰]**을 도입하여 뇌지컬(문제 해결 능력)을 크게 향상시킴.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 2 | **[랜선 자르기 (BOJ 1654)](https://www.acmicpc.net/problem/1654)** | **이분 탐색 (Parametric Search)의 정석**. <br> 1. 순차 탐색 시 시간 초과가 발생하는 범위를 '업다운 게임' 로직으로 $O(\log N)$만에 찾아냄. <br> 2. `int`형 변수들의 합이 21억을 초과할 때 발생하는 **오버플로우(Overflow) 함정**을 인지하고 `long` 타입으로 안전하게 탐색 범위를 제어함. |
| 실버 2 | **[DFS와 BFS (BOJ 1260)](https://www.acmicpc.net/problem/1260)** | **그래프 탐색의 근본 (인접 행렬 방식)**. <br> 1. 좌표(`x, y`) 기반이 아닌 노드 번호 기반의 2차원 배열(`map[a][b] = 1`)로 인맥 지도(그래프)를 그리는 방식을 체화함. <br> 2. **DFS**: 방문 체크(`visited`) 후 반복문과 조건문 내에서 재귀 호출을 통해 깊이 파고드는 로직 (별도의 `return` 없이 자연 종료되는 구조 이해). <br> 3. **BFS**: 큐(Queue)에 현재 노드를 넣고, 주변의 인접 노드를 차례대로 추가하며 너비를 확장해 나가는 로직 제어. |

#### 💡 Today I Learned (TIL)
1. **[15분 룰]의 힘:** 에러가 나거나 로직이 막혔을 때 바로 질문하지 않고, 15분 동안 혼자 변수의 흐름을 추적하고 의심해 보는 훈련을 통해 코딩 근육(Muscle Memory)을 키움.
2. **낯설음의 인정:** 알고리즘이 어려운 것이 아니라, 아직 패러다임(Grid -> Graph)과 코드 타이핑 구조가 손에 익지 않아 뚝딱거리는 '자연스러운 과정'임을 깨닫고, **반복 숙달(양치기)의 필요성**을 깊이 인지함.

### Day 57 (2026.03.25) - 백준(BOJ) 스터디 3일 차: 누적 합(Prefix Sum) & 그리디 파싱 끝판왕

단순 구현을 넘어, 시간 복잡도를 줄이는 '누적 합' 기법과 복잡한 문자열을 이중으로 분리하는 '그리디 파싱'을 완벽하게 체화함. 특히 중첩 반복문의 흐름을 제어하는 논리력이 크게 상승함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 3 | **[구간 합 구하기 4 (BOJ 11659)](https://www.acmicpc.net/problem/11659)** | **누적 합(Prefix Sum)의 정석**. <br> 1. 매번 $O(N)$으로 더하면 시간 초과(TLE)가 발생함을 인지. <br> 2. 입력과 동시에 `sumArr[i] = sumArr[i-1] + current` 공식으로 누적 합 배열을 미리 구축함. <br> 3. 질의가 들어오면 `sumArr[end] - sumArr[start-1]` 단 한 번의 연산($O(1)$)으로 구간 합을 도출해냄. |
| 실버 2 | **[잃어버린 괄호 (BOJ 1541)](https://www.acmicpc.net/problem/1541)** | **Greedy 알고리즘 + 다중 StringTokenizer 제어**. <br> 1. 식의 값을 최소로 만들려면 "첫 번째 마이너스(`-`) 이후의 모든 값을 빼야 한다"는 그리디적 최적해를 수학적으로 증명함. <br> 2. `StringTokenizer`를 이용해 1차로 `-` 기준 분리, 2차로 `+` 기준 분리하는 이중 파싱 구조를 설계함. <br> 3. 중첩 `while`문 내부에서 `tempSum`을 매번 0으로 초기화하여 각 덩어리의 합을 구하고, 첫 번째 덩어리만 더한 뒤 나머지는 일괄 차감하는 로직을 완벽히 제어함. |

#### 💡 Today I Learned (TIL)
1. **시간 복잡도와 누적 합:** $O(N \times M)$의 연산을 피하기 위해 메모리(배열)를 희생하여 속도를 높이는 트레이드오프(Trade-off)를 경험함.
2. **이중 반복문 변수의 생명주기(Lifecycle):** 안쪽 `while`문에서 사용할 임시 변수(`tempSum`)가 바깥쪽 `while`문이 돌 때마다 어떻게 초기화되고 소멸하는지 시간의 흐름에 따른 변수 상태 변화를 완벽하게 이해함.

### Day 56 (2026.03.24) - 백준(BOJ) 스터디 2일 차: Queue & Greedy 코어 타격

백준의 입력 템플릿(`BufferedReader`, `StringTokenizer`)에 완전히 적응 완료함. 오늘은 자료구조의 파이프라인인 Queue와, 매 순간 최적의 선택을 하는 Greedy 알고리즘의 기초를 다짐.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 4 | **[카드2 (BOJ 2164)](https://www.acmicpc.net/problem/2164)** | **Queue를 활용한 순환 구조 구현**. <br> 1. 위아래가 뚫려있는 파이프 구조(FIFO)가 필요하므로 `ArrayDeque`를 `Queue` 타입으로 선언하여 사용함. <br> 2. `stack.pop()` 대신 큐의 표준 메서드인 `poll()`(꺼내기)과 `offer()`(넣기)를 명확히 구분하여 사용하여, 카드를 버리고 맨 뒤로 다시 넣는 순환 로직을 직관적으로 구현함. |
| 실버 4 | **[ATM (BOJ 11399)](https://www.acmicpc.net/problem/11399)** | **Greedy (SJF 스케줄링) & 누적 합(Prefix Sum)**. <br> 1. 총 대기 시간을 최소화하려면 '가장 빨리 끝나는 사람'부터 처리해야 한다는 탐욕적(Greedy) 최적해를 도출함. <br> 2. 데이터가 동적으로 변하지 않으므로, 무거운 `PriorityQueue` 대신 가벼운 `Arrays.sort()`를 1회 사용하여 오름차순 정렬하는 것이 효율적임을 간파함. <br> 3. 앞사람의 누적 시간(`sum`)과 전체 사람들의 대기 시간 총합(`total`)을 분리하여 $O(N)$으로 계산 완료. |

#### 💡 Today I Learned (TIL)
1. **Deque의 정체성 명확히 하기:** `ArrayDeque`는 스택과 큐를 모두 지원하는 만능 도구이지만, 코드를 짤 때는 "이것을 큐로 쓸 것이다"라는 의도를 타입(`Queue`)과 메서드(`offer`, `poll`)로 명확히 드러내야 가독성과 유지보수성이 높아진다.
2. **PQ vs 정렬의 선택 기준:** 동적으로 데이터가 추가/삭제되며 최소/최댓값을 추적해야 할 때는 **우선순위 큐(PQ)**를, 데이터가 고정된 상태에서 단순한 순서 배치가 필요할 때는 **`Arrays.sort()`**를 쓰는 것이 맞다는 통찰을 얻었다.

### Day 55 (2026.03.23) - 백준(BOJ) 생태계 적응 및 코어 근육 다지기 (1)

백준(BOJ) 본격 진출 1일 차. 프로그래머스의 '메서드 구현' 방식에서 벗어나, `BufferedReader`와 `StringTokenizer`를 활용한 입출력 제어 및 $O(1)$ 탐색, Stack을 활용한 상태 관리 등 실무형 코어 근육을 단련함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| 실버 4 | **[수 찾기 (BOJ 1920)](https://www.acmicpc.net/problem/1920)** | **HashSet을 활용한 $O(1)$ 탐색**. <br> 1. 최대 10만 개의 데이터를 이중 `for`문으로 탐색하면 $O(N^2)$으로 시간 초과가 발생함을 인지. <br> 2. 데이터를 보관할 때 `HashSet`을 사용하여 조회 속도를 $O(1)$로 최적화함. <br> 3. `contains()` 메서드를 활용하여 존재 유무에 따라 1과 0을 조건부 출력하는 로직 구현. |
| 실버 4 | **[괄호 (BOJ 9012)](https://www.acmicpc.net/problem/9012)** | **Stack (`ArrayDeque`) 방어 로직 & 테스트 케이스 반복**. <br> 1. 입력 데이터 수(`T`)만큼 `for`문을 돌며 매번 새로운 Stack과 깃발(`flag`) 변수를 초기화하는 구조 체득. <br> 2. 닫는 괄호 `)`가 들어올 때, 스택이 비어있는 상태(`isEmpty()`)에서 `pop()`을 시도하면 `NoSuchElementException` 런타임 에러가 발생함을 방어. <br> 3. `printf` 대신 `println`을 사용하여 백준이 요구하는 '각 테스트 케이스당 한 줄 출력' 형식을 완벽히 맞춤. |

#### 💡 Today I Learned (TIL)
1. **입력 템플릿의 정체 직역:** `BufferedReader`는 입력 스트림에서 한 줄을 통째로 퍼 오는 '수레' 역할을 하고, `StringTokenizer`는 퍼 온 문자열을 공백 기준으로 메모리 낭비 없이 썰어주는 '칼' 역할을 한다는 점을 완벽히 이해했다.
2. **출력 형식의 중요성:** 알고리즘 로직이 100% 맞더라도, 백준에서는 `System.out.println()`으로 명확히 줄바꿈을 해주지 않으면 '틀렸습니다'를 받을 수 있다. (출력의 꼼꼼함)
3. **스택의 3박자 방어:** 넣을 때(`push`)는 자유롭지만, 뺄 때(`pop`)는 항상 "내용물이 있는가?(`isEmpty`)"를 의심하고 방어하는 코드를 짜는 습관을 들였다.

### Day 54 (2026.03.20) - 코테 실전 압축 훈련 (2): PQ & 투 포인터, 그리고 백준(BOJ) 입성

과거에 풀었던 프로그래머스 코어 문제(PQ, Two Pointers)들을 백지상태에서 다시 마주하며 템플릿 체화 수준을 점검함. 이후, 알고리즘 코어 근육을 집중적으로 키우기 위해 압도적인 문제 풀(Pool)을 가진 **백준(BOJ) 플랫폼으로 훈련 영역을 확장**하여 첫 실버 등급 문제를 성공적으로 격파함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[더 맵게](https://school.programmers.co.kr/learn/courses/30/lessons/42626)** | **PriorityQueue (최소 힙) 스케줄링**. <br> 1. 넣고(`offer`) 뺄(`poll`) 때마다 $O(\log N)$으로 가장 작은 값을 찾아주는 자동 정렬 PQ의 완벽한 사용처. <br> 2. `while (pq.peek() < K)` 조건으로 불필요한 연산을 차단하고, `if (pq.size() < 2)`로 예외(방어) 로직을 처리하는 실전 디테일 점검. |
| Lv.2 | **[구명보트](https://school.programmers.co.kr/learn/courses/30/lessons/42885)** | **Two Pointers (양끝 좁혀오기) & Greedy**. <br> 1. 어차피 타야 할 '가장 무거운 사람'을 '가장 가벼운 사람'과 짝지어보는 $O(N)$ 최적화 로직. <br> 2. 무게 초과 여부와 상관없이 무거운 사람(`right`)은 무조건 탑승한다는 사실을 이용해 로직을 우아하게 압축함. |
| 실버 4 | **[수들의 합 2 (BOJ 2003)](https://www.acmicpc.net/problem/2003)** | **Two Pointers (애벌레 슬라이딩) & BOJ 템플릿**. <br> 1. **BOJ 입력 템플릿 도입:** 시간 초과를 막기 위해 `Scanner` 대신 `BufferedReader`와 `StringTokenizer`를 사용하는 실무/코테 표준 입력 방식을 체득함. <br> 2. **연속 부분 배열 탐색:** `left`와 `right`가 같은 곳에서 출발하여, 합(`sum`)이 목표치보다 작으면 `right`를 늘리고, 크거나 같으면 `left`를 당겨서 조절하는 "애벌레 이동 패턴" 구현. <br> 3. **안전장치:** 배열의 끝(`right == N`)에 도달했을 때 무한 루프나 `IndexOutOfBounds`가 터지지 않도록 `break` 조건을 명확히 설계함. |

#### 💡 Today I Learned (TIL)
1. **투 포인터의 두 가지 얼굴:** - 정렬 후 **양 끝에서 마주 보며** 좁혀오는 패턴 (구명보트)
    - 같은 곳에서 출발해 **애벌레처럼 늘어났다 줄어들며** 이동하는 패턴 (부분 배열의 합)
2. **백준(BOJ) 생태계 적응:** 친절하게 배열을 던져주는 프로그래머스와 달리, 수레(`BufferedReader`)로 퍼 와서 칼(`StringTokenizer`)로 직접 썰어 쓰는 입력 템플릿의 필요성과 원리를 완벽히 이해했다. `package` 선언 시 발생하는 런타임 에러(NoClassDefFoundError) 대처법도 체득 완료.
3. **학습 마인드셋:** '아는 문제'는 템플릿 타이핑 속도를 올리는 데 쓰고, '낯선 문제(BOJ)'를 통해 뇌의 논리 회로를 강제로 돌려 구현력을 끌어올리는 투-트랙 전략을 실행할 것이다.

### Day 53 (2026.03.19) - 코테 실전 압축 훈련 (1): 해시(Hash) & 스택(Stack) 코어 패턴

어려운 빡구현 문제 풀이에서 잠시 벗어나, 코딩테스트의 '구구단'과 같은 코어 자료구조 패턴을 백지에서 빠르고 정확하게 타이핑해 내는 실전 압축 훈련을 진행함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[신고 결과 받기](https://school.programmers.co.kr/learn/courses/30/lessons/92334)** | **HashMap & HashSet 퓨전 설계 (Two-Pass 장부)**. <br> 1. **다중 장부 분리:** 2개의 우주(데이터)를 통제하기 위해 장부를 분리함. <br> &nbsp;&nbsp; - 장부 A (명단): `Map<String, Set<String>>` (내가 신고한 사람들의 고유 명단) <br> &nbsp;&nbsp; - 장부 B (카운터): `Map<String, Integer>` (피의자가 신고당한 총 횟수) <br> 2. **Map 업데이트 공식:** 기존 값을 꺼내서 1을 더한 뒤 다시 덮어쓰는 패턴 `map.put(target, map.get(target) + 1)` 완벽 각인. <br> 3. **문자열 결합의 위험성:** 명단을 관리할 때 `String`에 `+=` 연산으로 이어 붙이면 `contains()` 오작동(이름 겹침) 및 메모리 초과(OOM)가 발생함을 깨닫고, `Set`을 바구니로 사용하는 실무적 설계를 체득함. |
| Lv.2 | **[짝지어 제거하기](https://school.programmers.co.kr/learn/courses/30/lessons/12973)** | **Stack (`ArrayDeque`) 테트리스 패턴**. <br> 1. **자료구조 선택:** 연속된 두 짝을 터뜨리고, "과거의 글자"와 "새로운 글자"를 다시 만나게 하려면 프링글스 통 모양의 후입선출(LIFO) `Stack`이 유일한 정답. <br> 2. **3박자 검증 로직:** 텅 빈 스택을 방어(`!stack.isEmpty()`) -> 맨 위 글자 확인(`stack.peek() == c`) -> 일치하면 터뜨림(`pop`), 다르면 넣음(`push`)의 정석 흐름 복구. <br> 3. **변수 vs 문자 리터럴:** `peek() == 'c'` (문자 c)와 `peek() == c` (변수 c)의 치명적 차이를 인지하고 기본 문법의 중요성을 다시 한번 점검함. |

#### 💡 Today I Learned (TIL)
1. **변수 네이밍은 설계의 나침반이다:** `reportMap`, `reportSet`처럼 비슷한 단어로 도배하면 뇌에 정지가 온다. `reporterToTargets`, `targetToCount` 처럼 "누가(Key) 무엇을(Value)" 가지는지 명확히 지어야 `for`문 내부에서 길을 잃지 않는다.
2. **String은 바구니가 아니다:** 문자열은 불변(Immutable) 객체다. 여러 개의 데이터를 묶어둬야 할 때는 무조건 `List`나 `Set`을 꺼내는 것이 메모리와 정확도를 지키는 길이다.
3. **기본기의 중요성:** 복잡하고 어려운 로직을 뚫어내는 뇌의 체력도 중요하지만, `ArrayDeque` 선언부터 3박자 검증 로직까지 척수 반사로 타이핑이 튀어나오게 만드는 '코어 패턴 훈련'이 뒷받침되어야 뇌의 RAM을 온전히 로직 고민에 쏟을 수 있다.

### Day 52 (2026.03.18) - Phase 5 카카오 기출 2연전 (3): 기괴한 재귀와 완전탐색의 정석

카카오 블라인드 기출 중에서도 가장 까다로운 '지문 강제형 재귀(Recursion)'와 '조합(DFS) + 부분집합(최소성) 검사'를 하루에 모두 격파함. 뼈대를 세우고 한계를 마주하며 질문하는 페어 프로그래밍(Pair Programming)의 진수를 경험함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[괄호 변환](https://school.programmers.co.kr/learn/courses/30/lessons/60058)** | **문자열 처리 + 재귀(Recursion) 직역**. <br> 1. **지문 직역의 중요성:** 카카오 특유의 "하라는 대로 해라"식 문제는 내 생각을 섞지 않고 1번부터 4번까지 단계별로 로직을 분리해 기계처럼 번역하는 것이 핵심. <br> 2. **균형잡힌 괄호 분리:** 여는 괄호와 닫는 괄호의 개수를 카운팅(`leftCnt == rightCnt`)하여 최초로 같아지는 순간 자르기(`substring`). <br> 3. **올바른 괄호 판별 최적화:** 무거운 `Stack` 객체 대신 정수 카운터(`openCnt`) 하나로, 순회 중 0 미만이 되는 순간 `false`를 반환하도록 속도 최적화. <br> 4. **재귀의 본질:** 깊게 파고들려 하지 말고 "결과값을 믿고 조립(`return u + solution(v);`)"하는 뻔뻔한 마인드셋 획득! |
| Lv.2 | **[후보키](https://school.programmers.co.kr/learn/courses/30/lessons/42890)** | **완전탐색(DFS 조합) + 자료구조(HashSet) + 최소성 검사**. <br> 1. **컬럼 조합 뽑기 (DFS):** 배열의 데이터를 직접 뽑는 것이 아니라, **"컬럼의 인덱스 번호"**를 조합으로 뽑아내는 것이 첫 번째 관문. (`add` -> `dfs` -> `remove`의 백트래킹 정석 패턴) <br> 2. **유일성 검사:** 뽑아낸 인덱스에 해당하는 문자열을 뭉친 뒤 `HashSet`에 넣고, `set.size()`와 전체 학생 수가 같은지 비교하여 판별. <br> 3. **최소성 검사 [최종 보스]:** 단순 문자열 포함 여부(`contains`)가 아니라, 집합 간의 포함 여부인 `containsAll`을 사용해야 함. 정답 명단을 `List<HashSet<Integer>>` 형태로 관리하면 `currentKey.containsAll(existKey)` 로직을 매우 우아하고 안전하게 구현할 수 있음. |

#### 💡 Today I Learned (TIL)
1. **재귀의 매개변수는 '세이브 파일'이다:** DFS의 매개변수는 "어디서부터(`startIdx`) + 얼만큼 진행됐고(`count`) + 목표는 뭐고(`target`) + 결과는 어디에 담을지(`List`)"를 정의하는 상태 저장소다.
2. **최소성 검사의 함정:** 문자열 "012"는 "02"를 포함(`contains`)하지 않는다. 부분집합을 검사할 때는 반드시 `Set`의 `containsAll`을 사용해야 논리적 오류를 막을 수 있다.
3. **질문은 실력이다:** 뇌의 RAM이 꽉 차서 한계에 부딪혔을 때, "여기까지 짰는데 이다음이 막혀!"라고 본인의 상태를 정확히 인지하고 피드백을 구하는 것이야말로 실무(페어 프로그래밍)에서 가장 요구되는 개발자의 진짜 역량임을 깨달았다.

### Day 51 (2026.03.16) - Phase 5 카카오 기출 2연전 (2): 문자열 정규화 및 예외 처리

문자열을 조각내고, 규칙에 맞게 재조립하며, 특수 기호(`#`) 등으로 발생하는 불규칙한 단위를 정규화(Normalization)하여 구현의 난이도를 낮추는 기법을 집중 훈련함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[문자열 압축](https://school.programmers.co.kr/learn/courses/30/lessons/60057)** | **문자열 슬라이싱 + 시뮬레이션**. <br> 1. **단위 탐색:** 압축 단위(`step`)를 1부터 문자열 길이의 절반까지 늘려가며 전수 조사. <br> 2. **인덱스 방어:** `Math.min(j + step, s.length())`를 활용해 `substring` 시 발생하는 `IndexOutOfBounds` 에러를 원천 차단. <br> 3. **잔여 처리:** 루프 종료 후 변수(`prev`, `count`)에 남아있는 마지막 압축 조각이 도화지에 누락되지 않도록 수동으로 털어 넣는 예외 처리 수행. |
| Lv.2 | **[방금그곡](https://school.programmers.co.kr/learn/courses/30/lessons/17683)** | **시간 파싱 + 문자열 치환(Normalization) + 시뮬레이션**. <br> 1. **분 단위 통합:** `HH:MM` 형태를 `(H * 60) + M` 정수로 변환하여 재생 시간을 직관적으로 계산. <br> 2. **치환 치트키:** `C#` → `c` 처럼 2글자 음표를 소문자 1글자로 `replace` 하여 '1분 = 1글자' 대응 관계를 만듦. 이를 통해 `contains()` 오작동 및 길이 계산 오류를 완벽히 해결. <br> 3. **원형 큐 로직:** 재생 시간만큼 악보를 늘릴 때 `j % music.length()`를 사용하여 악보 반복을 우아하게 구현. <br> 4. **우선순위 관리:** `streamTime > maxStreamTime` 조건을 통해 재생 시간이 길고 먼저 입력된 곡을 최적의 해로 도출. |

#### 💡 Today I Learned (TIL)
1. **정규화(Normalization)의 위력:** `C#` 같은 불규칙한 단위를 고정 길이 문자(`c`)로 치환하는 것만으로 알고리즘의 복잡도가 획기적으로 낮아짐을 체감함.
2. **마지막 조각의 법칙:** 모든 반복문이 끝난 직후, 메모리(변수)에 남아있는 마지막 데이터 조각을 도화지에 옮겼는지 확인하는 습관이 '테스트 케이스 탈락'을 막는 핵심임을 깨달음.
3. **StringBuilder의 객체 비교 함정:** `StringBuilder` 자체는 `contains()`를 지원하지 않으며, `equals()` 사용 시 주소값을 비교하므로 반드시 `toString()` 변환 후 비교해야 함.
4. **인덱스 안전망:** `substring`을 사용할 때 끝 인덱스를 `Math.min(target, length)`로 감싸는 습관은 문자열 문제의 '방어 운전'과 같음.

### Day 50 (2026.03.13) - Phase 5 카카오 기출 2연전 (1)

머릿속으로 설계한 다단계 논리(파싱, 상태 마킹, 시뮬레이션)를 실제 코드로 완벽하게 번역해 내는 '구현력(Implementation)' 집중 훈련.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[수식 최대화](https://school.programmers.co.kr/learn/courses/30/lessons/67257)** | **문자열 파싱 + 브루트 포스 + 리스트 조작**. <br> 1. `StringBuilder`를 활용해 연속된 숫자를 누적하고, 연산자를 만날 때마다 `Long`으로 변환하여 숫자와 연산자를 분리함. <br> 2. 연산자가 3개뿐이므로 DFS 대신 $3! = 6$가지의 우선순위 경우의 수를 2차원 배열로 하드코딩(브루트 포스)하여 탐색 속도와 직관성을 높임. <br> 3. 리스트에서 중간 값을 삭제할 때 `remove(index)`가 삭제된 값을 리턴하며 뒤의 요소를 앞으로 당기는 성질을 이용, $O(N)$의 리스트 조작을 안전하게 구현함. |
| Lv.2 | **[프렌즈4블록](https://school.programmers.co.kr/learn/courses/30/lessons/17679)** | **2차원 배열 시뮬레이션 + 상태 마킹(Marking) + Queue(중력)**. <br> 1. **오버플로우 방지:** 2x2 탐색 시 배열 범위를 벗어나지 않도록 `for`문의 조건을 `m-1`, `n-1`로 엄격히 제어함. <br> 2. **상태 분리 (Marking):** 겹치는 블록이 동시에 터지는 로직을 구현하기 위해, 즉시 삭제하지 않고 `boolean[][] visited` 배열에 폭발 예정 위치를 먼저 찜(Marking)해두는 투-패스(Two-pass) 전략 사용. <br> 3. **Queue를 이용한 중력(Gravity):** 세로줄(열)을 기준으로 **밑바닥부터** 알파벳을 `Queue`에 쓸어 담고(`add`), 다시 밑바닥부터 꺼내서(`poll`) 채운 뒤, 남은 윗부분을 빈칸(`'-'`)으로 덮어버리는 우아한 중력 로직 구현. |

### Day 49 (2026.03.12) - Phase 5 하드코어 샌드박스: 카카오 기출 2연전

면접 직후의 피로도를 이겨내고, 카카오 블라인드 기출의 핵심인 2차원 탐색(BFS)과 완전탐색(DFS 조합 + 해시)을 하루에 2문제 연속으로 격파함. 알고리즘 설계(Architecture) 뼈대를 스스로 세우는 구현력을 증명함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[거리두기 확인하기](https://school.programmers.co.kr/learn/courses/30/lessons/81302)** | **BFS (레이더망 탐색)**. <br> 1. **탐색 조건:** 모든 응시자(`P`) 위치에서 BFS를 시작하되, 맨해튼 거리가 2 이하인 곳까지만 탐색(`dist >= 2` 시 `continue`). <br> 2. **장애물 처리:** 빈 테이블(`O`)은 큐에 넣어 통과시키고, 파티션(`X`)은 무시하여 벽을 구현함. <br> 3. **방문 체크:** 방문 배열(`visited`)을 통해 자기 자신을 위반자로 오인하는 무한 루프 및 역주행을 완벽히 차단함. |
| Lv.2 | **[메뉴 리뉴얼](https://school.programmers.co.kr/learn/courses/30/lessons/72411)** | **DFS (조합) + HashMap**. <br> 1. **중복 방지:** "WX", "XW" 같은 순서가 다른 동일 조합을 막기 위해, 원본 주문 문자열을 미리 알파벳순으로 정렬(`Arrays.sort`). <br> 2. **조합 생성:** DFS 재귀(`dfs(order, current + order.charAt(j), j + 1, courseLength)`)를 이용해 목표 길이만큼의 모든 단품 메뉴 조합을 빠짐없이 생성함. (반복문 조건: `j < order.length()`) <br> 3. **Map.Entry 활용:** `HashMap`에 생성된 조합의 등장 횟수를 누적하고, `entrySet()`을 활용해 $O(N)$의 속도로 최대 빈도 조합(최소 2명 이상)을 깔끔하게 추출함. |

### Day 48 (2026.03.10) - Phase 5 면접 웜업: 문자열 파싱 & 해시맵

면접 전날, 코딩 감각을 유지하기 위한 가벼운 웜업. 자바의 문자열 조작(`substring`)과 `HashMap`을 활용한 실시간 사전(Dictionary) 구현 능력을 점검함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[[3차] 압축 (카카오 블라인드)](https://school.programmers.co.kr/learn/courses/30/lessons/17684)** | **LZW 무손실 압축 알고리즘 (HashMap + Two Pointers)**. <br> 1. **자료구조 선택:** 단어(Key)의 존재 여부를 $O(1)$로 확인하고 고유 번호(Value)를 매핑하기 위해 `HashMap`을 사용함. <br> 2. **Two Pointers 탐색:** 시작점 `i`를 기준으로 끝점 `j`를 늘려가며 `substring(i, j)`로 단어를 자름. <br> 3. **종료 조건:** 자른 단어가 사전에 없을 때(`!dict.containsKey`) 탐색을 멈춤. <br> 4. **로직 분기:** 직전까지의 가장 긴 단어(`j-1`)의 색인 번호를 정답 리스트에 추가하고, 사전에 없던 새 단어(`j`)를 다음 번호로 사전에 등록함. <br> 5. **인덱스 제어:** `for`문의 기본 증감식(`i++`)을 제거하고, `i = j - 1`로 시작점을 직접 점프시켜 중복 탐색을 방지함. |

### Day 47 (2026.03.09) - Phase 5 공채 특화 모드: 탐색 심화 & 자료구조 최적화

단순한 BFS 탐색을 넘어, 대기업 실전 코딩테스트의 핵심 트렌드인 **'탐색(BFS) + 자료구조(Map, Set) 결합을 통한 시간 복잡도 최적화'**를 완벽하게 훈련함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[석유 시추 (PCCP 기출)](https://school.programmers.co.kr/learn/courses/30/lessons/250136)** | **Two-Pass 전략 (BFS + HashMap + HashSet)**. <br> 1. **시간 초과 방지:** 시추관을 찌를 때마다 탐색하면 $O(N^3)$으로 터짐. 미리 조사를 끝내고 나중에 조회만 하는 Two-Pass 설계가 필수적임. <br> 2. **Pass 1 (장부 기록):** 전체 땅을 순회하며 `1`을 만나면 BFS를 돌림. 덩어리 크기(Size)를 구하고, 지나간 땅은 고유한 `ID(2, 3...)`로 덮어씌움. 알아낸 매장량은 `HashMap`에 `[ID: Size]` 형태로 기록. <br> 3. **Pass 2 (시추 및 계산):** 각 열을 수직으로 찌르면서 만나는 땅의 `ID`들을 `HashSet`에 담음. (ㄷ자 모양 덩어리를 두 번 관통할 때 발생하는 중복 합산 방지). 이후 Set에 담긴 고유 ID들만 Map에서 찾아 값을 더해 최댓값을 갱신함. |

### Day 46 (2026.03.03) - Phase 5 공채 특화 모드: 스케줄링 & 모노토닉 스택

공채 코딩테스트에 무조건 출제되는 필수 유형 2가지 집중 훈련. "시간의 흐름에 따른 상태 변화"를 우선순위 큐(PQ)와 스택(Stack)으로 $O(N \log N)$ 및 $O(N)$으로 최적화하는 기법을 마스터함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[호텔 대실](https://school.programmers.co.kr/learn/courses/30/lessons/155651)** | **스케줄링 (Sorting + PriorityQueue)**. <br> 1. **시간 파싱:** `"HH:MM"` 문자열을 `HH * 60 + MM` 형태의 분(Minute) 단위 정수로 변환. <br> 2. **2-Step 정렬:** 먼저 손님들을 **'시작 시간' 기준 오름차순**으로 정렬 (`Arrays.sort`). <br> 3. **PQ 상태 관리:** `PriorityQueue`에는 사용 중인 방의 **'청소 완료 시간(종료 시간 + 10)'**만 저장. `pq.peek() <= 현재 시작 시간` 이면 기존 방을 이어서 쓰고(`poll`), 아니면 새 방을 파서(`offer`) 최종 `pq.size()`로 필요 객실 수를 구함. |
| Lv.2 | **[뒤에 있는 큰 수 찾기](https://school.programmers.co.kr/learn/courses/30/lessons/154539)** | **모노토닉 스택 (Monotonic Stack)**. <br> 1. **O(N) 최적화:** 100만 개의 배열을 이중 for문으로 돌면 시간 초과. 단 한 번의 순회로 해결해야 함. <br> 2. **인덱스 저장:** 스택에 숫자의 '값'이 아닌 **'인덱스(위치)'**를 저장(`stack.push(i)`)하는 것이 핵심. <br> 3. **대기실 로직:** 현재 숫자(`numbers[i]`)가 스택 맨 위 인덱스의 숫자(`numbers[stack.peek()]`)보다 크면, `pop`한 인덱스 위치의 정답을 현재 숫자로 갱신함. |


### Day 45 (2026.03.02) - Phase 5 공채 빈출 모드: 그리디 & 투 포인터

데이터의 크기가 커서(10만~100만) BFS/이중 루프 탐색이 불가능할 때, 직관적인 그리디(양팔 저울) 방식과 투 포인터(Two Pointers)를 활용하여 $O(N)$ 시간 복잡도로 최적화하는 실전 기법을 훈련함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[두 큐 합 같게 만들기](https://school.programmers.co.kr/learn/courses/30/lessons/118667)** | **Queue & Greedy (양팔 저울 알고리즘)**. <br> 1. **탐색의 함정:** 최소 횟수라고 무조건 BFS를 쓰면 길이(300,000) 때문에 터짐. <br> 2. **직관적 최적화:** 두 큐의 합을 비교해 무거운 쪽에서 빼서(`pop`) 가벼운 쪽에 넣는(`insert`) 과정을 반복하는 O(N) 로직 구현. <br> 3. **안전장치:** 누적합 오버플로우 방지를 위한 `long` 타입 사용 및 무한 루프 방지를 위한 한계치(`queue.length * 4`) 설정. |
| Lv.2 | **[연속된 부분 수열의 합](https://school.programmers.co.kr/learn/courses/30/lessons/178870)** | **투 포인터 (Two Pointers)**. <br> 1. **O(N) 최적화:** 길이가 100만인 배열에서 이중 for문은 무조건 시간 초과. `left`, `right` 두 개의 포인터를 벌레처럼 움직여 한 번의 순회로 탐색. <br> 2. **조건 분기:** `sum < k` 이면 `right` 증가(확장), `sum > k` 이면 `left` 증가(축소). <br> 3. **경계값 주의:** `right`를 증가시킬 때 배열의 끝을 넘어가는지 확인하는 `if (right < sequence.length)` 안전장치가 필수적임. |

### Day 44 (2026.02.27) - Phase 5 자립심 훈련: 탐색 심화 & 자료구조 직관

스스로 문제의 요구사항을 분석하여 올바른 자료구조(Stack)를 선택하는 직관력을 기르고, 까다로운 예외 조건과 상태 복구(Backtracking)가 포함된 깊이 우선 탐색(DFS)을 완벽하게 구현함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[양궁대회](https://school.programmers.co.kr/learn/courses/30/lessons/92342)** | **DFS & Backtracking (평행우주 템플릿)**. <br> 1. **이분 탐색적 접근:** 과녁마다 "어피치보다 딱 1발 더 쏜다" vs "0발 쏘고 포기한다" 두 갈래로만 뻗어나가 최적화. <br> 2. **백트래킹:** `ryan[index] = 0`으로 탐색 후 화살을 회수하여 이전 상태로 완벽히 원상 복구함. <br> 3. **안전한 복사:** 배열 참조(주소) 오류를 막기 위해 정답 갱신 시 반드시 `answer = ryan.clone()`을 사용하여 스냅샷을 찍음. |
| Lv.2 | **[택배상자](https://school.programmers.co.kr/learn/courses/30/lessons/131704)** | **Stack (파이프라인 단일화)**. <br> 1. **자료구조 도출:** "가장 마지막에 보관한 상자부터 꺼낸다"는 지문에서 **후입선출(LIFO) Stack**을 정확히 캐치함. <br> 2. **로직 최적화:** 메인 벨트 처리와 보조 벨트 처리를 `if-else`로 나누지 않고, 일단 다가오는 모든 상자를 스택에 무조건 `push` 한 뒤, 스택의 맨 위(`peek()`)를 검사해 `while`문으로 가능한 만큼 `pop` 하는 우아한 파이프라인 로직을 구현함. |

### Day 43 (2026.02.26) - 탐색(BFS)의 유연한 활용과 2차원 DP 심화

단순한 길 찾기를 넘어 다중 목표를 순차적으로 탐색하는 BFS 모듈화 기법과, 이전의 결과를 메모장에 기록하여 재활용하는 2차원 보드판 DP(동적계획법)를 훈련함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[미로 탈출](https://school.programmers.co.kr/learn/courses/30/lessons/159993)** | **2단 콤보 BFS & 상태 캡슐화**. <br> 1. **BFS 모듈화:** 출발지(S) -> 레버(L) -> 출구(E)로 이어지는 다중 목표 탐색을 위해, 타겟 문자만 바꿔 재사용할 수 있도록 BFS 로직을 분리함. <br> 2. **상태 캡슐화:** 큐에 단순히 좌표만 넣지 않고 `x, y, count(이동 시간)`를 묶은 `Node` 클래스를 담아 갈림길에서의 거리 계산 오류를 방지함. <br> 3. **예외 처리:** 중간 기착지(L)로 갈 수 없는 경우(`time1 == -1`), 끝까지 계산하지 않고 조기 종료하는 방어 로직 구현. |
| Lv.2 | **[가장 큰 정사각형 찾기](https://school.programmers.co.kr/learn/courses/30/lessons/12905)** | **2차원 DP (Bottom-Up) & 배열 패딩**. <br> 1. **점화식 도출:** 현재 칸이 1일 때, `Math.min(왼쪽, 위쪽, 왼쪽 대각선 위) + 1`을 통해 자신이 우측 하단 꼭짓점이 되는 가장 큰 정사각형의 길이를 메모이제이션(Memoization) 함. <br> 2. **배열 패딩(Padding):** 보드판의 0번 인덱스 행/열에서 `-1` 조회가 일어나는 `IndexOutOfBounds` 에러를 막기 위해, 원본 배열보다 가로세로 1칸씩 더 큰 `dp[rows+1][cols+1]` 보드판을 깔아두는 실무적 트릭 적용. |

#### 💡 Today I Learned (TIL)
1. **BFS 이동 거리의 함정:** 큐를 돌릴 때 외부 변수(`int count = 0`)를 증가시키면 큐에 쌓인 개수만큼 숫자가 뻥튀기된다. 반드시 현재 노드가 가진 이동 거리에 `+1`을 해서 다음 노드로 넘겨야 한다 (`node.count + 1`).
2. **DP의 본질 (바텀업):** DP는 복잡한 수식이 아니라, "빈 도화지(판)를 쫙 깔아두고, 앞 칸의 결과를 베껴서 내 칸을 채우는" 타뷸레이션(Tabulation) 기법임을 완벽히 체감했다.
3. **도화지 늘리기 (Padding):** 2차원 배열의 경계선(0번 인덱스) 바깥을 탐색해야 할 때는, 조건문(`if`)으로 덕지덕지 막는 것보다 처음부터 배열 크기를 `+1` 해서 `0`으로 채워진 완충 지대를 만드는 것이 훨씬 우아하고 안전한 코딩이다.

### Day 42 (2026.02.25) - 탐색 & DP의 귀환

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[무인도 여행](https://school.programmers.co.kr/learn/courses/30/lessons/154540)** | **BFS (Connected Component) & Stream API**. <br> 1. **2차원 격자 탐색:** `dx, dy` 방향 배열을 활용해 상하좌우를 탐색하며 조건(바다 아님, 방문 안 함)에 맞는 연결된 칸(섬)을 `Queue`를 이용해 싹쓸이함. (주의: BFS 구현 시 재귀 호출 섞지 않기!) <br> 2. **리스트 변환 문법:** `List<Integer>`를 `int[]`로 변환할 때 `list.stream().mapToInt(i -> i).toArray()`를 사용하여 코드를 한 줄로 간결하게 처리. |
| Lv.2 | **[숫자 변환하기](https://school.programmers.co.kr/learn/courses/30/lessons/154538)** | **BFS 최단거리 탐색 vs DP (바텀업)**. <br> 1. **BFS 접근:** `[현재 숫자, 연산 횟수]`를 큐에 넣고 `+n, *2, *3` 세 갈래로 탐색. 1차원 배열 `visited`를 사용해 중복 계산을 방지하고 가장 먼저 `y`에 도달하는 최단거리를 구함. <br> 2. **DP 접근:** `y+1` 크기의 배열을 만들고, `x`부터 `y`까지 순회하며 미래의 위치(`i+n, i*2, i*3`)에 `Math.min(현재 기록, 내 위치 기록 + 1)`로 최솟값을 갱신해 나가는 보드판 채우기 방식. |

### Day 41 (2026.02.24) - 원형 수열 & 완전탐색

슬라이딩 윈도우의 확장판인 원형 수열 처리와, 재귀함수를 이용해 모든 경우의 수를 빠짐없이 만들어내는 완전탐색(DFS) 훈련.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[연속 부분 수열 합의 개수](https://school.programmers.co.kr/learn/courses/30/lessons/131701)** | **원형 수열 (Modulo) & 누적 합 & HashSet**. <br> 1. **원형 인덱스 처리:** 배열 크기를 2배로 늘리지 않고, `(i + j) % length`를 사용하여 배열 끝을 넘어갈 때 0번 인덱스로 자연스럽게 회귀하도록 최적화. <br> 2. **누적 합 & 중복 제거:** 이중 루프(바깥 `i`: 시작점, 안쪽 `j`: 걸음 수)를 돌며 누적 합(`sum`)을 구하고, 매 턴마다 `Set`에 `add`하여 길이에 상관없이 모든 경우의 수와 중복을 한 번에 해결. |
| Lv.2 | **[모음 사전](https://school.programmers.co.kr/learn/courses/30/lessons/84512)** | **DFS (완전탐색)**. <br> 1. **재귀(Recursion) 설계:** 빈 문자열 `""`부터 시작하여 재료(`A, E, I, O, U`)를 순차적으로 붙여가며 재귀 호출. <br> 2. **탈출 조건:** 단어의 길이(`len`)가 5가 되면 즉시 `return`. <br> 3. **List & indexOf:** 생성된 모든 단어를 전역 변수 `List`에 순서대로 담고, `indexOf(word)`를 통해 인덱스를 찾음 (0부터 시작하므로 정답은 `+1` 처리). 제한사항이 작을 때는 직관적인 완전탐색이 가장 안전한 방법임. |
### Day 40 (2026.02.23) - 정렬의 재발견 & 규칙 찾기

배열을 직접 만들거나 이중 루프를 돌리면 터지는 효율성 문제들을 정렬(Sort), 해시(Hash), 그리고 수학적 규칙(Math)으로 우아하게 해결하는 훈련.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[전화번호 목록](https://school.programmers.co.kr/learn/courses/30/lessons/42577)** | **Sort vs Hash 두 가지 풀이법**. <br> 1. **정렬(Sort) 풀이:** 문자열 배열을 `Arrays.sort()` 하면 비슷한 문자열이 인접하게 정렬됨. `phone_book[i+1].startsWith(phone_book[i])`로 바로 뒷사람과만 비교하여 O(N)으로 최적화. <br> 2. **해시(Hash) 풀이:** `HashSet`에 모든 번호를 넣고, 각 번호를 `substring`으로 쪼개가며 `set.contains(조각)`으로 접두어 존재 여부 확인. |
| Lv.2 | **[n^2 배열 자르기](https://school.programmers.co.kr/learn/courses/30/lessons/87390)** | **Math & 규칙 찾기**. <br> 1. **메모리 초과 방지:** 크기가 최대 천만 단위인 배열을 실제로 생성하면 안 됨. <br> 2. **좌표 역추적:** 1차원 인덱스 `i`를 통해 2차원 좌표를 `row = (int)(i / n)`, `col = (int)(i % n)`으로 구함. <br> 3. **값 규칙:** 해당 칸에 들어가는 숫자는 `Math.max(row, col) + 1`. <br> 4. **자료형 주의:** `left`, `right`가 매우 크므로 순회 시 `long i`를 사용해야 하며, 형변환 캐스팅 위치 주의. |

### Day 39 (2026.02.21) - 홈 오피스 코딩: 슬라이딩 윈도우 & 상태 관리

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[할인 행사](https://school.programmers.co.kr/learn/courses/30/lessons/131127)** | **HashMap & 슬라이딩 윈도우(Sliding Window)**. <br> 1. **타겟 세팅:** `want`와 `number` 배열을 묶어 기준이 되는 `mainMap` 생성. <br> 2. **윈도우 이동:** `discount` 배열을 0부터 `길이 - 10`까지 순회하며 10일 치 창문을 이동. <br> 3. **상태 초기화:** 매 반복마다 `subMap`을 새로 생성(`new HashMap<>()`)하여 이전 루프의 데이터 오염 방지. <br> 4. **비교 치트키:** `mainMap.equals(subMap)`을 사용하여 종류와 개수가 완벽히 일치하는지 한 번에 확인. |
| Lv.2 | **[괄호 회전하기](https://school.programmers.co.kr/learn/courses/30/lessons/76502)** | **String(회전) & Stack(올바른 괄호) & 메서드 분리**. <br> 1. **문자열 회전:** `s.substring(1) + s.substring(0, 1)`을 통해 앞글자를 맨 뒤로 보내는 방식 사용. <br> 2. **메서드 분리(Refactoring):** 회전 로직과 괄호 검사 로직을 분리(`isValid` 메서드)하여 코드의 가독성을 높임. <br> 3. **Stack 짝꿍 검사:** 여는 괄호는 `push`, 닫는 괄호는 `peek()`으로 짝꿍(`()`, `{}`, `[]`)이 정확히 맞는지 확인 후 `pop()`. 비어있거나 짝이 다르면 `false` 반환. |

### Day 38 (2026.02.20) - 실무형 자료구조 (Map & LRU)

상태 변화를 추적하고, 제한된 공간에서 데이터를 관리하는 실무 밀접형 로직 훈련.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[오픈채팅방](https://school.programmers.co.kr/learn/courses/30/lessons/42888)** | **HashMap & Two-Pass 전략**. <br> 1. **1차 순회:** `Enter`, `Change` 이벤트 시 `map.put(id, nickname)`으로 최신 닉네임만 덮어쓰기. <br> 2. **2차 순회:** `Enter`, `Leave` 이벤트 시 `map.get(id)`로 최종 닉네임을 불러와 메시지 리스트(`ArrayList`) 생성. <br> 3. `Leave` 시에는 닉네임 배열(`words[2]`)이 없으므로 `IndexOutOfBounds` 주의. |
| Lv.2 | **[[1차] 캐시](https://school.programmers.co.kr/learn/courses/30/lessons/17680)** | **LRU(Least Recently Used) 알고리즘 & List**. <br> 1. **예외 처리:** `cacheSize == 0`일 때는 캐시가 없으므로 무조건 `길이 * 5` 리턴. <br> 2. **Cache Hit:** 리스트에 존재하면 해당 요소를 `remove()`하고 다시 `add()`하여 **맨 뒤(최신)**로 보냄 (+1). <br> 3. **Cache Miss:** 리스트에 없고 꽉 찼다면(`size >= cacheSize`), **맨 앞(index 0, 가장 오래된 것)**을 `remove(0)`하고 새 요소를 맨 뒤에 `add()` (+5). <br> 4. `String` 불변성 주의: `city = city.toUpperCase();` 처럼 변수에 다시 담아줘야 함. |

### Day 37 (2026.02.19) - 두뇌 부팅: Set과 DP(규칙 찾기)

연휴 복귀 후 가벼운 로직 설계 훈련. 중복 제거와 수열 규칙 발견 감각 회복.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[영어 끝말잇기](https://school.programmers.co.kr/learn/courses/30/lessons/12981)** | **HashSet & Math**. <br> 1. **중복 체크:** `Set`에 등장한 단어를 저장하고 `contains()`로 확인. <br> 2. **순서 계산:** 사람 번호는 `(i % n) + 1`, 차례는 `(i / n) + 1`. (인덱스 보정 주의) |
| Lv.2 | **[멀리 뛰기](https://school.programmers.co.kr/learn/courses/30/lessons/12914)** | **DP (피보나치 수열)**. <br> 1. **규칙 발견:** 1칸(1), 2칸(2), 3칸(3), 4칸(5)... -> 앞의 두 수를 더하면 다음 수가 됨. <br> 2. **점화식:** `dp[i] = (dp[i-1] + dp[i-2]) % 1234567`. <br> 3. **초기화 주의:** `n=1`일 때 예외 처리 및 `dp` 배열 크기(`n+1`) 설정 필수. |

### Day 36 (2026.02.12) - 구현력(Implementation) 끝판왕: 파싱과 해시맵

복잡한 요구사항을 코드로 옮기는 '구현력' 집중 훈련. 문자열 파싱과 자료구조(Map) 활용 능력 강화.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[k진수에서 소수 개수 구하기](https://school.programmers.co.kr/learn/courses/30/lessons/92335)** | **String Parsing & Math**. <br> 1. `split("0")`: 0을 기준으로 자르면 복잡한 소수 조건(0P0, P0 등)이 자동 해결됨. <br> 2. `Long.parseLong`: k진수로 변환하면 숫자가 매우 길어지므로 `long` 필수. <br> 3. 소수 판별: `Math.sqrt(n)`까지만 검사하여 효율성 확보 (`num % i == 0`). |
| Lv.2 | **[주차 요금 계산](https://school.programmers.co.kr/learn/courses/30/lessons/92341)** | **Hash Map & Simulation**. <br> 1. **Two Maps Strategy**: `parkingMap`(입차 기록)과 `timeMap`(누적 시간) 분리 관리. <br> 2. **Helper Method**: 시간 문자열("HH:MM")을 분(min)으로 바꾸는 로직을 분리하여 가독성 확보. <br> 3. `Math.ceil`: `(double)` 캐스팅 없이 나누면 소수점이 버려지는 점 주의. |

### Day 35 (2026.02.10) - 구현력 강화: 문자열 파싱과 집합 (샌드박스 훈련 시작)

힌트 없이 스스로 로직을 설계하는 **15분 샌드박스** 훈련 도입. 직관적인 아이디어를 코드로 옮기는 구현력 집중 연습.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[튜플](https://school.programmers.co.kr/learn/courses/30/lessons/64065)** | **String Parsing & Sort**. <br> 1. 집합의 **길이(`s.length()`)** 순서대로 정렬해야 순서를 알 수 있다는 핵심 아이디어 도출. <br> 2. `Set`으로 중복을 체크하며 순서대로 배열에 담기. |
| Lv.2 | **[뉴스 클러스터링](https://school.programmers.co.kr/learn/courses/30/lessons/17677)** | **다중집합(Multi-set) 구현**. <br> 1. 2글자씩 자를 때 인덱스 범위는 `length() - 1`까지. <br> 2. 교집합: `list2.remove(item)`이 `true`면 교집합. <br> 3. 합집합: `(list1 전체) + (list2 남은 것)`으로 최적화 구현. |

### Day 34 (2026.02.09) - BFS & DFS 실전 응용: 흐름을 제어하라

정해진 규칙에 따라 움직이는 시뮬레이션과, 모든 가능성을 열어두는 완전 탐색의 실전 문제 풀이.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[리코쳇 로봇](https://school.programmers.co.kr/learn/courses/30/lessons/169199)** | **BFS + 구현(Simulation)**. <br> 1. 한 칸씩 이동이 아니라, 장애물이나 벽에 부딪힐 때까지 `while`로 미끄러지는 로직 구현. <br> 2. `visited`는 이동 경로가 아닌 **도착(멈춤) 지점**에만 체크해야 함. |
| Lv.2 | **[이모티콘 할인행사](https://school.programmers.co.kr/learn/courses/30/lessons/150368)** | **DFS(Backtracking) + 완전 탐색**. <br> 1. 이모티콘마다 4가지 할인율을 적용하는 모든 경우의 수($4^m$) 탐색. <br> 2. `calculate()` 로직 분리: 장바구니 담기(Loop) -> 예산 비교(Loop 종료 후) -> 최댓값 갱신(전체 종료 후). |

### Day 33 (2026.02.07) - 완전 탐색 & 백트래킹: 컴퓨터를 믿어라

모든 경우의 수를 다 확인하는 완전 탐색(Brute Force)과, 갔던 길을 되돌아오는 백트래킹(Backtracking) 학습.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[피로도](https://school.programmers.co.kr/learn/courses/30/lessons/87946)** | **순열(Permutation) + DFS**. <br> 1. `visited` 배열로 방문 체크. <br> 2. 재귀 호출이 끝나면 `visited = false`로 되돌려(Backtracking) 다른 순서 탐색 가능하게 함. |
| Lv.2 | **[전력망을 둘로 나누기](https://school.programmers.co.kr/learn/courses/30/lessons/86971)** | **완전 탐색 + BFS**. <br> 1. 전선 하나를 끊음 (`map[a][b] = 0`). <br> 2. BFS로 한쪽 네트워크 송전탑 개수(`cnt`) 산출. <br> 3. 전선 복구 (`map[a][b] = 1`) 후 다음 전선으로 이동. |

### Day 32 (2026.02.06) - 그리디 (Greedy): 현재의 최선이 미래의 최선

순간의 선택이 전체 결과에 영향을 미치는 탐욕법(Greedy) 알고리즘 학습. "기준(Criteria)"을 어떻게 세우느냐가 핵심.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[요격 시스템](https://school.programmers.co.kr/learn/courses/30/lessons/181188)** | **구간 스케줄링(Interval Scheduling)** 변형. <br> 1. 미사일의 **끝나는 지점(End)** 기준으로 오름차순 정렬. <br> 2. `start >= lastShot`일 때만 새로 발사하고, 발사 위치는 해당 미사일의 `end`로 설정하여 최대한 겹치게 유도. |
| Lv.2 | **[마법의 엘리베이터](https://school.programmers.co.kr/learn/courses/30/lessons/148653)** | **자릿수 처리 + 조건 분기**. <br> 1. 일의 자리부터 처리하며 0으로 만들기. <br> 2. `digit > 5`: 올림, `digit < 5`: 내림. <br> 3. **`digit == 5`**: 앞자리가 5 이상이면 올림(`>= 5`), 아니면 내림. (연쇄적인 자릿수 올림 효과 고려) |

### Day 31 (2026.02.05) - DP의 정석과 심화 (규칙 찾기)

DP의 가장 기본적인 패턴(누적 합)과 수학적 규칙을 찾아야 하는 심화 유형을 학습함.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.3 | **[정수 삼각형](https://school.programmers.co.kr/learn/courses/30/lessons/43105)** | **2차원 배열 DP (Top-Down)**. <br> 1. 위층의 왼쪽(`j-1`)과 오른쪽(`j`) 중 큰 값을 선택해 내려오며 더함. <br> 2. `dp[i][j] += Math.max(prev[left], prev[right])` 점화식이 핵심. |
| Lv.2 | **[3 x n 타일링](https://school.programmers.co.kr/learn/courses/30/lessons/12902)** | **수학적 점화식 유도**. <br> 1. 홀수는 불가(0). <br> 2. **기본 규칙:** `dp[i-2] * 3` (3가지 모양). <br> 3. **특수 규칙:** `dp[i-4], dp[i-6]...` 각각 `* 2` (쪼개지지 않는 꼬인 모양). <br> 4. `long` 타입과 나머지 연산 필수. |

### Day 30 (2026.02.02) - BFS 심화 (그래프와 상태 변화)

오늘은 **너비 우선 탐색(BFS)**의 끝판왕 문제들을 다뤘음. 그래프가 눈에 보일 때와 보이지 않을 때(논리적 연결)를 모두 해결하는 능력을 기름.

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.3 | **[가장 먼 노드](https://school.programmers.co.kr/learn/courses/30/lessons/49189)** | **그래프 구현 & 거리 계산**. <br> 1. `ArrayList<ArrayList<Integer>>`로 흩어진 간선 정보를 **인접 리스트**로 정리. <br> 2. `dist[next] = dist[cur] + 1` 공식을 사용해 시작점으로부터 퍼져나가는 물결의 거리를 기록함. |
| Lv.3 | **[단어 변환](https://school.programmers.co.kr/learn/courses/30/lessons/43163)** | **암시적 그래프 탐색 (Implicit Graph)**. <br> 1. 연결선이 없으므로 `canChange(s1, s2)` 함수를 만들어 논리적으로 연결 여부 판단. <br> 2. 큐에 `(단어, 단계)` 객체를 `offer`하는 행위가 곧 **"다음 징검다리로 갈아타는 것"**임을 이해함. |

### Day 29 (2026.01.30) - 고급 도구 장착 (다익스트라 & DP)

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[배달](https://school.programmers.co.kr/learn/courses/30/lessons/12978)** | **다익스트라(Dijkstra)** 알고리즘 입문. <br> 1. `dist` 배열을 무한대로 초기화. <br> 2. `PriorityQueue`로 가장 가까운 노드부터 탐색. <br> 3. `nextTime < dist[next]` 일 때만 갱신(Update)하고 큐에 넣는다. |
| Lv.2 | **[2 x n 타일링](https://school.programmers.co.kr/learn/courses/30/lessons/12900)** | **DP (동적계획법)**. <br> 마지막 타일을 놓는 경우의 수(세로 1개 vs 가로 2개)를 생각하면 **피보나치 수열**(`dp[i] = dp[i-1] + dp[i-2]`)이 된다. <br> 숫자가 크므로 `%` 연산 필수. |

### Day 28 (2026.01.28) - 카카오 기출 (문자열 구현 끝판왕)

| 난이도 | 문제 이름 | 핵심 정리 |
|:---:|:---|:---|
| Lv.2 | **[n진수 게임](https://school.programmers.co.kr/learn/courses/30/lessons/17687)** | `Integer.toString(num, n)`으로 진수 변환. <br> 전체 문자열을 `StringBuilder`로 먼저 다 만들고, 인덱스 규칙(`i += m`)으로 추출. |
| Lv.2 | **[파일명 정렬](https://school.programmers.co.kr/learn/courses/30/lessons/17686)** | `Comparator` 커스텀 정렬. <br> 문자열 파싱은 **"인덱스(위치)"**를 먼저 찾고 `substring`으로 자르는 게 정신 건강에 이롭다. |

### Day 27 (2026.01.27) - 문자열 구현력과 완전탐색 설계

복잡한 문자열 요구사항을 꼼꼼하게 구현하거나, 연속된 수의 합을 찾는 로직을 설계함.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[JadenCase 문자열 만들기](https://school.programmers.co.kr/learn/courses/30/lessons/12951)** | [Link](./Day27/JadenCase.java) | `String`, `Flag`, `StringBuilder` <br> 공백 처리가 핵심. `split`보다 한 글자씩 순회하며 `boolean flag`로 다음 글자의 대소문자 여부를 결정하는 것이 안전함. |
| Lv.2 | **[숫자의 표현](https://school.programmers.co.kr/learn/courses/30/lessons/12924)** | [Link](./Day27/NumberExpression.java) | `Brute Force`, `Math` <br> 연속된 자연수의 합. 이중 반복문을 사용하되, 합이 타겟을 넘어가면 즉시 `break`하여 효율성 확보. (투 포인터로 최적화 가능) |

### Day 26 (2026.01.26) - 수학적 규칙과 문자열 파싱

문자열을 숫자로 변환하여 대소 비교를 하거나, 유클리드 호제법을 이용해 GCD/LCM을 구하는 수학적 기초 문제 해결.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[최댓값과 최솟값](https://school.programmers.co.kr/learn/courses/30/lessons/12939)** | [Link](./Day26/MaxMin.java) | `String split`, `Parsing` <br> 공백으로 구분된 문자열을 `split`하여 배열로 만들고, `Integer.parseInt` 후 최소/최대 갱신. |
| Lv.2 | **[N개의 최소공배수](https://school.programmers.co.kr/learn/courses/30/lessons/12953)** | [Link](./Day26/NLCM.java) | `GCD`, `LCM`, `Euclidean Algorithm` <br> 두 수의 최소공배수를 구한 뒤, 그 결과와 다음 수의 최소공배수를 구하는 과정을 반복(누적). |
| Lv.1 | **[최대공약수와 최소공배수](https://school.programmers.co.kr/learn/courses/30/lessons/12940)** | [Link](./Day26/GCDLCM.java) | `Recursion` <br> **유클리드 호제법:** `gcd(b, a % b)` 재귀 호출로 최대공약수를 구하고, `(A * B) / GCD` 공식으로 최소공배수 도출. |

### Day 25 (2026.01.23) - 이진수(Binary)와 API 활용

자바의 내장 함수를 활용해 복잡한 이진수 변환과 비트 계산을 효율적으로 처리함.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[이진 변환 반복하기](https://school.programmers.co.kr/learn/courses/30/lessons/70129)** | [Link](./Day25/BinaryTransformation.java) | `Binary`, `String` <br> `Integer.toBinaryString(len)`: 길이를 이진수 문자열로 변환. <br> `replace("0", "")`로 0 제거 후 길이 차이 계산. |
| Lv.2 | **[다음 큰 숫자](https://school.programmers.co.kr/learn/courses/30/lessons/12911)** | [Link](./Day25/NextBigNumber.java) | `Bit Count`, `Brute Force` <br> **`Integer.bitCount(n)`**: 이진수에서 1의 개수를 반환하는 치트키 함수. <br> 조건을 만족할 때까지 1씩 증가시키며 탐색. |

### Day 24 (2026.01.22) - 문자열 처리 & 좌표 구현

자료구조보다는 직관적인 논리와 구현력이 중요한 문제들. '불순물 제거'와 '발자국 기록' 패턴 학습.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[스킬트리](https://school.programmers.co.kr/learn/courses/30/lessons/49993)** | [Link](./Day24/SkillTree.java) | `String`, `indexOf` <br> 선행 스킬 외의 글자는 모두 제거(Filtering)한 뒤, 남은 문자열이 족보의 0번 인덱스부터 시작하는지 확인. |
| Lv.2 | **[방문 길이](https://school.programmers.co.kr/learn/courses/30/lessons/49994)** | [Link](./Day24/VisitingLength.java) | `Implementation`, `HashSet` <br> 좌표 이동 시 **"A->B"**와 **"B->A"**를 모두 Set에 저장하여 중복된 길(Path) 방문을 방지. |

### Day 23 (2026.01.21) - 힙(Heap) & 시뮬레이션

우선순위 큐를 활용해 '가장 큰 위기'를 막거나 '가장 효율적인 작업'을 선택하는 복합 시뮬레이션 구현.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[디펜스 게임](https://school.programmers.co.kr/learn/courses/30/lessons/142085)** | [Link](./Day23/DefenseGame.java) | `PriorityQueue(Max)`, `Greedy` <br> 일단 막고 나중에 무적권을 사용하는 **'사후 조치'** 전략. 지나간 라운드 중 최댓값을 복구함. |
| Lv.3 | **[디스크 컨트롤러](https://school.programmers.co.kr/learn/courses/30/lessons/42627)** | [Link](./Day23/DiskController.java) | `PriorityQueue(Min)`, `Simulation` <br> **SJF(Shortest Job First)** 스케줄링. 요청 시간순 정렬과 소요 시간순 정렬(Heap)을 조합해 평균 대기 시간을 최소화. |

### Day 22 (2026.01.20) - 힙 (Heap) & 우선순위 큐

데이터가 추가될 때마다 자동으로 정렬을 유지하며, 최댓값이나 최솟값을 $O(\log N)$의 속도로 효율적으로 찾아내는 알고리즘.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.3 | **[야근 지수](https://school.programmers.co.kr/learn/courses/30/lessons/12927)** | [Link](./Day22/OvertimeIndex.java) | `PriorityQueue(Max)`, `Greedy` <br> 제곱의 합을 최소로 만들기 위해 **가장 큰 값**부터 줄임. `reverseOrder()`로 최대 힙 구현. |
| Lv.3 | **[이중우선순위큐](https://school.programmers.co.kr/learn/courses/30/lessons/42628)** | [Link](./Day22/DualPriorityQueue.java) | `Dual PQ`, `remove()` <br> 최댓값/최솟값용 큐를 2개 운영. 한쪽 삭제 시 `remove`로 반대쪽 동기화하는 것이 핵심. |



#### 💡 Today I Learned (TIL)
1. **PriorityQueue의 방향성:** 자바의 기본 `PriorityQueue`는 최소 힙(Min Heap)이다. 최대 힙이 필요하면 `Collections.reverseOrder()`를 반드시 명시해야 한다.
2. **peek()와 poll()의 용도:** 데이터를 실제로 삭제하며 가져올 때는 `poll()`, 삭제하지 않고 값만 확인할 때는 `peek()`를 사용해 데이터 파괴를 방지한다.
3. **쌍권총 전략(Dual PQ):** 하나의 자료구조로 양쪽 끝(최대/최소)을 제어하기 힘들 때, 두 개의 큐를 동기화하여 관리하는 설계 패턴을 익혔다.

### Day 21 (2026.01.19) - 해시 & 그리디 (Hash & Greedy)

`HashMap`을 이용해 데이터의 빈도수를 세고, **종류(Key)**와 **개수(Value)**를 상황에 맞게 비교하는 훈련.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[귤 고르기](https://school.programmers.co.kr/learn/courses/30/lessons/138476)** | [Link](./Day21/Tangerine.java) | `HashMap`, `Sort` <br> 크기별 개수를 세고(`Map`), 개수가 많은 순(`Value 정렬`)으로 담아야 종류가 최소가 됨. |
| Lv.2 | **[롤케이크 자르기](https://school.programmers.co.kr/learn/courses/30/lessons/132265)** | [Link](./Day21/Rollcake.java) | `Map vs Set` <br> 전체(`Map`)에서 하나씩 빼서 상대(`Set`)에게 줌. **0개가 된 Key를 `remove`**하여 `size()`(종류 수)를 비교하는 것이 핵심. |

### Day 20 (2026.01.16) - 동적계획법 (Dynamic Programming)

큰 문제를 작은 문제로 쪼개어 해결하고, 그 결과를 저장(**Memoization**)하여 중복 계산을 피하는 효율적인 알고리즘.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.3 | **[정수 삼각형](https://school.programmers.co.kr/learn/courses/30/lessons/43105)** | [Link](./Day20/IntegerTriangle.java) | `DP`, `Math.max` <br> 위에서부터 내려오며 자신의 **왼쪽/오른쪽 부모 중 더 큰 값**을 선택해 누적 합을 구함. |
| Lv.2 | **[땅따먹기](https://school.programmers.co.kr/learn/courses/30/lessons/12913)** | [Link](./Day20/Hopscotch.java) | `DP`, `Math.max` <br> **이전 행에서 같은 열을 제외**한 나머지 열 중 최댓값을 더해나가는 방식. `i=1`부터 시작하는 것이 핵심. |

### Day 19 (2026.01.15) - 이분 탐색 (Binary Search)

정답의 범위를 좁혀가며 최적의 해를 찾는 **Up & Down 게임** 방식의 알고리즘.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.3 | **[입국심사](https://school.programmers.co.kr/learn/courses/30/lessons/43238)** | [Link](./Day19/Immigration.java) | `Binary Search`, `Long` <br> 시간을 기준으로 **Up/Down**을 수행. 특정 시간(`mid`) 내에 처리가능한 인원수를 계산하여 범위를 좁힘. |
| Lv.3 | **[징검다리 건너기](https://school.programmers.co.kr/learn/courses/30/lessons/64062)** | [Link](./Day19/SteppingStone.java) | `Parametric Search` <br> 인원수를 기준으로 **Up/Down** 수행. `mid`명이 건널 때 연속된 0인 돌(`skip`)이 `k` 이상이면 불가능 판단. |
### Day 18 (2026.01.14) - 수학 & 구현 (Math & Logic)

복잡한 자료구조 없이, **수학적 규칙(짝수/홀수, 2로 나누기)**을 찾아내어 효율적으로 계산하는 사고력을 기름.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[예상 대진표](https://school.programmers.co.kr/learn/courses/30/lessons/12985)** | [Link](./Day18/ExpectedBracket.java) | `Math` <br> 토너먼트에서 다음 라운드 번호는 **`(n + 1) / 2`** 가 된다는 규칙을 발견하여 반복문으로 해결. |
| Lv.2 | **[점프와 순간 이동](https://school.programmers.co.kr/learn/courses/30/lessons/12980)** | [Link](./Day18/JumpTeleport.java) | `Top-down`, `Modulo(%)` <br> 0 -> N으로 가는 것보다 **N -> 0으로 거꾸로 가는 것**이 훨씬 쉬움. 짝수면 `/2`(순간이동), 홀수면 `-1`(점프). |

### Day 17 (2026.01.13) - 스택/큐 심화 (Simulation & Time)

단순한 데이터 저장을 넘어, **시간의 흐름**을 큐(Queue)로 표현하거나 **과거의 시점**을 스택(Stack)으로 추적하는 심화 기법을 익혔다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[다리를 지나는 트럭](https://school.programmers.co.kr/learn/courses/30/lessons/42583)** | [Link](./Day17/TruckBridge.java) | `Queue`, `Simulation` <br> 다리 위를 큐로 구현. 빈 공간을 **0(공기 트럭)**으로 채워 시간 흐름을 유지하고 무게를 관리함. |
| Lv.2 | **[주식가격](https://school.programmers.co.kr/learn/courses/30/lessons/42584)** | [Link](./Day17/StockPrice.java) | `Stack` <br> 가격이 아닌 **시점(Index)**을 스택에 저장. 가격이 떨어지는 순간 `pop()`하여 **`현재 - 과거`**로 버틴 시간을 계산. |

### Day 16 (2026.01.07) - 탐욕법 (Greedy)

미래를 고려하지 않고, 현재 시점에서 가장 이득이 되는 선택을 반복하여 최적의 해를 찾는 알고리즘.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[구명보트](https://school.programmers.co.kr/learn/courses/30/lessons/42885)** | [Link](./Day16/Lifeboat.java) | `Two Pointers`, `Sorting` <br> 가장 무거운 사람과 가벼운 사람을 짝짓는 것이 이득. 정렬 후 양 끝에서 조여오는 방식 사용. |
| Lv.2 | **[큰 수 만들기](https://school.programmers.co.kr/learn/courses/30/lessons/42883)** | [Link](./Day16/BigNumber.java) | `Greedy`, `StringBuilder` <br> 앞자리에 큰 수가 와야 전체가 커짐. **"내 앞의 숫자가 나보다 작으면 지운다"**는 규칙을 반복 적용. |

### Day 15 (2026.01.06) - 완전탐색 심화 (Brute Force)

단순 반복문을 넘어, 수학적 공식을 세우거나 재귀함수(Recursion)를 이용해 모든 경우의 수를 만드는 심화 기법을 익혔다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[카펫](https://school.programmers.co.kr/learn/courses/30/lessons/42842)** | [Link](./Day15/Carpet.java) | `Math Rule` <br> 전체 넓이의 약수를 구하고, **`(가로-2) * (세로-2) == yellow`** 공식을 통해 테두리와 알맹이의 관계를 검증함. |
| Lv.2 | **[소수 찾기](https://school.programmers.co.kr/learn/courses/30/lessons/42839)** | [Link](./Day15/PrimeNumber.java) | `Recursion`, `HashSet` <br> 종이 조각을 붙여 숫자를 만드는 순열(Permutation) 구현. **`substring`**으로 사용한 카드를 제외하며 재귀 호출하는 로직이 핵심. |

### Day 14 (2026.01.05) - 완전탐색 (Brute Force)

컴퓨터의 빠른 연산 능력을 이용해 모든 경우의 수를 확인하는 **완전탐색**의 기초를 다졌다. 요령을 피우기보다 **규칙을 찾아 반복문으로 해결**하는 것이 핵심이다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.1 | **[모의고사](https://school.programmers.co.kr/learn/courses/30/lessons/42840)** | [Link](./Day14/MockExam.java) | `Modulo(%)` <br> 반복되는 패턴(수포자의 찍기)을 처리할 때 **나머지 연산(`%`)**으로 인덱스를 무한 순환시키는 테크닉. |
| Lv.1 | **[최소직사각형](https://school.programmers.co.kr/learn/courses/30/lessons/86491)** | [Link](./Day14/MinRectangle.java) | `Idea`, `Math.max/min` <br> 명함을 회전시켜 **"큰 쪽은 가로, 작은 쪽은 세로"**로 몰아넣는 규칙을 발견하는 것이 핵심. 굳이 배열을 새로 만들 필요 없이 `max` 변수 갱신만으로 해결 가능. |

### Day 13 (2026.01.02) - 정렬 심화 (Custom Sort)

단순한 오름차순/내림차순을 넘어, **람다식(Lambda)**을 활용해 나만의 정렬 기준을 만드는 법을 익혔다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[가장 큰 수](https://school.programmers.co.kr/learn/courses/30/lessons/42746)** | [Link](./Day14/BiggestNumber.java) | `Lambda`, `compareTo` <br> 숫자 크기가 아닌 **문자열 결합 순서**가 핵심. `(o2+o1).compareTo(o1+o2)`로 붙였을 때 더 큰 쪽을 우선순위로 둠. |
| Lv.1 | **[문자열 내 마음대로 정렬하기](https://school.programmers.co.kr/learn/courses/30/lessons/12915)** | [Link](./Day14/StringCustomSort.java) | `Custom Sort` <br> 정렬 조건이 여러 개일 때(n번째 글자 -> 단어 전체) `if-else` 로직을 람다식 내부에 구현함. |

### Day 12 (2025.12.30) - 정렬 (Sort)

가장 기본이 되는 알고리즘인 정렬을 학습했다. 자바의 내장 라이브러리를 활용하면 복잡한 구현 없이도 쉽게 문제를 해결할 수 있다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.1 | **[K번째수](https://school.programmers.co.kr/learn/courses/30/lessons/42748)** | [Link](./Day13/KthNumber.java) | `Sort`, `Arrays.copyOfRange` <br> 배열을 자르고(`copyOfRange`) 정렬(`sort`)하는 과정을 익힘. 인덱스가 0부터 시작하는 점만 주의하면 된다. |

### Day 11 (2025.12.29) - DFS vs BFS 실전 비교

같은 문제를 두 가지 방식으로 모두 풀어보며 차이점을 명확히 정리했다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.3 | **[네트워크](https://school.programmers.co.kr/learn/courses/30/lessons/43162)** | [DFS](./Day12/Network_dfs.java) <br> [BFS](./Day12/Network_bfs.java) | `DFS/BFS`, `Connected Component` <br> 연결된 덩어리(네트워크)의 개수를 세는 문제. **방문 체크(`visited`)**가 핵심이며, 재귀(DFS)와 큐(BFS) 구현 방식을 비교 학습함. |

### Day 10 (2025.12.26) - 너비 우선 탐색 (BFS)

최단 거리 문제의 치트키, **BFS(Breadth-First Search)**를 학습했다. "가까운 곳부터 모두 방문하고 나아가는" 물결 같은 탐색 방식을 익혔다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[게임 맵 최단거리](https://school.programmers.co.kr/learn/courses/30/lessons/1844)** | [Link](./Day10/GameMap.java) | `BFS`, `Queue` <br> 최단 경로는 **BFS**가 유리하다. `Queue`를 이용해 방문 순서를 관리하고, 맵에 직접 거리를 누적(`+1`)시키는 테크닉을 사용했다. |

#### 💡 Today I Learned (TIL)
1. **BFS의 핵심은 Queue:** 먼저 발견한 길을 먼저 탐색해야 너비(가로)로 퍼질 수 있다. 그래서 선입선출(FIFO)인 큐를 사용한다.
2. **거리 누적 테크닉:** `maps[next] = maps[current] + 1` 공식을 사용하면, 별도의 변수 없이 맵 자체에 이동 횟수를 기록할 수 있다.
3. **한글 설계의 중요성:** 코드를 바로 치기보다 "큐 생성 -> 시작점 등록 -> 무한반복 -> 4방향 탐색" 순서로 한글 주석을 먼저 적는 것이 구현에 훨씬 도움이 된다.

### Day 9 (2025.12.24) - 깊이 우선 탐색 (DFS)

탐색 알고리즘의 핵심인 **DFS(Depth-First Search)**를 학습했다. "한 우물만 끝까지 판다"는 개념을 익히고, 이를 구현하는 **재귀(Recursion)** 패턴을 연습했다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[타겟 넘버](https://school.programmers.co.kr/learn/courses/30/lessons/43165)** | [Link](./Day9/TargetNumber.java) | `DFS(Recursion)` <br> 더하거나(+), 빼거나(-) 두 갈래 길로 재귀 호출. 끝까지 갔을 때(`index == length`) 타겟 확인. |
| Lv.2 | **[피로도](https://school.programmers.co.kr/learn/courses/30/lessons/87946)** | [Link](./Day9/Fatigue.java) | `DFS` + `Backtracking` <br> 순열(Permutation)처럼 순서가 중요할 땐 `visited` 배열 사용. 들어갈 때 `true`, 나올 때 `false`가 핵심. |

#### 💡 Today I Learned (TIL)
1. **DFS의 공식:** DFS는 **재귀함수(Recursion)**다. "나와 똑같은 분신을 다음 단계로 보낸다"고 생각하자.
2. **탈출 조건 (Base Case):** 재귀는 영원히 돌면 안 된다. 반드시 `if (index == 끝)` 같은 종료 조건이 맨 위에 있어야 한다.
3. **백트래킹 (Backtracking):** `visited` 체크를 했다가, 재귀가 끝나고 돌아나올 때 다시 풀어주는(`false`) 기술. 그래야 다른 순서의 길도 탐색할 수 있다.

### Day 8 (2025.12.23) - 완전 탐색 (Brute Force)

"컴퓨터의 빠른 계산 능력을 믿자." 모든 경우의 수를 확인하여 정답을 찾는 **완전 탐색** 기법을 연습했다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.1 | **[모의고사](https://school.programmers.co.kr/learn/courses/30/lessons/42840)** | [Link](./Day8/MockExam.java) | `Modulo(%)` <br> `%` 연산자로 반복되는 패턴 구현. `ArrayList`를 활용해 동적 결과 담기. |
| Lv.2 | **[카펫](https://school.programmers.co.kr/learn/courses/30/lessons/42842)** | [Link](./Day8/Carpet.java) | `Math Rule` <br> 약수(`width * height`) 탐색. 테두리 제외 공식 `(w-2)*(h-2) == yellow` 검증. |

#### 💡 Today I Learned (TIL)
1. **나머지 연산자(%):** 배열의 인덱스를 순환시킬 때(`i % length`) 가장 강력한 도구다.
2. **ArrayList vs Array:** 결과의 개수를 모를 땐 `ArrayList`에 담고, 마지막에 `stream`이나 `loop`로 `int[]` 변환을 한다.
3. **완전 탐색의 기본:** 복잡한 알고리즘을 고민하기 전에, **"가능한 범위를 루프로 다 돌려볼 수 있나?"** 먼저 생각하자. (컴퓨터는 생각보다 빠르다.)

### Day 7 (2025.12.22) - 우선순위 큐 & 클래스 활용

오늘은 큐의 심화 버전인 **PriorityQueue**와, 데이터를 객체로 묶어 관리하는 **Class 활용법**을 익혔다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[더 맵게](https://school.programmers.co.kr/learn/courses/30/lessons/42626)** | [Link](./Day7/MoreSpicy.java) | `PriorityQueue` <br> 자동 정렬(최소 힙) 특성 활용. `peek()`로 조건 확인 후 `poll()` 2번 수행. |
| Lv.2 | **[프로세스](https://school.programmers.co.kr/learn/courses/30/lessons/42587)** | [Link](./Day7/ProcessQueue.java) | `Queue` + `Custom Class` <br> 단순 `int`가 아니라 `Document` 객체를 만들어 인덱스와 값을 함께 관리함. |

#### 💡 Today I Learned (TIL)
1. **PriorityQueue의 위력:** `offer`할 때는 순서가 상관없지만, `poll`할 때는 무조건 **가장 작은 값**이 나온다. (최소 힙 구조)
2. **객체 활용 (Relationship):** 큐에 단순히 숫자만 넣으면 위치 정보를 잃어버린다. `class Document { int idx; int priority; }` 처럼 객체로 포장해서 넣어야 상태를 기억할 수 있다.
3. **로직 설계의 디테일:** `boolean flag`를 사용해 상태(나보다 센 놈이 있는가?)를 체크하고, `return`의 위치를 정확히 잡아야 로직이 꼬이지 않는다.

### Day 6 (2025.12.18) - 스택/큐 심화 (Stack & Queue Deep Dive)

이전에 풀었던 문제들을 다시 점검하며, `Stack` 클래스 대신 `ArrayDeque`를 적용하고 로직의 오류(Concurrent Modification)를 개선하는 데 집중했다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[올바른 괄호](https://school.programmers.co.kr/learn/courses/30/lessons/12909)** | [Link](./Day6/Programmers_12909_올바른괄호.java) | `ArrayDeque` <br> `Integer` 인덱스가 아닌 `Character` 값을 담아 직관성 확보. 불필요한 `push` 방지. |
| Lv.2 | **[기능개발](https://school.programmers.co.kr/learn/courses/30/lessons/42586)** | [Link](./Day6/Programmers_42586_기능개발.java) | `ArrayList` + `Greedy` <br> 큐의 크기가 변하는 루프(`poll`)의 위험성 해결. 기준값(`maxDay`) 갱신 로직. |

#### 💡 Today I Learned (TIL)
1. **자료형의 중요성:** 스택에 인덱스(int)를 넣고 문자(char)와 비교하는 실수를 주의하자. (제네릭 타입 확인 필수)
2. **동적 배열 루프의 함정:** `for (i < list.size())` 내부에서 `remove`나 `poll`을 수행하면 전체 크기가 줄어들어 루프가 조기에 종료되거나 인덱스가 꼬인다.
3. **로직의 단순화:** 무조건 큐를 고집하기보다, `Greedy`하게 기준값(max)을 갱신하며 리스트에 담는 것이 더 효율적일 때가 있다.

### Day 5 (2025.12.17) - 해시 (Hash) 복습 & 재활

오랜만의 알고리즘 복습. Map을 활용해 조회 속도를 높이고, 인덱스를 기억하는 기술을 익혔다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.1 | **[달리기 경주](https://school.programmers.co.kr/learn/courses/30/lessons/178871)** | [Link](./Day5/RunningRace.java) | `HashMap` + `Array` Swap <br> 이름으로 등수 찾기(Map) + 등수로 이름 찾기(Array) 동기화. |
| Lv.1 | **[가장 가까운 같은 글자](https://school.programmers.co.kr/learn/courses/30/lessons/142086)** | [Link](./Day5/NearestLetter.java) | `Map Update` <br> Map은 값을 덮어쓸 수 있다. 최신 인덱스를 계속 갱신(`put`)하며 거리 계산. |

#### 💡 Today I Learned (TIL)
1. **Map의 이중성:** `put`은 데이터를 넣을 때도 쓰지만, **기존 값을 최신 값으로 업데이트**할 때도 쓴다.
2. **거리 계산:** 거리는 항상 `현재 인덱스 - 과거 인덱스`다.
3. **Map에 무엇을 담을까:** 계산된 결과(거리)를 담는 게 아니라, 계산에 필요한 **원본 데이터(인덱스)**를 담아야 한다.

### Day 4 (2025.12.11) - 해시 (Hash)

오늘은 데이터를 **Key-Value** 쌍으로 저장하여 검색 속도를 O(1)로 최적화하는 **해시(Hash)** 알고리즘을 집중 공략했다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.1 | **[완주하지 못한 선수](https://school.programmers.co.kr/learn/courses/30/lessons/42576)** | [Link](./Day4/DidNotFinish.java) | `HashMap` <br> 참가자(+1), 완주자(-1) 카운팅. 0이 아닌 사람 찾기. |
| Lv.1 | **[추억 점수](https://school.programmers.co.kr/learn/courses/30/lessons/176963)** | [Link](./Day4/MemoryScore.java) | `Map Lookup` <br> 이름-점수 매핑. 이중 for문에서 `map.get`으로 점수 합산. |
| Lv.2 | **[의상](https://school.programmers.co.kr/learn/courses/30/lessons/42578)** | [Link](./Day4/Clothes.java) | `Map.values()` <br> 종류별 개수 세기. `(A+1)*(B+1)...-1` 공식 적용. |
| Lv.1 | **[성격 유형 검사하기](https://school.programmers.co.kr/learn/courses/30/lessons/118666)** | [Link](./Day4/PersonalityTest.java) | `Kakao Tech` <br> 선택지에 따른 점수 배분(비동의/동의). 지표별 점수 비교 및 사전순 정렬. |

#### 💡 Today I Learned (TIL)
1. **빈도수 세기 공식:** `map.put(key, map.getOrDefault(key, 0) + 1);` 은 해시 문제의 알파이자 오메가다.
2. **Key가 필요 없을 때:** 맵의 모든 값을 계산해야 할 때는 `map.values()`를 사용하면 편하다.
3. **Map을 쓰는 이유:** 배열은 인덱스(숫자)로만 찾을 수 있지만, Map은 이름(String) 등 어떤 키로든 O(1)로 찾을 수 있다.

---

### Day 3 (2025.12.10) - 정렬 (Sorting)

오늘은 데이터를 순서대로 나열하여 효율적으로 처리하는 **정렬(Sorting)** 알고리즘을 연습했다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.1 | **[K번째수](https://school.programmers.co.kr/learn/courses/30/lessons/42748)** | [Link](./Day3/KthNumber.java) | `Arrays.copyOfRange`, `Arrays.sort` <br> 자바 인덱스(0부터)와 사람의 순서(1부터) 차이 주의 (-1 필수). |
| Lv.1 | **[예산](https://school.programmers.co.kr/learn/courses/30/lessons/12982)** | [Link](./Day3/Budget.java) | `Greedy` <br> 최대한 많은 팀을 지원하려면 **작은 금액**부터 줘야 함 -> 오름차순 정렬. |
| Lv.1 | **[과일 장수](https://school.programmers.co.kr/learn/courses/30/lessons/135808)** | [Link](./Day3/FruitSeller.java) | `Reverse Indexing` <br> 오름차순 정렬 후, **뒤에서부터** m개씩 끊어서 최저점을 찾아야 이득. |
| Lv.2 | **[H-Index](https://school.programmers.co.kr/learn/courses/30/lessons/42747)** | [Link](./Day3/HIndex.java) | `Sort`, `Comparison` <br> 정렬 후 `인용 횟수(값)`와 `남은 논문 수(길이-i)`가 만나는 균형점 찾기. |

#### 💡 Today I Learned (TIL)
1. **정렬의 힘:** 단순히 줄 세우는 것뿐만 아니라, **Greedy(욕심쟁이)** 전략의 기본이 된다. (작은 것부터 vs 큰 것부터)
2. **인덱스 함정:** 문제에서 "k번째"라고 하면 코드는 무조건 `k-1`이다.
3. **H-Index의 교훈:** 문제는 어려워 보여도 정렬하면 `for`문 하나로 풀린다. **"내 점수 >= 남은 개수"** 공식 기억하기.

---

## 📅 Daily Log

### Day 2 (2025.12.09) - 스택 & 큐 (Stack & Queue)

오늘은 **LIFO(스택)**와 **FIFO(큐)**의 동작 원리를 이해하고, `Deque`와 `LinkedList`를 활용해 순서가 중요한 문제들을 해결했다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.2 | **[올바른 괄호](https://school.programmers.co.kr/learn/courses/30/lessons/12909)** | [Link](./Day2/CorrectParentheses.java) | `Stack(Deque)` <br> 여는 괄호 `push`, 닫는 괄호 `pop`. 빈 스택 체크(`isEmpty`)가 핵심. |
| Lv.2 | **[기능개발](https://school.programmers.co.kr/learn/courses/30/lessons/42586)** | [Link](./Day2/FunctionDevelopment.java) | `Queue` <br> `Math.ceil`로 날짜 계산. 앞사람(반장)보다 늦게 끝나면 대기, 빠르면 같이 배포. |
| Lv.2 | **[짝지어 제거하기](https://school.programmers.co.kr/learn/courses/30/lessons/12973)** | [Link](./Day2/PairRemoval.java) | `Stack` <br> `String` 대신 `Character` 사용(성능). 연속된 같은 글자 터트리기(테트리스). |
| Lv.1 | **[크레인 인형뽑기 게임](https://school.programmers.co.kr/learn/courses/30/lessons/64061)** | [Link](./Day2/CraneGame.java) | `Stack`, `2D Array` <br> `moves` 순서대로 열 탐색. 뽑은 인형을 스택에 넣고 비교/삭제. |

#### 💡 Today I Learned (TIL)
1. **Stack 클래스는 이제 그만:** 옛날 `Stack` 클래스 대신 `Deque<Integer> stack = new ArrayDeque<>();`를 쓰는 것이 더 빠르고 효율적이다.
2. **나눗셈의 함정:** 자바 정수 나눗셈(`7/3=2`)은 소수점을 버린다. 올림(`ceil`)을 하려면 `(double)`로 캐스팅이 필수다.
3. **Queue 활용:** `Queue`는 인터페이스이므로 `LinkedList`로 구현하여 `offer`(넣기), `poll`(빼기)을 사용한다.
4. **로직 분리:** 크레인 게임처럼 복잡할 땐 '탐색(moves)'과 '처리(stack)' 로직을 명확히 분리해야 실수가 적다.

---

### Day 1 (2025.12.08) - 자료구조 기초 (List, Set)

오늘은 **ArrayList**와 **HashSet**의 차이점을 이해하고, 상황에 맞게 골라 쓰는 연습을 했다.

| 난이도 | 문제 이름 | 풀이 코드 | 키워드 / 핵심 정리 |
|:---:|:---|:---:|:---|
| Lv.1 | **[같은 숫자는 싫어](https://school.programmers.co.kr/learn/courses/30/lessons/12906)** | [Link](./Day1/HateSameNumber.java) | `ArrayList` <br> `contains()`는 O(n)이라 느림. 직전 값(`get(size-1)`)과 비교하여 O(1)로 처리. |
| Lv.1 | **[폰켓몬](https://school.programmers.co.kr/learn/courses/30/lessons/1845)** | [Link](./Day1/Ponketmon.java) | `HashSet` <br> 중복 제거가 핵심. `Math.min(max, typeCount)` 로직 활용. |
| Lv.1 | **[제일 작은 수 제거하기](https://school.programmers.co.kr/learn/courses/30/lessons/12935)** | [Link](./Day1/RemoveSmallestNumber.java) | `Collections.min()` <br> `remove(int index)`와 `remove(Object value)` 혼동 주의. |
| Lv.1 | **[두 개 뽑아서 더하기](https://school.programmers.co.kr/learn/courses/30/lessons/68644)** | [Link](./Day1/PickTwoAndAdd.java) | `HashSet` → `List` <br> 이중 for문(`j=i+1`), `Set`으로 중복 제거 후 `List.sort()` 정렬. |

#### 💡 Today I Learned (TIL)
1. **ArrayList 제거의 함정:** 숫자를 제거할 때 `remove(10)`을 하면 10번째 인덱스가 지워짐. 값을 지우려면 `remove(list.indexOf(10))` 처럼 인덱스를 찾아야 함.
2. **Set의 위력:** 중복을 제거해야 할 때는 고민하지 말고 `HashSet`을 쓰자.
3. **배열 반환 패턴:** `ArrayList`로 로직을 수행하고 마지막에 `new int[list.size()]`로 변환해서 리턴하는 패턴에 익숙해짐.