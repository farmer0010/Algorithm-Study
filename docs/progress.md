# Progress — Algorithm-Study

## 코딩테스트 진행 상태
- 현재 Day: 140 (완료 — 키패드 누르기, 이상한 문자 만들기, 문자열 다루기 기본 3문제 전부 완주. Lv.1 전용 세트)

## 최근 5일 요약 (문제 선정 시 중복 회피용)
- Day 136: 가사 검색(Lv.4, 카카오 2020) 신규 도입 — Trie(정/역방향 이중 구조), 왕복 15회 이상.
- Day 137: Lv.2~3 복습 + Map API 문법 정리 — A로 B 만들기, 순위 검색(블라인드 재도전, Map API 실수 0회 확인).
- Day 138: SKT 코테(8/31 토) 대비로 급전환 — 시저 암호(Lv.1, 왕복 3회), 프렌즈4블록(Lv.2, 블라인드 재도전, 왕복 11회 이상, 시뮬레이션+중력), 짝지어 제거하기(Lv.2, 블라인드 재도전, 왕복 2회, 스택). 반복문 증감 방향 실수 2회 반복 발생, 반면 변수 스코프 이해와 "스택의 이중 반전" 개념은 스스로 도출하는 긍정 신호 확인.
- Day 139: SKT 코테 종료 후 재개, Lv.4 잠정 보류·Lv.1~3 집중 모드로 전환. 숫자 문자열과 영단어(Lv.1, 신규, 왕복 3회) — Map 기반 파싱, 영단어 매칭 타이밍 스스로 설계. 다리를 지나는 트럭(Lv.2, 블라인드 재도전, 왕복 10회 이상) — 큐 슬롯 고정+0패딩 트릭은 잘 구현했으나, 종료 조건(큐 isEmpty → 다른 변수 기반으로 전환) 설계에서 여러 차례 시행착오 후 스스로 해결. 사라지는 발판(Lv.3)은 다음 세션으로 이월.
- **Day 140 (신규):** 사라지는 발판(Lv.3)은 정답률이 낮아 사용자 요청으로 보류, Lv.1 3문제 세트로 재구성. 키패드 누르기(Lv.1, 신규, 왕복 2회) — 격자 좌표 맨해튼 거리 계산 신규 도입, BFS 대신 공식 사용을 스스로 판단. 이상한 문자 만들기(Lv.1, 신규, 왕복 2회) — split(" ")의 trailing 공백 손실 함정을 실패 케이스 분석으로 스스로 해결. 문자열 다루기 기본(Lv.1, 신규, 왕복 0회) — 조건 검사 순서 최적화를 스스로 설계.

## 예고된 다음 문제
- **사라지는 발판(Lv.3) — 정답률(18%)이 낮아 사용자 요청으로 보류. "다음 세션 1번 배치" 계획은 취소, 추후 컨디션 좋을 때 재배치 예정.**
- **징검다리(Lv.4) — Lv.1~3 마스터리(사용자 판단)가 확인되기 전까지 잠정 보류. (변경 없음)**
- 4장 문자열 챕터 잔여 후보: 3진법 뒤집기, 문자열 내 p와 y의 개수, 자연수 뒤집어 배열로 만들기 (이상한 문자 만들기·문자열 다루기 기본은 Day140에 완료).
- 12장 구현 챕터 신규 확인 후보: 체육복(Lv.1), 없는 숫자 더하기(Lv.1), 문자열 내림차순으로 배치하기(Lv.1) — cross-verified 시도는 안 했으나 check_List상 표기 그대로면 미풀이 가능성 있음.

## 최근 다룬 핵심 유형 (중복 방지 체크리스트)
- BFS (미로/리코쳇 로봇 등, 단어 변환 — 암묵적 그래프 재응용)
- 그리디 - 투 포인터
- 해시맵 카운팅 (정렬 + Map 조합)
- 최소 힙(PriorityQueue) 기반 구간 스케줄링 (호텔 대실)
- 큐 두 개 시뮬레이션 (순환 배열) (두 큐 합 같게 만들기)
- 구간 합 / 차분 배열 (Difference Array) (파괴되지 않은 건물)
- 이분 탐색 (파라메트릭 서치) — 신규 도입 완료 (입국심사)
- 수학적 완전탐색 (약수 관계) — 신규 도입 완료 (카펫)
- 그래프 최단경로 (플로이드-워셜) — 신규 도입 완료 (합승 택시 요금)
- DP (동적 계획법) — 신규 도입 완료 (정수 삼각형, Top-down 누적 방식)
- Union-Find(분리집합) + MST(크루스칼) — 신규 도입 완료 (섬 연결하기)
- 그래프 단일 출발점 최단경로 (다익스트라, PriorityQueue + Lazy Deletion) — 신규 도입 완료 (배달)
- DP 2차원 격자형 (min(위, 왼쪽, 대각선) + 1 패턴) — 신규 도입 완료 (가장 큰 정사각형 찾기)
- 플로이드-워셜의 "관계 추론(도달 가능성)" 응용 — 신규 도입 완료 (순위)
- 다단계 문자열 규칙 순차 처리 (7단계형) — 재응용 완료, 왕복 0회 (신규 아이디 추천)
- 재귀(DFS) 기반 "포함/제외" 조합 생성 — 메뉴 리뉴얼 패턴 재응용 완료 (순위 검색)
- 해시맵 전처리 인덱싱 (등록 시점에 미리 다수 키로 펼쳐두기) — 신규 도입 완료 (순위 검색)
- 이진 탐색 lower-bound 응용 ("특정 값 이상 개수 세기") — 입국심사 패턴 재응용 완료 (순위 검색)
- BFS depth 관리 (Node+depth 묶음 방식) — 재응용 완료, 왕복 1회 (단어 변환)
- 모노토닉 스택 (Monotonic Stack) — 신규 도입 완료, 왕복 4~5회 (주식 가격)
- 백트래킹(DFS) 기반 조합 배정 + Set을 이용한 순서무관 중복 제거 — 신규 도입 완료, 왕복 1회 (불량 사용자)
- 재귀(Recursion) 기반 "믿음의 도약" (from/to/via 역할 교대) — 신규 도입 완료, 왕복 0회 (하노이의 탑)
- 트리 구성(좌표 기반 BST 삽입) + 전위/후위 순회 재귀 — 신규 도입 완료 (길 찾기 게임)
- 1차원 순수 재귀 (분할 후 위임 패턴, `cut(M,N)=N+cut(M-1,N)`) — 신규 도입 완료, 왕복 3회 (종이 자르기)
- 부모 체인 단방향 재귀 (트리형이지만 한 방향으로만 타고 올라가는 단순 구조) — 신규 도입 완료, 왕복 1회 (다단계 칫솔 판매)
- 백트래킹 + 1차원 압축 설계 (2차원 보드 대신 depth당 값 하나만 기록) — 재응용 완료, 왕복 2회 (N-Queen)
- 연속 구간(Run-length) 그룹핑 + 대표 원소 키 설계 (TreeMap 활용 정렬) — 신규 도입 완료, 왕복 1회 (외톨이 알파벳)
- 백트래킹(DFS) 기반 순열 배정 (1:1 매칭 최적화) — N-Queen 패턴 재응용, 왕복 거의 0회 (체육대회)
- 역방향 재귀 (자식→부모 인덱스 역산, 몫/나머지 활용) — 다단계 칫솔 판매 패턴 재응용 + 신규 수식, 왕복 2회 (유전법칙)
- 이벤트 기반 시뮬레이션 + 우선순위 큐 이중 관리 (대기열/실행대기열 분리) — 신규 도입 완료, 왕복 8회 내외 (운영체제)
- 좌표평면 방향탐색 (dx/dy 순환 배열, 격자 BFS 패턴 재적용 시 부호 재검증 필요) — 재응용 완료, 왕복 4회 (실습용 로봇)
- 그리디 + 최소 힙 반복 병합 (허프만 스타일) — 신규 도입 완료, 왕복 0회 (신입사원 교육)
- 구간 겹침 최대 개수 (투 포인터) — 호텔 대실 패턴 재응용 완료, 왕복 7~8회 (카페 확장)
- 3차원 상태 공간 BFS (좌표 + 아이템 사용 여부 함께 추적) — 신규 도입 완료, 왕복 6회 이상 (보물 지도)
- 트리(이진 트리) + 백트래킹, 정적 구조/동적 상태 분리 관리 — 신규 도입 완료, 왕복 8회 이상 (양과 늑대)
- Trie(트라이) + 접두사/접미사 와일드카드 매칭 (정방향/역방향 이중 트라이) — 신규 도입 완료, 왕복 15회 이상 (가사 검색)
- 노드별 카운팅 자료구조의 공간복잡도 최적화 (고정 크기 배열 → 해시맵 전환) — 신규 도입 완료, 동일 세션 내 재응용 (가사 검색)
- Map 기반 문자열-숫자 매핑 + 접두사 길이 매칭 — 신규 도입 완료, 왕복 3회 (숫자 문자열과 영단어)
- 큐 슬롯 고정 + 0 패딩 시뮬레이션 (고정 크기 큐로 물리적 공간 표현) — 재응용, 왕복 10회 이상, 종료조건 설계 다수 시행착오 (다리를 지나는 트럭)
- 격자 좌표 맨해튼 거리 계산 (`|행차이|+|열차이|`) — 신규 도입, BFS와의 선택 기준을 스스로 판단 (키패드 누르기)
- String.split(String, limit) 파라미터 — trailing 공백 보존 트릭, 신규 도입 (이상한 문자 만들기)

## 다음 회차 후보 유형 (Day 141+)
- 사라지는 발판(Lv.3) — 게임 이론형 완전탐색, 신규 유형. 정답률이 낮아 사용자 요청으로 보류 중 — 추후 컨디션 좋을 때 재배치.
- 징검다리(Lv.4, 이분 탐색/파라메트릭 서치 재응용) — Lv.1~3 집중 기간 동안 보류.
- 4장 문자열 챕터(Lv.0~1, 8문제 미검증) — 낮은 우선순위 유지, Lv.1~3 반복 훈련 시 필러 문제로 활용 가능.
- 블라인드 재도전 후보 (Lv.2~3 반복 훈련용): 등굣길(Day93), 소수 찾기(Day15), 불량 사용자, 주식 가격, 스킬트리 등 — check_List상 미풀이 표기와 무관하게 반복 훈련 목적이면 재선정 가능.

## Lv.4 진입 판단 (Day 139 기준 갱신)
- **긍정적 신호 강화:** 체육대회(백트래킹, N-Queen 재응용)를 코치 개입 최소한으로 첫 코드에서 바로 통과 — 재귀/백트래킹 감각이 "재귀"라는 표찰이 없는 완전탐색형 문제에도 자연 전이됨을 확인. 재귀 관련 재검토 기준(2번: 왕복 3~4회 이내)은 사실상 충족.
- **신규 긍정 신호 (Day134):** 보물 지도(좌표 기반 BFS)에서 배열 크기를 `n+1`로 지적 없이 스스로 설계함 — 그래프형 배열 크기 재검토 기준(1번)에 대한 첫 데이터 포인트 확보.
- **미해결/신규 신호:** "선언은 했지만 반환 흐름에 연결 안 됨" 패턴이 재귀 호출 결과 미반환, 분기 일부 경로 return 누락 등으로 재발하여, 이 패턴군 자체의 완전한 해소는 아직 진행형. "상태 공간을 확장할 때 관련된 모든 참조 지점에 일관되게 반영하지 못하는" 유형의 실수가 Day134~135에 반복 관찰됨.
- **신규 데이터 (Day135):** 트리+백트래킹이라는 완전 신규 패턴에서 왕복 8회 이상 소요됨 — 신규 패턴 도입 세션 특성상 재검토 기준 2번은 아직 미충족이나, 막힐 때마다 원인을 스스로 좁혀나가는 디버깅 태도는 안정적으로 유지됨.
- **신규 데이터 (Day136):** 완전히 새로운 자료구조(Trie)를 개념 설명만 듣고 스스로 설계해내는 능력이 이번에도 확인됨 — Day135(트리)에 이어 2세션 연속으로 "새 자료구조를 처음 도입할 때 구조적 설계 자체는 코치 개입 없이 해낸다"는 패턴이 관찰됨. 다만 Map API 문법에서는 인자 개수/순서, 필드-메서드 구분 관련 실수가 같은 세션 안에서 여러 차례 반복됨.
- **긍정적 신호 추가 (Day137):** Day136에서 지적됐던 Map API(`getOrDefault`/`computeIfAbsent`) 인자 개수·시그니처 혼동이, 순위 검색 블라인드 재도전에서 단 한 번도 재발하지 않음.
- **신규 관찰 (Day137):** 이진 탐색 `mid` 재계산 위치, 제어 흐름의 배타적 분기 관련 실수가 새로 나타남 — "반복문/분기문 골격 설계" 계열의 실수로 별도 관찰 필요.
- **참고할 만한 신규 신호 (Day138):** 자료구조가 아닌 "연산의 성질"(반전 두 번 = 복원) 자체를 스스로 도출한 것은, 향후 더 복잡한 상태 변환 문제(Lv.4)에서도 도움이 될 수 있는 새로운 차원의 직관.
- **사용자 요청에 의한 진행 방향 결정 (Day139):** 코치 판단 기준으로는 재검토 기준 1·2번이 상당 부분 충족되어가는 추세이나, 사용자 본인이 "Lv.4 진입은 Lv.1~3이 완전히 마스터된 이후로" 명시적으로 요청함. 이는 코치의 기술적 판단과 별개로 학습자의 페이스 결정권을 존중해야 하는 사안 — Lv.4(징검다리) 재개 시점은 사용자가 다시 신호를 줄 때까지 보류.
- **신규 관찰 (Day139):** 다리를 지나는 트럭에서 "자료구조의 불변 속성(큐 크기 고정)을 놓치고 종료 조건을 잘못 설계"하는 새로운 유형의 실수가 나타남 — 기존 "상태 갱신 참조 지점 누락" 계열과는 결이 다른, "이 자료구조에서 뭐가 변하고 뭐가 고정인지 구분 못 함" 계열로 별도 관찰 필요. 다만 잘못된 방향(`break`)을 스스로 의심하고 되돌아온 것은 좋은 디버깅 직관의 증거.

### Lv.4 진입 재검토 기준 (변경 없음)
1. 새로운 그래프형 문제를 처음 마주쳤을 때, 배열 크기를 지적 없이 스스로 n+1로 잡는 것이 **2~3세션 연속**으로 확인될 것
2. 한 문제당 코드 제출 왕복이 평균 **3~4회 이내**로 줄어들 것
3. **같은 세션 안에서** 동일한 지적(변수명, 배열 크기, 경계 조건 등)을 두 번 이상 반복해서 받지 않을 것

## 최근 취약 포인트 (누적 추적용)
- **✅ 긍정 신호 (Day140):** "이동 거리 계산 = BFS"라는 반사적 연결을 코치 질문 한 번에 스스로 재검토하고, 격자 규칙성을 근거로 공식 기반 풀이로 전환함 — 도구 선택 판단력 향상 신호.
- **✅ 긍정 신호 (Day140):** 실패한 테스트 케이스를 보고 원인(연속 공백 vs trailing 공백)을 스스로 좁혀 나가며 `split`의 limit 파라미터까지 도달함 — 이전 학습(JadenCase, Day100)을 새로운 상황에 정확히 재적용함.
- **신규, 경미 (Day140):** 배열을 개별 변수(`left_row, left_col`)에서 묶음 배열(`left`)로 리팩토링하는 과정에서 타입 선언(`int` → `int[]`)을 놓치는 컴파일 에러 — 리팩토링 시 "선언부까지 함께 바꿨는가" 점검 습관으로 일반화 필요.
- **신규, 경미 (Day140):** 변수 이름 리네이밍(`map` → `phone_map`) 중 일부 사용처를 놓치는 실수 — 기존 "변수명 불일치" 계열과 동일, 계속 관찰.
- **✅ 긍정 신호 (Day139):** 숫자 문자열과 영단어에서 영단어 매칭 타이밍(길이 기반 접두사 충돌 없는 구간 감지)을 코치 개입 없이 정확하게 설계함.
- **✅ 긍정 신호 (Day139):** 다리를 지나는 트럭에서 스스로 짠 `break` 로직이 "아직 할 일이 남았는데 억지로 끝내는" 형태라는 걸 의심하고, 반복 조건 자체를 수정하는 올바른 방향으로 빠르게 전환함.
- **신규 (Day139):** 자료구조의 "불변 속성"을 못 알아채고 종료조건으로 잘못 사용하는 실수 — 큐를 고정 크기로 패딩 설계해놓고도 `q.isEmpty()`를 종료 조건으로 남겨둬 무한루프 발생. "이 자료구조에서 실제로 변하는 값과 고정된 값을 먼저 구분"하는 습관으로 일반화 필요.
- **재발 (Day139):** 반복문 종료 조건 설계 시 AND/OR 선택 실수 — "둘 중 하나만 참이어도 계속"인 상황을 처음에 `&&`로 잘못 묶었다가 스스로 `||`로 교정. Day133(운영체제)과 동일 계열, 계속 관찰.
- **재발 (Day139):** `&&` 조건에서 배열 인덱스 접근이 범위체크보다 먼저 평가되도록 순서를 잘못 배치 — 이후 스스로 순서 교정.
- **재발 (Day139):** 경계값(등호) 민감도 — `<` vs `<=`, `nextIdx <= length` vs `< length` 등에서 여러 차례 등호 하나 차이로 헤맴. 기존 항목과 동일 계열, 계속 관찰.
- **재발 (Day138):** 반복문 증감 방향(`i++`/`i--`) 실수 — 2회 발생(그 중 1회는 무한루프+인덱스 초과 위험).
- **재발 (Day138):** 변수명 불일치(`isNext` 선언 vs `is_next` 사용) — 기존 "for문 증감식/변수명 오타" 항목과 동일 계열, 계속 관찰.
- **재발 (Day138):** 존재하지 않는 메서드명으로 먼저 시도하는 경향 (`Character.isUpper/isLower`, `Deque` import 누락) — "새 메서드 사용 전 시그니처/존재 여부 확인" 습관, 계속 관찰 대상.
- **신규 (Day138):** 조건 없이 무조건 실행되는 문장이 직전 로직의 결과를 덮어쓰는 실수 (프렌즈4블록 중력 로직) — "이 줄이 매번 무조건 실행되는가"를 점검하는 습관으로 일반화.
- **✅ 긍정 신호 (Day138):** 변수 스코프를 스스로 정확히 인지함.
- **✅ 긍정 신호 (Day138):** 스택의 "이중 반전으로 순서 복원" 개념을 힌트 없이 스스로 제안함.
- **✅ 해소 확인 (Day137):** Map API 시그니처(인자 개수/순서) 혼동 — Day128, Day136에서 반복됐던 문제가 Day137에서 재발 없이 정확히 사용됨.
- **신규 (Day137):** 반복문/제어 흐름 골격 설계 실수 — 이진 탐색 `mid` 계산 위치, 재귀 함수 배타적 분기 미처리.
- **신규 (Day137, 경미):** 메서드 인자 개수 오기억 — `String.trim(" ")`, `Map.getOrDefault(key, 0)` 타입 불일치 등.
- **경로 위의 "시작 노드(root)" 처리 누락 (Day136 신규):** Trie에서 확정 글자 0개 쿼리 처리 시 `root` 자신이 카운팅에서 누락되는 문제.
- **"시각(timestamp)"과 "기간(duration)" 혼동 (Day134):** 카페 확장에서 도착 시각과 제조 시간을 섞어 비교하려는 실수.
- **상태 공간 확장 시 참조 지점 전체의 일관성 누락 (Day134):** 보물 지도에서 2차원→3차원 visit 확장 시 여러 참조 지점 반영 누락.
- **구조(자료구조) 확장 시 참조 지점 전체 갱신 누락 (Day135 재발):** 양과 늑대에서 candidate 리스트 관련 재발.
- **초기화 단계의 경계 조건(이중 처리) 실수 (Day135):** 루트 노드 이중 카운팅.
- **조건식 설계 시 AND/OR 혼동 및 상수 오용 (Day133):** 운영체제에서 논리연산자 방향 착오, 배열 길이 대신 무관한 상수 사용.
- **배열 인덱싱이 이미 부호를 담고 있음에도 위에 중복 분기(if)를 씌우는 습관 (Day133).**
- **격자탐색 부호를 다른 도메인(좌표평면)에 무비판적으로 재사용 (Day133):** 예시 손 검증으로 즉시 자가 교정함 — 개선 신호.
- **"선언은 했지만 반환 흐름에 연결 안 됨" 패턴의 새로운 변형 (Day132):** 재귀 호출 결과 미반환 형태로 재발.
- **분기가 있는 함수에서 "모든 실행 경로가 값을 리턴하는가" 확인 누락 (Day132).**
- **char 리터럴과 String 리터럴 혼동 (Day131):** Day132에서 개선 확인.
- **배열 인덱스와 저장값의 역할 혼동 (Day131).**
- **빈 컬렉션에서 peek()/pop() 호출 순서 실수 (Day129).**
- **백트래킹 시 방문 배열(visit) 초기화 위치 오류 (Day129):** Day132에서 개선 확인.
- **원본 문자열 vs split 결과 배열 인덱싱 혼동 (Day128).**
- **Map 조회 API 선택 혼동 (Day128):** Day137에서 해소 확인.
- **그래프형 배열 크기 설계 실수 (관찰 지속, Day134 긍정 데이터 1건).**
- **대칭 관계 단방향 체크 누락 (Day127 발견):** 관찰 지속.
- **if-else 배타적 분기 처리:** Day128 이후 개선 신호 유지 중.
- 코드 신뢰보다 검증 우선, 다차원 배열 인덱스 범위 혼동, 이분 탐색 종료 조건/경계 이동 방향 착오, 조건 반전 실수 — 기존 항목 유지, 계속 추적 필요.

## 참고: check_List 파일 신뢰도 이슈 (Day 128 발견, Day 130/131 추가 확인)
- Day 128 문제 선정 과정에서 `check_List`의 "미풀이로 추정" 표시 다수가 실제로는 이미 오래전에 풀린 문제로 확인됨 (예: 거리두기 확인하기 Day49, 가장 큰 수 Day13, 디스크 컨트롤러 Day23, 다리를 지나는 트럭 Day17, 보석 쇼핑 Day76, 두 개 뽑아서 더하기 Day1, 등굣길 Day93, 양궁대회 Day44, 메뉴 리뉴얼 Day49 등).
- **Day 130 추가 확인:** 계획했던 3문제(가장 큰 수 Day13, 다리를 지나는 트럭 Day17, 등굣길 Day93)가 전부 중복으로 드러남. 모음 사전(Day41), 소수 찾기(Day15), 보석 쇼핑(Day76), 디스크 컨트롤러(Day23)도 이미 완료된 문제임을 확인.
- **Day 131 추가 확인:** 모음 사전(Day41), 양궁대회(Day44), 타겟 넘버(Day9/90/107), 괄호 변환(Day52)이 또 "미풀이로 추정"으로 오표기되어 있던 것을 발견.
- **Day 132~136 확인:** 여러 세션에 걸쳐 교차검증 완료, 오표기 이슈 없는 경우도 다수 확인됨 (외톨이 알파벳/체육대회/유전법칙, 운영체제/실습용 로봇/신입사원 교육, 카페 확장/보물 지도/양과 늑대, 가사 검색/징검다리).
- **Day 137 대규모 재확인:** 순위 검색, 신고 결과 받기, 베스트앨범, 가장 큰 수, 피보나치 수, 메뉴 리뉴얼/거리두기 확인하기, 단속카메라, 양궁대회 등 다수가 "미풀이"로 잘못 표시되어 있던 것을 확인. **check_List의 8~11장, 13장 대부분이 신뢰할 수 없는 상태.**
- **Day 138 추가 확인:** 4장 문자열의 "신규 아이디 추천(Lv.1)"도 오표기 확인 — 8~13장뿐 아니라 4장까지 오표기 범위 확대됨.
- **권장:** 여유 있을 때 `check_List`를 README.md 전체 기준으로 재검증해서 갱신할 것. 그 전까지는 매일 문제 선정 직후, 반드시 project_knowledge_search로 교차 확인 후 세트를 확정할 것.

## 사용 안내 (이 파일의 목적)
- 이 파일은 매일 문제 선정 시 README.md 전체를 다시 검색하지 않고 최근 상태만 빠르게 파악하기 위한 경량 상태 파일입니다.
- 매일 훈련이 끝나면, README.md 갱신과 함께 이 파일의 현재 Day, 최근 5일 요약, 예고된 다음 문제, 취약 포인트 섹션을 갱신해주세요.
- 최근 5일 요약은 6일째부터는 가장 오래된 항목을 삭제하여 항상 5개 내외로 유지하는 것을 권장합니다.
- "Lv.4 진입 판단" 섹션은 재검토 기준이 충족되기 전까지는 매 세션 종료 시 근거(제출 왕복 횟수, 반복 지적 여부)를 간단히 업데이트해주세요.
- **[README 출력 양식 — 2026.08.12 갱신]** 매일 README.md에 반영할 Day 정리는 다음 구조를 따른다:
  1. `### Day N (YYYY.MM.DD) - 핵심 유형 요약` 헤더
  2. 그 아래 1~2문장짜리 소개 단락 (그날 새로 체화한 개념 요약, 이월 여부 명시)
  3. `난이도 | 문제 이름 | 파일 이름 | 핵심 정리` 표 — 각 문제의 "핵심 정리" 칸 안에 왕복 횟수를 굵게 명시하고, 번호 매긴(1. 2. 3...) 세부 학습 포인트를 이어서 적는다.
  4. 마지막에 `#### 💡 Today I Learned (TIL)` — 번호 매긴 리스트, 각 항목은 굵은 소제목: 설명 형태.
  - 채팅에 출력할 때는 내부에 마크다운 헤더/표/굵은 글씨가 섞여 있어 렌더링이 깨질 수 있으므로, 항상 4중 백틱(````)으로 전체를 감싸서 출력한다.
- **[Day130 신규 지침]** 문제 선정 직후, check_List 확인만으로 끝내지 말고 반드시 project_knowledge_search로 실제 미풀이 여부를 검증한 뒤 세트를 확정할 것.
- **[Day132 신규 지침]** PCCP류 문제는 "코딩테스트 연습" 검색으로 안 나올 수 있음 — 코스 페이지 직접 링크 안내할 것.
- **[Day134 신규 지침]** 상태 공간(visit)을 확장할 때는, 확장 직후 "초기화 / 범위체크 / 방문마킹 / 큐 삽입값"의 4곳을 순서대로 훑어 누락된 지점이 없는지 체크하는 습관을 세션 도입부에서 짚어줄 것.
- **[Day136 신규 지침]** Lv.4 이상 문제는 제약조건에 "전체 길이의 합" 같은 숨은 제한이 있는 경우가 많음 — 최악의 경우를 계산할 때 빠짐없이 검색해서 확인할 것. 새 Map/컬렉션 API가 처음 등장할 때는 사용 전에 시그니처를 먼저 명확히 짚어줄 것.
- **[Day139 신규 지침]** 사용자가 Lv.4 진입 보류를 명시적으로 요청한 이상, 코치가 판단하는 "재검토 기준 충족 여부"와 무관하게 사용자가 먼저 신호를 줄 때까지 Lv.4 신규 문제는 세트에 배치하지 않는다. 반복문/큐 등 자료구조를 활용한 시뮬레이션 문제에서는 "이 자료구조의 어떤 속성이 고정이고 어떤 속성이 변하는지"를 도입부에서 먼저 짚어주는 것이 종료조건 설계 실수를 줄이는 데 효과적일 수 있음.

## 파일 매핑 (Day139)
- Day139/NumberStringAndWord.java — 숫자 문자열과 영단어 (Lv.1)
- Day139/TruckBridge.java — 다리를 지나는 트럭 (Lv.2, 블라인드 재도전)
- 다음 세션으로 이월: 사라지는 발판 (Lv.3)

## 파일 매핑 (Day140)
- Day140/KeypadPress.java — 키패드 누르기 (Lv.1)
- Day140/StrangeCharacter.java — 이상한 문자 만들기 (Lv.1)
- Day140/StringHandlingBasic.java — 문자열 다루기 기본 (Lv.1)