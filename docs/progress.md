# Progress — Algorithm-Study

## 코딩테스트 진행 상태
- 현재 Day: 133 (완료 — PCCP 모의고사 1회를 운영체제(Lv.3)로 완주하고, 곧바로 2회 챕터(실습용 로봇, 신입사원 교육) 신규 개척)
- 다음 세션: **Day 134 — PCCP 모의고사 2회 3~4번(카페 확장 Lv.2, 보물 지도 Lv.3)으로 2회 챕터 완주.** 문제 선정 직후 반드시 project_knowledge_search로 미풀이 여부 재검증 후 확정할 것.
- **[Day132 프로세스 개선]** PCCP 모의고사 문제는 일반 "코딩테스트 연습" 검색창에 이름을 쳐도 안 걸림 — 별도 코스 페이지(`school.programmers.co.kr/learn/courses/15008` 등)로 직접 접근해야 함. 또한 코치(Claude)가 문제 지문을 기억에 의존해 설명하다 오류를 낸 사례 발생(체육대회 지문 오설명) — 이후 문제 지문은 항상 웹 검색/실제 페이지 확인 후 제시하는 것으로 습관 교정함.

## 최근 5일 요약 (문제 선정 시 중복 회피용)
- Day 129: 그래프 최단경로 재응용(BFS depth 관리, 단어 변환 — Day128 이월분 완주), 모노토닉 스택 신규 도입(주식 가격), 백트래킹(DFS) + Set 중복 제거 신규 도입(불량 사용자) — WordTransformation.java, StockPrice.java, BadUser.java. **3문제 모두 완주.** 주식 가격은 모노토닉 스택 신규 도입 과정에서 4~5회 왕복 — "빈 컬렉션에서 peek/pop 호출" 이슈가 신규 취약 포인트로 관찰됨.
- Day 130: 재귀(Recursion) 신규 도입(하노이의 탑, "믿음의 도약" 체화) + 트리 구성(BST 삽입)·전위/후위 순회 신규 도입(길 찾기 게임). check_List 교차검증 누락으로 세트 재구성. 하노이의 탑은 왕복 0회, 길 찾기 게임은 다수 왕복 끝에 완주. 세션 종료 시 본인이 "재귀 감이 아직 완전히 안 잡혔다"고 피드백함.
- Day 131: 재귀(Recursion) 단독 집중 훈련 — Lv.0~3 난이도 순수 재귀/백트래킹 3문제(종이 자르기, 다단계 칫솔 판매, N-Queen). **3문제 모두 완주, 왕복 각각 3회/1회/2회로 Day130 대비 뚜렷한 개선.** char/String 리터럴 혼동(`'-'` vs `"-"`)으로 NPE 발생 후 즉시 수정. 세션 종료 시 재귀에 대한 심리적 부담이 확연히 줄어든 신호 관찰됨.
- Day 132: 재귀 훈련 이후 첫 실전 로테이션 복귀 — 완전 미개척 챕터였던 14장 PCCP 모의고사 1회 1~3번(외톨이 알파벳, 체육대회, 유전법칙) 신규 개척. **3문제 모두 완주, 왕복 1회/거의0회/2회.** 체육대회에서 N-Queen 백트래킹 패턴이 "재귀"라는 이름표 없는 완전탐색형 문제에도 즉시 전이되는 강한 긍정 신호 확인. 유전법칙(역방향 재귀, 자식→부모 역산)에서 "재귀 호출 결과를 반환에 연결 안 함" + "분기 일부 경로에서 return 누락"이라는, 기존 취약점의 새로운 변형이 재발함.
- Day 133: PCCP 모의고사 1회를 운영체제(Lv.3)로 완주하고, 곧바로 2회 챕터(실습용 로봇 Lv.1, 신입사원 교육 Lv.2)를 신규 개척. **3문제 모두 완주, 왕복 8회 내외/4회/0회.** 이벤트 기반 시뮬레이션(대기열/실행대기열 PQ 이중 관리)을 신규 도입 — 우선순위 정렬 기준(점수 우선, 호출시각은 타이브레이크)을 반례로 스스로 검증했고, 조건식(AND/OR 혼동, 배열 길이 대신 무관한 상수 사용) 실수는 있었으나 매번 반례·손 시뮬레이션으로 자가 교정. 좌표평면 방향탐색(실습용 로봇)에서 격자 BFS의 row/col 부호를 그대로 가져다 쓰다 어긋난 것을 예시 검증으로 교정. 그리디+최소힙 반복 병합(허프만 스타일, 신입사원 교육)은 왕복 0회로 최고 성과.

## 예고된 다음 문제
- **Day 134: PCCP 모의고사 2회 3~4번(카페 확장 Lv.2, 보물 지도 Lv.3)으로 2회 챕터 완주.**
- **필수:** 문제 선정 직후 project_knowledge_search로 실제 미풀이 여부를 반드시 재검증한 뒤 세트를 확정할 것.
- **필수:** PCCP류 문제는 "코딩테스트 연습" 검색이 아니라 코스 페이지 직접 링크로 접근 안내할 것. 문제 지문은 기억에 의존하지 말고 매번 검색/확인 후 제시할 것.

## 최근 다룬 핵심 유형 (중복 방지 체크리스트)
- BFS (미로/리코쳇 로봇 등, 단어 변환 — 암묵적 그래프 재응용)
- 그리디 - 투 포인터
- 해시맵 카운팅 (정렬 + Map 조합)
- 최소 힙(PriorityQueue) 기반 구간 스케줄링 (호텔 대실)
- 큐 두 개 시뮬레이션 (순환 배열) (두 큐 합 같게 만들기)
- 구간 합 / 차분 배열 (Difference Array) (파괴되지 않은 건물)
- 이분 탐색 (파라메트릭 서치) — 신규 도입 완료 (입국심사)
- 수학적 완전탐색 (약수 관계) — 신규 도입 완료 (카펫)
- 그래프 최단경로 (플로이드-워셜) — 신규 도입 완료 (합승 택시 요금)
- DP (동적 계획법) — 신규 도입 완료 (정수 삼각형, Top-down 누적 방식)
- Union-Find(분리집합) + MST(크루스칼) — 신규 도입 완료 (섬 연결하기)
- 그래프 단일 출발점 최단경로 (다익스트라, PriorityQueue + Lazy Deletion) — 신규 도입 완료 (배달)
- DP 2차원 격자형 (min(위, 왼쪽, 대각선) + 1 패턴) — 신규 도입 완료 (가장 큰 정사각형 찾기)
- 플로이드-워셜의 "관계 추론(도달 가능성)" 응용 — 신규 도입 완료 (순위)
- 다단계 문자열 규칙 순차 처리 (7단계형) — 재응용 완료, 왕복 0회 (신규 아이디 추천)
- 재귀(DFS) 기반 "포함/제외" 조합 생성 — 메뉴 리뉴얼 패턴 재응용 완료 (순위 검색)
- 해시맵 전처리 인덱싱 (등록 시점에 미리 다수 키로 펼쳐두기) — 신규 도입 완료 (순위 검색)
- 이진 탐색 lower-bound 응용 ("특정 값 이상 개수 세기") — 입국심사 패턴 재응용 완료 (순위 검색)
- BFS depth 관리 (Node+depth 묶음 방식) — 재응용 완료, 왕복 1회 (단어 변환)
- 모노토닉 스택 (Monotonic Stack) — 신규 도입 완료, 왕복 4~5회 (주식 가격)
- 백트래킹(DFS) 기반 조합 배정 + Set을 이용한 순서무관 중복 제거 — 신규 도입 완료, 왕복 1회 (불량 사용자)
- 재귀(Recursion) 기반 "믿음의 도약" (from/to/via 역할 교대) — 신규 도입 완료, 왕복 0회 (하노이의 탑)
- 트리 구성(좌표 기반 BST 삽입) + 전위/후위 순회 재귀 — 신규 도입 완료 (길 찾기 게임)
- 1차원 순수 재귀 (분할 후 위임 패턴, `cut(M,N)=N+cut(M-1,N)`) — 신규 도입 완료, 왕복 3회 (종이 자르기)
- 부모 체인 단방향 재귀 (트리형이지만 한 방향으로만 타고 올라가는 단순 구조) — 신규 도입 완료, 왕복 1회 (다단계 칫솔 판매)
- 백트래킹 + 1차원 압축 설계 (2차원 보드 대신 depth당 값 하나만 기록) — 재응용 완료, 왕복 2회 (N-Queen)
- 연속 구간(Run-length) 그룹핑 + 대표 원소 키 설계 (TreeMap 활용 정렬) — 신규 도입 완료, 왕복 1회 (외톨이 알파벳)
- 백트래킹(DFS) 기반 순열 배정 (1:1 매칭 최적화) — N-Queen 패턴 재응용, 왕복 거의 0회 (체육대회)
- 역방향 재귀 (자식→부모 인덱스 역산, 몫/나머지 활용) — 다단계 칫솔 판매 패턴 재응용 + 신규 수식, 왕복 2회 (유전법칙)
- 이벤트 기반 시뮬레이션 + 우선순위 큐 이중 관리 (대기열/실행대기열 분리) — 신규 도입 완료, 왕복 8회 내외 (운영체제)
- 좌표평면 방향탐색 (dx/dy 순환 배열, 격자 BFS 패턴 재적용 시 부호 재검증 필요) — 재응용 완료, 왕복 4회 (실습용 로봇)
- 그리디 + 최소 힙 반복 병합 (허프만 스타일) — 신규 도입 완료, 왕복 0회 (신입사원 교육)

## 다음 회차 후보 유형 (Day 134+)
- PCCP 모의고사 2회 3~4번(카페 확장 Lv.2, 보물 지도 Lv.3)으로 2회 챕터 완주 우선 고려.
- Lv.4 진입은 그래프형 배열 크기(n+1) 기준 재검토 데이터 부족으로 계속 보류. (Day133에도 그래프형 문제 없어 데이터 미확보)

## Lv.4 진입 판단 (Day 132 기준 갱신)
- **긍정적 신호 강화:** 체육대회(백트래킹, N-Queen 재응용)를 코치 개입 최소한으로 첫 코드에서 바로 통과 — 재귀/백트래킹 감각이 "재귀"라는 표찰이 없는 완전탐색형 문제에도 자연 전이됨을 확인. 재귀 관련 재검토 기준(2번: 왕복 3~4회 이내)은 사실상 충족.
- **미해결/신규 신호:** "선언은 했지만 반환 흐름에 연결 안 됨" 패턴이 새로운 변형(재귀 호출 결과 미반환, 분기 일부 경로 return 누락)으로 재발하여, 기준 3번("같은 세션 안에서 동일 지적 두 번 이상 금지")과는 별개로 이 패턴군 자체의 완전한 해소는 아직 진행형. 그래프형 배열 크기(n+1) 기준은 Day132에도 그래프형 문제가 없어 데이터 없음.
- **결론:** 재귀/백트래킹의 "설계 전이력"은 뚜렷하게 좋아졌으나, "반환 연결" 계열의 세부 실수는 형태를 바꿔가며 계속 나타나는 중. 다음 그래프형 문제 등장 시 배열 크기 기준을 최우선 확인하고, 반환 누락 패턴은 앞으로도 계속 관찰할 것.

### Lv.4 진입 재검토 기준 (변경 없음)
1. 새로운 그래프형 문제를 처음 마주쳤을 때, 배열 크기를 지적 없이 스스로 n+1로 잡는 것이 **2~3세션 연속**으로 확인될 것
2. 한 문제당 코드 제출 왕복이 평균 **3~4회 이내**로 줄어들 것
3. **같은 세션 안에서** 동일한 지적(변수명, 배열 크기, 경계 조건 등)을 두 번 이상 반복해서 받지 않을 것

## 최근 취약 포인트 (누적 추적용)
- **조건식 설계 시 AND/OR 혼동 및 상수 오용 (Day133 신규):** 운영체제에서 `while(!pq.isEmpty() && idx<10)`처럼 논리연산자 방향 착오, 배열 길이 대신 무관한 상수(10, 점수 범위와 혼동) 사용. 반례와 손 시뮬레이션으로 매번 스스로 교정함 — 개선 여지는 있으나 자가 교정 속도는 빠른 편.
- **배열 인덱싱이 이미 부호를 담고 있음에도 위에 중복 분기(if)를 씌우는 습관 (Day133 신규):** 실습용 로봇에서 `dx[d]`,`dy[d]` 값 자체로 충분한데 `if(d==0||d==2)` 식 분기를 추가로 씀. 다음엔 배열 인덱싱만으로 분기 없이 처리하는 걸 스스로 시도해볼 것.
- **격자탐색 부호를 다른 도메인(좌표평면)에 무비판적으로 재사용 (Day133 신규):** 예시 손 검증으로 즉시 자가 교정함 — 개선 신호로 볼 수 있음.
- **"선언은 했지만 반환 흐름에 연결 안 됨" 패턴의 새로운 변형 (Day132):** 이번엔 변수가 아니라 **재귀 호출 자체**를 호출만 하고 결과를 받지도 리턴하지도 않는 형태로 재발함(유전법칙). Day131의 즉각적인 자가 교정과 달리, 이번엔 코치 지적이 필요했음 — 재귀 호출 결과를 반드시 변수에 담거나 즉시 리턴해야 한다는 원칙을 재귀 문제 도입부에서 매번 짚어줄 필요.
- **신규: 분기가 있는 함수에서 "모든 실행 경로가 값을 리턴하는가" 확인 누락 (Day132):** 유전법칙에서 `if`문만 작성하고 `else`를 빠뜨림. 조건 분기가 여러 개인 함수를 짤 때, 코드 작성 전 케이스를 말로 먼저 나열하는 습관 필요.
- **"선언은 했지만 반환 흐름에 연결 안 됨" 패턴 (Day131, 개선 신호):** 종이 자르기에서 재발했으나, 코치 지적 없이도 다음 시도에서 거의 바로 스스로 교정 — 자가 교정 속도가 빨라짐 (단, Day132에 재귀 호출 형태로 새로운 변형이 재발했으므로 완전 해소는 아님).
- **char 리터럴과 String 리터럴 혼동 (Day131):** `'-'`(char)와 `"-"`(String)를 `.equals()`로 비교하며 항상 false가 나와 NPE 발생. `.equals()`는 String 객체끼리만 의미가 있다는 걸 실제 런타임 에러로 체득. Day132(유전법칙)에서는 이 교훈을 스스로 적용해 `.equals()`를 정확히 사용함 — 개선 확인.
- **배열 인덱스와 저장값의 역할 혼동 (Day131):** N-Queen에서 `g_col[row] = col`과 `g_col[col] = row`를 헷갈림 — "이 배열을 나중에 조회할 때 어떤 규칙으로 읽을 것인가"를 먼저 정하고 그 규칙에 맞춰 저장해야 함을 스스로 교정.
- **빈 컬렉션에서 peek()/pop() 호출 순서 실수 (Day129):** `!stack.isEmpty() && 조건` 순서(short-circuit)로 방어해야 함을 지적 후 즉시 이해·적용. 스택/큐 계열 문제에서 계속 관찰 필요.
- **백트래킹 시 방문 배열(visit) 초기화 위치 오류 (Day129):** 재귀 함수 내부에서 매번 재생성하면 상태가 소실됨을 지적 후 정확히 이해. Day132(체육대회)에서는 `isVisit[]`을 함수 밖에서 한 번만 생성해 정확히 재사용 — 개선 확인.
- **원본 문자열 vs split 결과 배열 인덱싱 혼동 (Day 128):** 새 변수를 만든 시점에 "이제부터는 이 변수만 써야 한다"를 의식적으로 표시하는 습관 필요.
- **Map 조회 API 선택 혼동 (Day 128):** `get`/`getOrDefault`/`computeIfAbsent` 차이. "이 Key가 항상 존재를 보장받는가?"를 먼저 판단하고 API를 고르는 습관 필요.
- **그래프형 배열 크기 설계 실수 (관찰 지속):** Day133까지도 그래프형 문제가 없어 재발 여부 확인 불가. 다음 그래프 문제에서 계속 관찰.
- **대칭 관계 단방향 체크 누락 (Day 127 발견):** 관찰 지속.
- **if-else 배타적 분기 처리:** Day 128 이후 개선 신호 유지 중.
- 코드 신뢰보다 검증 우선, 경계 조건(등호) 민감도, 상태 동기화 누락, for문 증감식/변수명 오타, 다차원 배열 인덱스 범위 혼동, 이분 탐색 종료 조건/경계 이동 방향 착오, 조건 반전 실수 — 기존 항목 유지, 계속 추적 필요.

## 참고: check_List 파일 신뢰도 이슈 (Day 128 발견, Day 130/131 추가 확인)
- Day 128 문제 선정 과정에서 `check_List`의 "미풀이로 추정" 표시 다수가 실제로는 이미 오래전에 풀린 문제로 확인됨 (예: 거리두기 확인하기 Day49, 가장 큰 수 Day13, 디스크 컨트롤러 Day23, 다리를 지나는 트럭 Day17, 보석 쇼핑 Day76, 두 개 뽑아서 더하기 Day1, 등굣길 Day93, 양궁대회 Day44, 메뉴 리뉴얼 Day49 등).
- **Day 130 추가 확인:** 계획했던 3문제(가장 큰 수 Day13, 다리를 지나는 트럭 Day17, 등굣길 Day93)가 전부 중복으로 드러남. 모음 사전(Day41), 소수 찾기(Day15), 보석 쇼핑(Day76), 디스크 컨트롤러(Day23)도 이미 완료된 문제임을 확인.
- **Day 131 추가 확인:** 재귀 문제 선정 과정에서 모음 사전(Day41), 양궁대회(Day44), 타겟 넘버(Day9/90/107), 괄호 변환(Day52)이 또 "미풀이로 추정"으로 오표기되어 있던 것을 발견 — 매 세션 project_knowledge_search로 교차검증하는 습관이 이번에도 유효하게 작동해 중복 편성 사고는 재발하지 않음.
- **Day 132 확인:** 외톨이 알파벳/체육대회/유전법칙 3문제 모두 project_knowledge_search로 사전 교차검증 완료, 이번엔 오표기 이슈 없이 check_List 표시(미풀이)와 실제 상태가 일치함.
- **Day 133 확인:** 운영체제/실습용 로봇/신입사원 교육 3문제 모두 project_knowledge_search로 사전 교차검증 완료, check_List 표시(미풀이)와 실제 상태 일치 — 오표기 이슈 없음.
- **권장:** 여유 있을 때 `check_List`를 README.md 전체 기준으로 재검증해서 갱신할 것. 그 전까지는 **매일 문제 선정 직후, 반드시 project_knowledge_search로 교차 확인 후 세트를 확정할 것.**

## 사용 안내 (이 파일의 목적)
- 이 파일은 매일 문제 선정 시 README.md 전체를 다시 검색하지 않고 최근 상태만 빠르게 파악하기 위한 경량 상태 파일입니다.
- 매일 훈련이 끝나면, README.md 갱신과 함께 이 파일의 현재 Day, 최근 5일 요약, 예고된 다음 문제, 취약 포인트 섹션을 갱신해주세요.
- 최근 5일 요약은 6일째부터는 가장 오래된 항목을 삭제하여 항상 5개 내외로 유지하는 것을 권장합니다.
- "Lv.4 진입 판단" 섹션은 재검토 기준이 충족되기 전까지는 매 세션 종료 시 근거(제출 왕복 횟수, 반복 지적 여부)를 간단히 업데이트해주세요.
- **[README 출력 양식 — 2026.08.12 갱신]** 매일 README.md에 반영할 Day 정리는 다음 구조를 따른다:
  1. `### Day N (YYYY.MM.DD) - 핵심 유형 요약` 헤더
  2. 그 아래 1~2문장짜리 소개 단락 (그날 새로 체화한 개념 요약, 이월 여부 명시)
  3. `난이도 | 문제 이름 | 파일 이름 | 핵심 정리` 표 — 각 문제의 "핵심 정리" 칸 안에 **왕복 횟수를 굵게 명시**하고, 번호 매긴(1. 2. 3...) 세부 학습 포인트를 이어서 적는다.
  4. 마지막에 `#### 💡 Today I Learned (TIL)` — 번호 매긴 리스트, 각 항목은 **굵은 소제목: 설명** 형태.
  - 채팅에 출력할 때는 내부에 마크다운 헤더/표/굵은 글씨가 섞여 있어 렌더링이 깨질 수 있으므로, **항상 4중 백틱(````)으로 전체를 감싸서 출력한다.**
- **[Day130 신규 지침]** 문제 선정 직후, check_List 확인만으로 끝내지 말고 반드시 project_knowledge_search로 실제 미풀이 여부를 검증한 뒤 세트를 확정할 것.
- **[Day132 신규 지침]** PCCP류 문제는 "코딩테스트 연습" 검색으로 안 나올 수 있음 — 코스 페이지 직접 링크 안내할 것. 코치가 제시하는 문제 지문은 기억이 아니라 매번 검색/확인을 거친 것이어야 함.