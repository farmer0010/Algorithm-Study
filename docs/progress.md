# Progress — Algorithm-Study

## 코딩테스트 진행 상태
- 현재 Day: 138 (완료 — 시저 암호(Lv.1), 프렌즈4블록(Lv.2, 블라인드 재도전), 짝지어 제거하기(Lv.2, 블라인드 재도전) 3문제 완주.)
- **세션 중 계획 급전환:** 8/31(토) SKT 코딩테스트를 앞두고, 사전 예고했던 사라지는 발판(Lv.3)/징검다리(Lv.4) 신규 진입을 시험 이후로 연기. SKT 코테 특성(프로그래머스 기반, Lv.1~3, 시뮬레이션/스택 위주 출제 경향 확인) 조사 후 복습형 세트로 즉시 전환함.
- **check_List 신뢰도 이슈 추가 확인:** 4장 문자열의 "신규 아이디 추천(Lv.1)"도 "미풀이로 추정"으로 오표기되어 있었으나, 실제로는 Day128에 `NewIdRecommend.java`로 이미 완료된 문제로 확인됨. **8~13장뿐 아니라 4장에도 오표기가 존재 — 전면 재검증 필요 범위가 넓어짐.**
- **사용자 학습 계획 공유:** 토요일 SKT 코테 전까지 오전엔 이 세션(개념 복습), 오후엔 프로그래머스 "스킬체크"(모의 코딩테스트)로 추가 실전 연습 병행 예정.

## 최근 5일 요약 (문제 선정 시 중복 회피용)
- Day 134: PCCP 모의고사 2회 챕터 완주(카페 확장 Lv.2, 보물 지도 Lv.3) — 3차원 상태 공간 BFS 신규 도입.
- Day 135: 양과 늑대(Lv.3, 카카오 2022) 신규 도입 — 트리+백트래킹, 왕복 8회 이상.
- Day 136: 가사 검색(Lv.4, 카카오 2020) 신규 도입 — Trie(정/역방향 이중 구조), 왕복 15회 이상.
- Day 137: Lv.2~3 복습 + Map API 문법 정리 — A로 B 만들기, 순위 검색(블라인드 재도전, Map API 실수 0회 확인).
- **Day 138 (신규):** SKT 코테(8/31 토) 대비로 급전환 — 시저 암호(Lv.1, 왕복 3회), 프렌즈4블록(Lv.2, 블라인드 재도전, 왕복 11회 이상, 시뮬레이션+중력), 짝지어 제거하기(Lv.2, 블라인드 재도전, 왕복 2회, 스택). 반복문 증감 방향 실수 2회 반복 발생, 반면 변수 스코프 이해와 "스택의 이중 반전" 개념은 스스로 도출하는 긍정 신호 확인.

## 예고된 다음 문제
- **사라지는 발판(Lv.3), 징검다리(Lv.4) — SKT 코테(8/31 토) 종료 후 재개 예정으로 계속 이월.**
- SKT 코테 전까지(당분간): 신규 고난도 진입 대신, 구현/시뮬레이션·스택/큐·해시·그리디 중심의 **블라인드 재도전형 복습**을 우선 배치할 것.
- 4장 문자열 챕터 재검증 필요 (신규 아이디 추천 오표기 발견으로 신뢰도 재점검 범위 확대).

## 최근 다룬 핵심 유형 (중복 방지 체크리스트)
- BFS (미로/리코쳇 로봇 등, 단어 변환 — 암묵적 그래프 재응용)
- 그리디 - 투 포인터
- 해시맵 카운팅 (정렬 + Map 조합)
- 최소 힙(PriorityQueue) 기반 구간 스케줄링 (호텔 대실)
- 큐 두 개 시뮬레이션 (순환 배열) (두 큐 합 같게 만들기)
- 구간 합 / 차분 배열 (Difference Array) (파괴되지 않은 건물)
- 이분 탐색 (파라메트릭 서치) — 신규 도입 완료 (입국심사)
- 수학적 완전탐색 (약수 관계) — 신규 도입 완료 (카펫)
- 그래프 최단경로 (플로이드-워셜) — 신규 도입 완료 (합승 택시 요금)
- DP (동적 계획법) — 신규 도입 완료 (정수 삼각형, Top-down 누적 방식)
- Union-Find(분리집합) + MST(크루스칼) — 신규 도입 완료 (섬 연결하기)
- 그래프 단일 출발점 최단경로 (다익스트라, PriorityQueue + Lazy Deletion) — 신규 도입 완료 (배달)
- DP 2차원 격자형 (min(위, 왼쪽, 대각선) + 1 패턴) — 신규 도입 완료 (가장 큰 정사각형 찾기)
- 플로이드-워셜의 "관계 추론(도달 가능성)" 응용 — 신규 도입 완료 (순위)
- 다단계 문자열 규칙 순차 처리 (7단계형) — 재응용 완료, 왕복 0회 (신규 아이디 추천)
- 재귀(DFS) 기반 "포함/제외" 조합 생성 — 메뉴 리뉴얼 패턴 재응용 완료 (순위 검색)
- 해시맵 전처리 인덱싱 (등록 시점에 미리 다수 키로 펼쳐두기) — 신규 도입 완료 (순위 검색)
- 이진 탐색 lower-bound 응용 ("특정 값 이상 개수 세기") — 입국심사 패턴 재응용 완료 (순위 검색)
- BFS depth 관리 (Node+depth 묶음 방식) — 재응용 완료, 왕복 1회 (단어 변환)
- 모노토닉 스택 (Monotonic Stack) — 신규 도입 완료, 왕복 4~5회 (주식 가격)
- 백트래킹(DFS) 기반 조합 배정 + Set을 이용한 순서무관 중복 제거 — 신규 도입 완료, 왕복 1회 (불량 사용자)
- 재귀(Recursion) 기반 "믿음의 도약" (from/to/via 역할 교대) — 신규 도입 완료, 왕복 0회 (하노이의 탑)
- 트리 구성(좌표 기반 BST 삽입) + 전위/후위 순회 재귀 — 신규 도입 완료 (길 찾기 게임)
- 1차원 순수 재귀 (분할 후 위임 패턴, `cut(M,N)=N+cut(M-1,N)`) — 신규 도입 완료, 왕복 3회 (종이 자르기)
- 부모 체인 단방향 재귀 (트리형이지만 한 방향으로만 타고 올라가는 단순 구조) — 신규 도입 완료, 왕복 1회 (다단계 칫솔 판매)
- 백트래킹 + 1차원 압축 설계 (2차원 보드 대신 depth당 값 하나만 기록) — 재응용 완료, 왕복 2회 (N-Queen)
- 연속 구간(Run-length) 그룹핑 + 대표 원소 키 설계 (TreeMap 활용 정렬) — 신규 도입 완료, 왕복 1회 (외톨이 알파벳)
- 백트래킹(DFS) 기반 순열 배정 (1:1 매칭 최적화) — N-Queen 패턴 재응용, 왕복 거의 0회 (체육대회)
- 역방향 재귀 (자식→부모 인덱스 역산, 몫/나머지 활용) — 다단계 칫솔 판매 패턴 재응용 + 신규 수식, 왕복 2회 (유전법칙)
- 이벤트 기반 시뮬레이션 + 우선순위 큐 이중 관리 (대기열/실행대기열 분리) — 신규 도입 완료, 왕복 8회 내외 (운영체제)
- 좌표평면 방향탐색 (dx/dy 순환 배열, 격자 BFS 패턴 재적용 시 부호 재검증 필요) — 재응용 완료, 왕복 4회 (실습용 로봇)
- 그리디 + 최소 힙 반복 병합 (허프만 스타일) — 신규 도입 완료, 왕복 0회 (신입사원 교육)
- 구간 겹침 최대 개수 (투 포인터) — 호텔 대실 패턴 재응용 완료, 왕복 7~8회 (카페 확장)
- 3차원 상태 공간 BFS (좌표 + 아이템 사용 여부 함께 추적) — 신규 도입 완료, 왕복 6회 이상 (보물 지도)
- 트리(이진 트리) + 백트래킹, 정적 구조/동적 상태 분리 관리 — 신규 도입 완료, 왕복 8회 이상 (양과 늑대)
- Trie(트라이) + 접두사/접미사 와일드카드 매칭 (정방향/역방향 이중 트라이) — 신규 도입 완료, 왕복 15회 이상 (가사 검색)
- 노드별 카운팅 자료구조의 공간복잡도 최적화 (고정 크기 배열 → 해시맵 전환) — 신규 도입 완료, 동일 세션 내 재응용 (가사 검색)

## 다음 회차 후보 유형 (Day 139+, SKT 코테 이후)
- 사라지는 발판(Lv.3) — 게임 이론형 완전탐색, 신규 유형.
- 징검다리(Lv.4, 이분 탐색/파라메트릭 서치 재응용) — 계속 이월.
- 4장 문자열 챕터(Lv.0~1, 8문제 미검증) — 진짜 신규인지 재확인 필요, 우선순위는 낮음.

## Lv.4 진입 판단 (Day 137 기준 갱신)
- **긍정적 신호 강화:** 체육대회(백트래킹, N-Queen 재응용)를 코치 개입 최소한으로 첫 코드에서 바로 통과 — 재귀/백트래킹 감각이 "재귀"라는 표찰이 없는 완전탐색형 문제에도 자연 전이됨을 확인. 재귀 관련 재검토 기준(2번: 왕복 3~4회 이내)은 사실상 충족.
- **신규 긍정 신호 (Day134):** 보물 지도(좌표 기반 BFS)에서 배열 크기를 `n+1`로 지적 없이 스스로 설계함 — 그래프형 배열 크기 재검토 기준(1번)에 대한 첫 데이터 포인트 확보.
- **미해결/신규 신호:** "선언은 했지만 반환 흐름에 연결 안 됨" 패턴이 재귀 호출 결과 미반환, 분기 일부 경로 return 누락 등으로 재발하여, 이 패턴군 자체의 완전한 해소는 아직 진행형. "상태 공간을 확장할 때 관련된 모든 참조 지점에 일관되게 반영하지 못하는" 유형의 실수가 Day134~135에 반복 관찰됨.
- **신규 데이터 (Day135):** 트리+백트래킹이라는 완전 신규 패턴에서 왕복 8회 이상 소요됨 — 신규 패턴 도입 세션 특성상 재검토 기준 2번은 아직 미충족이나, 막힐 때마다 원인을 스스로 좁혀나가는 디버깅 태도는 안정적으로 유지됨.
- **신규 데이터 (Day136):** 완전히 새로운 자료구조(Trie)를 개념 설명만 듣고 스스로 설계(정/역방향 이중 구조, Node 필드 구성)해내는 능력이 이번에도 확인됨 — Day135(트리)에 이어 **2세션 연속으로 "새 자료구조를 처음 도입할 때 구조적 설계 자체는 코치 개입 없이 해낸다"**는 패턴이 관찰됨. 또한 이번 세션에서 처음으로 **"시공간 복잡도를 스스로 계산해서 자료구조 선택을 바꾸는" 능력**이 관찰됨(노드당 메모리 40,004바이트 × 최대 노드 수 200만 ≈ 80GB라는 계산을 직접, 정확히 해냄) — 기존 Lv.4 재검토 기준에는 없던 새로운 차원의 긍정 신호. 다만 Map API(`getOrDefault`, `computeIfAbsent`) 문법에서는 인자 개수/순서, 필드-메서드 구분 관련 실수가 같은 세션 안에서 여러 차례 반복됨 — 재검토 기준 3번("같은 세션 안 동일 지적 반복 금지")과는 결이 다르지만(배열 크기/경계조건이 아니라 "새 API 문법" 범주), 계속 관찰이 필요한 지점.
- **결론:** 새로운 자료구조를 스스로 설계하는 능력은 Day135~136 연속으로 뚜렷하게 확인되어 Lv.4 진입 감각의 핵심 축(구조 설계력)은 안정적으로 자리잡은 것으로 판단됨. 반면 "새 API를 정확한 시그니처로 실전에서 바로 사용하는 것"은 아직 반복 학습이 필요한 영역. 사용자 본인은 이 시점에서 Lv.2~3 기본기 재점검을 원함 — 코치 판단으로는 Lv.4 진행에 필수 전제조건은 아니나, API 문법 숙달이 목적이라면 유효한 선택.
- **긍정적 신호 추가 (Day137):** Day136에서 지적됐던 Map API(`getOrDefault`/`computeIfAbsent`) 인자 개수·시그니처 혼동이, 순위 검색 블라인드 재도전에서 **단 한 번도 재발하지 않음.** 재검토 기준 3번("같은 세션 안 동일 지적 반복 금지")과는 다른 축이지만, 취약 포인트 목록에서 처음으로 "해소 확인" 처리 가능한 항목이 나옴.
- **신규 관찰 (Day137):** 이진 탐색 `mid` 재계산 위치(루프 밖 vs 안), 제어 흐름의 배타적 분기(`if-else` vs 연속 `if`) 관련 실수가 새로 나타남 — 기존 "상태 갱신 참조 지점 누락" 계열과는 다른, "반복문/분기문 골격 설계" 계열의 실수로 별도 관찰 필요.
- Lv.4 진입 판단 결론(Day136)은 유지: 새 자료구조 스스로 설계하는 능력은 안정적으로 확인됨. Day137은 Lv.2~3 복습 세션이라 이 판단에 직접적 갱신 근거는 없음.
- **참고할 만한 신규 신호 (Day138):** 이번 세션은 시험 대비로 방향 전환되어 직접적 신규 데이터는 없으나, 자료구조가 아닌 "연산의 성질"(반전 두 번 = 복원) 자체를 스스로 도출한 것은, 향후 더 복잡한 상태 변환 문제(Lv.4)에서도 도움이 될 수 있는 새로운 차원의 직관.

### Lv.4 진입 재검토 기준 (변경 없음)
1. 새로운 그래프형 문제를 처음 마주쳤을 때, 배열 크기를 지적 없이 스스로 n+1로 잡는 것이 **2~3세션 연속**으로 확인될 것
2. 한 문제당 코드 제출 왕복이 평균 **3~4회 이내**로 줄어들 것
3. **같은 세션 안에서** 동일한 지적(변수명, 배열 크기, 경계 조건 등)을 두 번 이상 반복해서 받지 않을 것

## 최근 취약 포인트 (누적 추적용)
- **재발 (Day138):** 반복문 증감 방향(`i++`/`i--`) 실수 — 2회 발생(그 중 1회는 무한루프+인덱스 초과 위험). "이 루프가 커지는 방향인지 줄어드는 방향인지" 코드 짜기 전에 소리 내어 확인하는 습관 강화 필요.
- **재발 (Day138):** 변수명 불일치(`isNext` 선언 vs `is_next` 사용) — 기존 "for문 증감식/변수명 오타" 항목과 동일 계열, 계속 관찰.
- **재발 (Day138):** 존재하지 않는 메서드명으로 먼저 시도하는 경향 (`Character.isUpper/isLower` — 실제로는 `isUpperCase/isLowerCase`; `Deque` import 누락) — "새 메서드 사용 전 시그니처/존재 여부 확인" 습관, 계속 관찰 대상.
- **신규 (Day138):** 조건 없이 무조건 실행되는 문장이 직전 로직의 결과를 덮어쓰는 실수 (프렌즈4블록 중력 로직에서 `if` 없는 `n_board[i][j] = '0'`이 방금 채운 값을 지움) — "이 줄이 매번 무조건 실행되는가"를 점검하는 습관으로 일반화해서 추적할 것.
- **✅ 긍정 신호 (Day138):** 변수 스코프(유효범위)를 스스로 정확히 인지함 — 블록 밖에서 지역 변수에 접근하려다 막힌 게 아니라, 시도하기 전에 "범위가 아니잖아"라고 먼저 판단함.
- **✅ 긍정 신호 (Day138):** 스택의 "이중 반전으로 순서 복원" 개념을 힌트 없이 스스로 제안함 — 자료구조 선택 시 표면적 패턴 매칭이 아니라 연산의 성질 자체를 추론하는 능력 확인.
- **✅ 해소 확인 (Day137):** Map API 시그니처(인자 개수/순서) 혼동 — Day128, Day136에서 반복됐던 문제가 Day137 순위 검색 블라인드 재도전에서 재발 없이 정확히 사용됨. 계속 관찰하되 "해소됨" 표시로 격하 가능.
- **신규 (Day137):** 반복문/제어 흐름 골격 설계 실수 — ① 이진 탐색에서 `mid` 계산을 while 루프 밖에 한 번만 두어 무한루프 위험 발생, ② 재귀 함수에서 `if(depth==4)`와 `if(depth==0)`를 배타적이지 않은 연속 `if`로 작성해 의도치 않은 중복 호출 발생 (이후 `if-else` + `return`으로 스스로 교정). "이 블록이 끝나면 코드가 계속 아래로 흘러가는가"를 항상 점검하는 습관 강화 필요.
- **신규 (Day137, 경미):** 메서드 인자 개수 오기억 — `String.trim(" ")`(존재하지 않는 오버로드), `Map.getOrDefault(key, 0)`(타입 불일치) — 새 메서드를 쓸 때 시그니처를 먼저 확인하지 않고 직관으로 인자를 채우는 경향은 계속 관찰 대상.
- **경로 위의 "시작 노드(root)" 처리 누락 (Day136 신규):** Trie에서 각 노드마다 카운팅을 누적하는 로직을 `for`문 안에만 넣어서, 확정 글자가 0개인 쿼리(`"??"`)를 검색할 때 정작 `root` 자신은 한 번도 카운팅되지 않는 문제가 발생함. Day135(양과 늑대)의 "루트 이중 카운팅"과는 반대 방향이지만 같은 뿌리의 실수 — "경로의 양 끝(시작점/끝점)이 일반 반복 로직에 자연스럽게 포함되는지"를 항상 별도로 확인해야 한다는 원칙으로 일반화할 수 있음. 단, 원인 자가진단 자체는 유도 질문 이후 정확하고 신속하게 해냄 — 긍정적 신호.
- **"시각(timestamp)"과 "기간(duration)" 혼동 (Day134):** 카페 확장에서 도착 시각(절대적 시점)과 제조 시간(상대적 기간)을 `Math.max`로 섞어 비교하려는 실수가 반복됨. 변수 이름을 `startTime`/`endTime`처럼 명확히 분리한 뒤에야 교정됨.
- **상태 공간 확장 시 참조 지점 전체의 일관성 누락 (Day134):** 보물 지도에서 2차원 visit을 3차원(좌표+신발상태)으로 넓히는 아이디어 자체는 빠르게 캐치했으나, 초기화·범위체크·방문마킹 등 관련된 모든 참조 지점에 빠짐없이 반영하는 데 여러 차례 왕복이 필요했음.
- **구조(자료구조) 확장 시 참조 지점 전체 갱신 누락 (Day135 재발):** 양과 늑대에서 candidate 리스트 관련으로 재발함 — "자식들을 candidate에 추가"는 바로 떠올렸지만, 백트래킹 원복 시 "자식들을 candidate에서 다시 제거"하는 것은 별도 지적 후에야 반영함.
- **초기화 단계의 경계 조건(이중 처리) 실수 (Day135):** 루트 노드를 "이미 방문 완료" 상태로 세팅하면서, 동시에 candidate 목록에도 루트 자신을 넣어버려 재귀 진입 시 루트가 다시 한번 카운트되는 실수 발생.
- **조건식 설계 시 AND/OR 혼동 및 상수 오용 (Day133):** 운영체제에서 `while(!pq.isEmpty() && idx<10)`처럼 논리연산자 방향 착오, 배열 길이 대신 무관한 상수 사용. 반례와 손 시뮬레이션으로 매번 스스로 교정함.
- **배열 인덱싱이 이미 부호를 담고 있음에도 위에 중복 분기(if)를 씌우는 습관 (Day133):** 실습용 로봇에서 `dx[d]`,`dy[d]` 값 자체로 충분한데 `if(d==0||d==2)` 식 분기를 추가로 씀.
- **격자탐색 부호를 다른 도메인(좌표평면)에 무비판적으로 재사용 (Day133):** 예시 손 검증으로 즉시 자가 교정함 — 개선 신호.
- **"선언은 했지만 반환 흐름에 연결 안 됨" 패턴의 새로운 변형 (Day132):** 재귀 호출 자체를 호출만 하고 결과를 받지도 리턴하지도 않는 형태로 재발함(유전법칙).
- **분기가 있는 함수에서 "모든 실행 경로가 값을 리턴하는가" 확인 누락 (Day132):** 유전법칙에서 `if`문만 작성하고 `else`를 빠뜨림.
- **char 리터럴과 String 리터럴 혼동 (Day131):** `'-'`(char)와 `"-"`(String)를 `.equals()`로 비교하며 항상 false가 나와 NPE 발생. Day132에서 개선 확인.
- **배열 인덱스와 저장값의 역할 혼동 (Day131):** N-Queen에서 `g_col[row] = col`과 `g_col[col] = row`를 헷갈림.
- **빈 컬렉션에서 peek()/pop() 호출 순서 실수 (Day129):** `!stack.isEmpty() && 조건` 순서(short-circuit)로 방어해야 함.
- **백트래킹 시 방문 배열(visit) 초기화 위치 오류 (Day129):** 재귀 함수 내부에서 매번 재생성하면 상태가 소실됨. Day132에서 개선 확인.
- **원본 문자열 vs split 결과 배열 인덱싱 혼동 (Day128).**
- **Map 조회 API 선택 혼동 (Day128):** `get`/`getOrDefault`/`computeIfAbsent` 차이 — Day136에서도 문법 세부사항 관련해 계속 관찰 대상.
- **그래프형 배열 크기 설계 실수 (관찰 지속, Day134 긍정 데이터 1건).**
- **대칭 관계 단방향 체크 누락 (Day127 발견):** 관찰 지속.
- **if-else 배타적 분기 처리:** Day128 이후 개선 신호 유지 중.
- 코드 신뢰보다 검증 우선, 경계 조건(등호) 민감도, 상태 동기화 누락, for문 증감식/변수명 오타, 다차원 배열 인덱스 범위 혼동, 이분 탐색 종료 조건/경계 이동 방향 착오, 조건 반전 실수 — 기존 항목 유지, 계속 추적 필요.

## 참고: check_List 파일 신뢰도 이슈 (Day 128 발견, Day 130/131 추가 확인)
- Day 128 문제 선정 과정에서 `check_List`의 "미풀이로 추정" 표시 다수가 실제로는 이미 오래전에 풀린 문제로 확인됨 (예: 거리두기 확인하기 Day49, 가장 큰 수 Day13, 디스크 컨트롤러 Day23, 다리를 지나는 트럭 Day17, 보석 쇼핑 Day76, 두 개 뽑아서 더하기 Day1, 등굣길 Day93, 양궁대회 Day44, 메뉴 리뉴얼 Day49 등).
- **Day 130 추가 확인:** 계획했던 3문제(가장 큰 수 Day13, 다리를 지나는 트럭 Day17, 등굣길 Day93)가 전부 중복으로 드러남. 모음 사전(Day41), 소수 찾기(Day15), 보석 쇼핑(Day76), 디스크 컨트롤러(Day23)도 이미 완료된 문제임을 확인.
- **Day 131 추가 확인:** 재귀 문제 선정 과정에서 모음 사전(Day41), 양궁대회(Day44), 타겟 넘버(Day9/90/107), 괄호 변환(Day52)이 또 "미풀이로 추정"으로 오표기되어 있던 것을 발견.
- **Day 132 확인:** 외톨이 알파벳/체육대회/유전법칙 3문제 모두 project_knowledge_search로 사전 교차검증 완료, 오표기 이슈 없음.
- **Day 133 확인:** 운영체제/실습용 로봇/신입사원 교육 3문제 모두 교차검증 완료, 오표기 이슈 없음.
- **Day 134 확인:** 카페 확장/보물 지도/양과 늑대 3문제 모두 교차검증 완료, 오표기 이슈 없음.
- **Day 136 확인:** 가사 검색/징검다리 모두 project_knowledge_search로 사전 교차검증 완료, check_List 표시(미풀이)와 실제 상태 일치 — 오표기 이슈 없음. (징검다리는 검증만 완료, 실제 풀이는 다음 세션으로 이월)
- **Day 137 대규모 재확인:** 순위 검색(72412, 문제번호 자체도 오기재), 신고 결과 받기(Day53), 베스트앨범(Day88), 가장 큰 수(Day130), 피보나치 수(Day112), 메뉴 리뉴얼/거리두기 확인하기(Day49), 단속카메라(Day115), 양궁대회(Day44) 등 다수가 "미풀이"로 잘못 표시되어 있던 것을 확인. **check_List의 8~11장, 13장 대부분이 신뢰할 수 없는 상태 — 조만간 README.md 전체 기준으로 전면 재검증 권장.**
- **Day 138 추가 확인:** 4장 문자열의 "신규 아이디 추천(Lv.1)"도 "미풀이로 추정"으로 오표기되어 있었으나, 실제로는 Day128에 `NewIdRecommend.java`로 이미 완료됨. 오표기 범위가 8~13장뿐 아니라 4장까지 확대됨을 확인 — 전면 재검증 필요 범위가 넓어짐.
- **권장:** 여유 있을 때 `check_List`를 README.md 전체 기준으로 재검증해서 갱신할 것. 그 전까지는 매일 문제 선정 직후, 반드시 project_knowledge_search로 교차 확인 후 세트를 확정할 것.

## 사용 안내 (이 파일의 목적)
- 이 파일은 매일 문제 선정 시 README.md 전체를 다시 검색하지 않고 최근 상태만 빠르게 파악하기 위한 경량 상태 파일입니다.
- 매일 훈련이 끝나면, README.md 갱신과 함께 이 파일의 현재 Day, 최근 5일 요약, 예고된 다음 문제, 취약 포인트 섹션을 갱신해주세요.
- 최근 5일 요약은 6일째부터는 가장 오래된 항목을 삭제하여 항상 5개 내외로 유지하는 것을 권장합니다.
- "Lv.4 진입 판단" 섹션은 재검토 기준이 충족되기 전까지는 매 세션 종료 시 근거(제출 왕복 횟수, 반복 지적 여부)를 간단히 업데이트해주세요.
- **[README 출력 양식 — 2026.08.12 갱신]** 매일 README.md에 반영할 Day 정리는 다음 구조를 따른다:
  1. `### Day N (YYYY.MM.DD) - 핵심 유형 요약` 헤더
  2. 그 아래 1~2문장짜리 소개 단락 (그날 새로 체화한 개념 요약, 이월 여부 명시)
  3. `난이도 | 문제 이름 | 파일 이름 | 핵심 정리` 표 — 각 문제의 "핵심 정리" 칸 안에 왕복 횟수를 굵게 명시하고, 번호 매긴(1. 2. 3...) 세부 학습 포인트를 이어서 적는다.
  4. 마지막에 `#### 💡 Today I Learned (TIL)` — 번호 매긴 리스트, 각 항목은 굵은 소제목: 설명 형태.
  - 채팅에 출력할 때는 내부에 마크다운 헤더/표/굵은 글씨가 섞여 있어 렌더링이 깨질 수 있으므로, 항상 4중 백틱(````)으로 전체를 감싸서 출력한다.
- **[Day130 신규 지침]** 문제 선정 직후, check_List 확인만으로 끝내지 말고 반드시 project_knowledge_search로 실제 미풀이 여부를 검증한 뒤 세트를 확정할 것.
- **[Day132 신규 지침]** PCCP류 문제는 "코딩테스트 연습" 검색으로 안 나올 수 있음 — 코스 페이지 직접 링크 안내할 것. 코치가 제시하는 문제 지문은 기억이 아니라 매번 검색/확인을 거친 것이어야 함.
- **[Day134 신규 지침]** 상태 공간(visit)을 확장할 때는, 확장 직후 "초기화 / 범위체크 / 방문마킹 / 큐 삽입값"의 4곳을 순서대로 훑어 누락된 지점이 없는지 체크하는 습관을 세션 도입부에서 짚어줄 것.
- **[Day136 신규 지침]** Lv.4 이상 문제는 제약조건에 "개별 원소 길이" 제한과 별개로 "전체 길이의 합" 같은 숨은 제한이 있는 경우가 많음 — 최악의 경우(메모리/시간)를 계산할 때 이 전체 합 제한까지 빠짐없이 검색해서 확인할 것. 또한 새 Map/컬렉션 API가 처음 등장할 때는 사용 전에 시그니처(인자 개수, 반환 타입)를 먼저 명확히 짚어줄 것.

## 파일 매핑 (Day138)
- Day138/CaesarCipher.java — 시저 암호 (Lv.1)
- Day138/Friends4Block.java — 프렌즈4블록 (Lv.2, 블라인드 재도전)
- Day138/RemovePairs.java — 짝지어 제거하기 (Lv.2, 블라인드 재도전)
- 다음 세션으로 이월 예정(SKT 코테 이후): 사라지는 발판(Lv.3), 징검다리(Lv.4)
